# Delta — `agent-loop-skeleton` (AG-21)

> **Change**: `cachicamas-agent-concurrency-hardening` · **AG-21** · Target: `openspec/specs/agent-loop-skeleton/spec.md`
> **Ops**: **MODIFIED** `R-LSK-008` — `S-LSK-032` amended by a narrow, recorded release (design AD-9). **ADDED** `R-LSK-009` + `S-LSK-034` — AG-21's own exact-suffix filter widening (design AD-10). **MODIFIED** the Explicit non-requirements list — the *"Multi-turn state — AG-21 … (Still open …)"* line (`agent-loop-skeleton/spec.md:263`) closes.
> **AG-21 requests NO release from `R-LSK-004`.** The `hooks_test.go` release below is **normative, not substrate**: `hooks_test.go` is not on `R-LSK-004`'s forbidden list and has been an exact-suffix entry in both filters since AG-20 (`hooks_test.go:1998-2002`), so the edit trips no mechanical guard. What is owed is a recorded release against the **implementing test of a promoted scenario**, and it is taken here.

## Header maintenance obligation at promotion

`sdd-archive` MUST make exactly these header edits in the target spec and no others:

1. **Scenario range** (`agent-loop-skeleton/spec.md:7`): extend the allocated range so it reads `S-LSK-001` through **`S-LSK-034`**, keeping the lettered bites and keeping the sentence that this header *"states the allocated range and never a total"*. **Do not add a count.**
2. **Amendment line** (mirroring `:9`'s AG-20 line, appended after it): record that `R-LSK-009` is ADDED and `R-LSK-008` is MODIFIED by AG-21; that AG-21 requests **no** substrate release from `R-LSK-004`; that both substrate filters widen by exact filename suffix for AG-21's five new test files; and that one narrow, recorded release branch-scopes `S-LSK-032`'s diff-dependent block in `hooks_test.go`, with the full account in this delta.

This is a **body** correction, not a header edit, and it is additional to the two header edits above: `sdd-archive` MUST ALSO correct `S-LSK-032`'s own scenario clause — "*neither names `failure.go`, `cost_events.go`, `cost_events_test.go`, `stream_check_test.go` or `reconstruction_test.go`*" — to name only the three files genuinely absent (`cost_events_test.go`, `stream_check_test.go`, `reconstruction_test.go`), dropping `failure.go` and `cost_events.go` (present in both filters since AG-11 and AG-16 respectively), with the `(Previously: …)` note specified in `S-LSK-032`'s own AG-21 amendment section below (sdd-verify round-2, MAJOR-3). No other scenario body changes.

## Not modified, and why

| Element | Verdict |
|---|---|
| `R-LSK-004` and its forbidden list | **Byte-unchanged. No release requested.** AG-21 adds only `package agent_test` files and, by default, no production source at all (`R-CNH-008`) |
| `R-LSK-007` (AG-19's widening requirement) | **Byte-unchanged.** AG-21 adds no pre-existing filename to either filter, because it releases none |
| `S-LSK-031` (`scope_fence_test.go:142-158`) | **Byte-unchanged, and load-bearing on AG-21.** Its identical-entry-set assertion is one of the two independent guards that catch a one-sided widening — see `S-LSK-034`'s bite |
| `S-LSK-033` and its three-site fatal-on-empty-diff scan | **Byte-unchanged and reproduced verbatim below.** The AG-9 release below does **not** touch an anti-vacuity floor: it scopes a **presence** loop, which is a different shape from `if diff == "" { t.Fatal }` and is not what that guard scans for |
| `R-LSK-001`, `R-LSK-002`, `R-LSK-003`, `R-LSK-005`, `R-LSK-006` | **Byte-unchanged.** AG-21 touches no turn behavior |

## MODIFIED Requirements

### R-LSK-008 — AG-20 requests NO substrate release, and its guard-filter widening is enumerated by name

**AG-20 requests NO release from `R-LSK-004`, and that conclusion is recorded rather than inferred — the AG-15, AG-17 and AG-19 shape, not the AG-16 or AG-18 shape.** AG-20 modifies **`loop.go`, `harness.go` and `compaction.go`** and creates one production file (`hooks.go`) plus three test files in `package agent_test`, all under `backend/agent/src/agent/`. **None of those three modified files is a member of `R-LSK-004`'s forbidden list**, and neither is any file AG-20 creates. Seven consequences MUST be stated rather than left to be inferred:

1. **No `EventKind`, so no event file opens — and the reason is structural rather than budgetary.** AG-20 registers **none**. The stalled-observer report is a Go-side typed value delivered off the streaming path, because an event announcing the stall **is** a path from the stalled observer back onto the producer's stream, which is exactly what `S-AGE-010` forbids and exactly the invariant AG-20.2 exists to close. `event.go`, `event_descriptor.go`, `event_registry_test.go`, `compaction_events.go`, `delegation_events.go`, `permission_events.go` and `cost_events.go` MUST all be byte-unchanged, and the event-kind guard MUST pass at its committed kind count.
2. **The stall types live in `hooks.go`, NOT in `failure.go`, and the placement is DECLINED explicitly rather than left to convenience.** `failure.go` is on `R-LSK-004`'s forbidden list. Declaring the stall report there would pass the mechanical filter check silently — the guards compare filenames, not intent — while violating the requirement's prose. `failure.go` MUST be byte-unchanged.
3. **No validator edit, because the observer lane emits nothing.** `stream_check.go` MUST be byte-unchanged. The lane holds no sink, stamps no event and touches no sequence; a run with observers registered produces a byte-identical stream to the same run without them (`R-HKS-007`). The fix is emitter-side by construction, never validator-side (`R-RUN-003`).
4. **No NEW package-wide guarantee FOR THE HOOK SURFACE ITSELF, so the contract table gains no new row for it.** AG-20 declares no `L2C` row of its own: it adds no upward surface beyond the existing stream (`L2C-03`) and no exported `History` route (`L2C-07`), and its shipped default is inert **by type**. `expectedLayer2ContractRows` MUST keep exactly the row COUNT and the row SET's own identities it carries at the merge base — no row added, no row removed. **AG-15's failover seam, AG-17's strategy seam and AG-19's publishing seam are the decisive precedents**: structurally identical — a caller-invisible seam with an inert default — and none added a row. **This is unrelated to, and unweakened by, consequence 7 below**: an EXISTING row (`L2C-08`, owned by `agent-cancellation-tree`, not by this delta) was AMENDED during `sdd-verify` for a reason that has nothing to do with the hook taxonomy's own upward surface — the observer lane's goroutine lifetime, not anything it publishes. `doc.go` and `doc_contract_guard_test.go` are consequently NOT byte-unchanged overall, even though this consequence's own reason for touching them never fired.
5. **No cost, history, scheduler or Layer 1 file opens.** `cost_events.go` (released for AG-16 only), `cost_events_test.go`, `cost_usage.go`, `history.go`, `sequence.go`, `run_events.go`, `turn_events.go`, `reconstruction_test.go`, `scheduler.go`, `tool.go`, `delegation_seam.go`, `go.mod` and `go.sum` MUST be byte-unchanged, as MUST every file under `backend/agent/src/ai/` and every file under `backend/agent/src/agenttest/` — the test gate primitive is **used**, not widened. AG-11's, AG-14's, AG-16's and AG-18's releases are **not** extended to AG-20: every file named forbidden in `R-LSK-004` is forbidden again here. **`scope_fence_test.go` and `cancellation_test.go` are each released, narrowly, per consequence 6 below — the one entry this paragraph deliberately no longer names.**
6. **`scope_fence_test.go` and `cancellation_test.go` each carry ONE narrow, recorded release — the AG-11 (`turn_events.go`/`failure.go`) and AG-18 (`doc.go`/`doc_contract_guard_test.go`) precedent, discovered and required during `sdd-apply`, not planned at design time.** `scope_fence_test.go`'s `TestScopeFence_S_TLS_020_SeamRidesBesidePolicySlotNotInsideIt` and `cancellation_test.go`'s `TestCancellation_NestedRunCancelsLeafFirst` (S-DEL-015) each contain an anti-vacuity floor over an empty `git diff` (`scheduler.go` for the first; `delegation_seam.go` and `scheduler.go` for the second) that reads `if diff == "" { t.Fatal(...) }`. That floor fires on **every** branch cut after AG-19 merged which does not itself touch the diffed file — every one, not an errant one, because `baseRefIsHEAD`'s own skip (`scope_fence_test.go`'s own helper, unedited) covers only the literal post-merge-checkout case. Verified directly against `main` (`2a138b59`): both tests pass there and both failed on AG-20's own branch, for a reason with no connection to AG-20's own changes and no connection to `scope_fence_test.go`'s or `cancellation_test.go`'s own OWNING requirements (`R-DEL-009`/`S-DEL-024`, `S-CAN-015`) — neither of which this release touches. **The release is bounded precisely**: each file's edit is confined to converting that ONE `if diff == "" { t.Fatal(...) }` branch into `t.Skip(...)` with an explanatory message (`cancellation_test.go`'s own shape differs slightly: the per-path branch becomes `continue` rather than `t.Skip`, with a `scannedAny` flag and a SEPARATE terminal branch — itself `t.Logf(...)` followed by `return`, not `t.Skip`, matching this same function's own pre-existing `baseRefIsHEAD` idiom a few lines above rather than diverging from it — that fires only if NEITHER path had anything to scan); every other assertion, every other line, is unedited. An empty diff means the absence the guard checks holds vacuously (there are no added lines to violate it) — that is a skip (or, where the file's own idiom is log-and-return, that), not a failure, and AG-19's own branch is unaffected (its diff was non-empty, so the scan ran as shipped). Left unfixed, this identical shape would fire on AG-21, AG-22 and AG-23 alike. A regression guard, `TestHooks_AntiVacuityFloorsSkipRatherThanFail` (`agent-hook-taxonomy`, `S-LSK-033` below), source-scans all three sites — including its own — so this cannot silently regress; its own first version was itself found dead by `sdd-verify` (CRITICAL-1) and repaired in the same correction round consequence 7 belongs to.
7. **`doc.go` and `doc_contract_guard_test.go` each carry ONE narrow, recorded release — the SAME AG-18 precedent named in the "Not modified" table above, taken a second time for an unrelated reason — discovered and required during `sdd-verify` (CRITICAL-2), not planned at design time and not the release consequence 4 declined.** `L2C-08`'s package-wide contract row (`R-CAN-008`, owned by `agent-cancellation-tree`, not by this delta) claimed *"every goroutine the package itself owns has exited"* past the wind-down bound; AG-20's own observer lane goroutine may legitimately still be running, by design, while blocked inside a permanently stalled observer (`agent-hook-taxonomy`'s `R-HKS-008` consequence 1), so the claim was false of the shipped code the moment `hooks.go` shipped, and no delta recorded it until now. **The release is bounded precisely**: `doc.go`'s edit is confined to widening `L2C-08`'s existing third-party-tool carve-out to also name a permanently stalled third-party observing-hook invocation — reported typed by hook point and registration index, the identical mechanism the row already uses — and `doc_contract_guard_test.go`'s edit mirrors that text byte-for-byte into `expectedLayer2ContractRows` plus adds one new test, `TestDocContract_L2C08CarveOutClause`, following `TestDocContract_L2C07BothClausesTogether`'s own AG-18 shape exactly. No other row, no other clause, is touched. `expectedLayer2ContractRows` therefore does NOT keep exactly the row set it carried at the merge base **in TEXT** — consequence 4's own claim is about the row SET's identities and COUNT, both unchanged, not about every row's byte-exact text staying frozen forever, which was never literally true even before AG-20 (`L2C-07` itself was amended once, by AG-18). Full account: the `agent-cancellation-tree` delta (`R-CAN-006`, `R-CAN-008`, `S-CAN-016`, `S-CAN-017`).

8. **AG-21 carries ONE further narrow, recorded release against `S-LSK-032`'s IMPLEMENTING TEST — and it is a NORMATIVE release, not a substrate one.** `hooks_test.go` is not on `R-LSK-004`'s forbidden list and has been an exact-suffix entry in both filters since AG-20's own widening below (`hooks_test.go:1998-2002` asserts it), so the edit trips no mechanical guard; what is owed is a record that AG-21 edits the test implementing a **promoted** scenario of this requirement. The full account is `S-LSK-032`'s own amendment paragraph below. In one sentence: `TestHooks_SubstrateFilters_NoReleaseExactWideningInBothFilters`'s diff-dependent block (`hooks_test.go:1951-1993`) asserts a claim that was only ever true of **AG-20's own authoring branch**, and on every branch cut from `main` after AG-20 merged it fires three `t.Errorf` at `hooks_test.go:1990` — verified in this worktree, and reproduced by the coordinator at `54476ded` plus one empty commit. The block is branch-scoped; the diff-independent filter-entry half (`:1995-2010`) keeps running unconditionally on every branch.

**The same widening discipline binds AG-20, and its additions are enumerated BY NAME and NEVER BY COUNT**, because a count goes silently false the moment a later milestone appends — the property `S-LSK-020` exists to protect. `harness.go`, `harness_test.go`, `loop.go`, `loop_test.go`, `loop_hook_test.go`, `scheduler.go`, `tool.go`, `doc.go`, `doc_contract_guard_test.go` and every filename AG-19 added are **already** named in both substrate filters and need no addition — **so AG-20 adds no pre-existing filename, because it releases none.**

Both filters SHALL widen by one **exact filename suffix** for each of exactly these four:

- `/hooks.go`
- `/hooks_test.go`
- `/hooks_harness_test.go`
- `/hooks_compaction_test.go`

The **identical** four SHALL be added to **both** `filterOutLoopFiles` (`loop_test.go:837`) and `filterOutLoopHookFiles` (`loop_hook_test.go:907`). Adding to one and not the other is a defect this requirement forbids by name.

**Editing those two test files is MANDATORY, not optional, and it is NOT a release.** `compaction.go` is likewise **not** a member of the forbidden list, so its modification takes no release either. Without the four filter entries, `hooks.go` and its three test files would appear unstripped in the substrate-untouched guard's diff (`loop_test.go:1329-1335`) and the guard would fail on files the milestone is entitled to add. The edit is **confined to the two filter lists**: no assertion in either file changes, and AG-08's assertions in `loop_hook_test.go` are untouched. Both files are already named in both filters, so the fact that this change edits them is itself covered by the existing entries and constitutes **no release from `R-LSK-004`**, whose forbidden list names neither.

No wildcard, no prefix match, no directory-level relaxation; the two filters SHALL remain **byte-in-sync** with each other; and any file AG-20 adds that is not so named MUST still fail the guards. **`failure.go`, `cost_events.go`, `cost_events_test.go`, `stream_check_test.go` and `reconstruction_test.go` are each absent from AG-20's additions**, and each absence is deliberate: AG-20 MUST NOT edit any of them, and a filter entry would remove the guard that says so. Neither is any file under `backend/agent/src/ai/` or `backend/agent/src/agenttest/`. **No `agenttest` entry is added** — the filters do not govern that directory, and `agenttest` is byte-unchanged.

(Previously, at AG-21: this requirement's seven consequences carried no record that its own `S-LSK-032` scenario was implemented by a test whose diff-dependent block asserts an AG-20-authoring-branch-local claim, and which therefore fails on every descendant branch. A reader on such a branch would meet three failures naming `loop.go`, `harness.go` and `compaction.go` as "expected … to have changed" and could not tell from this requirement whether that was a defect in their own branch or in the guard.)

#### Scenarios

- **S-LSK-032** — **AG-20: no release taken from `R-LSK-004`, and the widening is exact, by name, in both filters.** Given the merge base of the AG-20 branch with `origin/main`, when `git diff` is taken over `backend/agent/`, then the set of pre-existing **non-test** files under `src/agent` that differ is exactly `{loop.go, harness.go, compaction.go}` **(plus `doc.go`, conditionally: see consequence 7 above — its own reason for changing is unrelated to `R-LSK-004`'s forbidden list and does not reopen this scenario's own claim)**; every file named forbidden by `R-LSK-004` is byte-unchanged, **`failure.go` included** (`doc.go` and `doc_contract_guard_test.go` are NOT included in this byte-unchanged claim — see consequence 7 above); the diff under `backend/agent/src/ai/` and under `backend/agent/src/agenttest/` is **empty**; the `go.mod`/`go.sum` diff is empty; `expectedLayer2ContractRows` carries exactly the row **set's identities and count** it carried at the merge base with no row added or removed (its `L2C-08` member's own TEXT is amended — a different claim, see consequence 7); and the event-kind guard passes at its committed kind count with AG-20 registering none. And when both substrate filters are read, then each names `/hooks.go`, `/hooks_test.go`, `/hooks_harness_test.go` and `/hooks_compaction_test.go` by **exact filename suffix**; the two filters carry an **identical** entry set; neither contains a wildcard, a prefix match or a directory pattern; neither names `failure.go`, `cost_events.go`, `cost_events_test.go`, `stream_check_test.go` or `reconstruction_test.go`; neither names any path under `agenttest`; and when the diff of `loop_test.go` and `loop_hook_test.go` against the merge base is read, then **each differs only within its filter's entry list** and no assertion in either file changed. And when an unnamed new file is placed in the package, then both substrate guards fail on it.

  **AG-21 amendment — the diff-dependent half is AG-20-AUTHORING-BRANCH-LOCAL, and that scope is now stated instead of being asserted universally.** Everything above stands **for AG-20's own authoring branch**, which is the only branch on which it was ever true. On any other branch it MUST NOT be asserted, and the implementing test MUST skip it by **logging and falling through**, never by skipping the whole test. Precisely:

  - **What is scoped.** The whole diff-dependent block of `TestHooks_SubstrateFilters_NoReleaseExactWideningInBothFilters` (`hooks_test.go:1951-1993`) — **both** the `allowed[]` loop (`:1977-1981`) and the `wantPreExisting` presence loop (`:1982-1992`). Two guards gate it, and **both are required**: (i) `nonTestChanged` is empty — the innocent-branch fast path, which is the default for a test-only milestone such as AG-21; and (ii) `nonTestChanged` is non-empty but does **not** contain `hooks.go`, AG-20's own new file and therefore that branch's authorship signature (`:1957-1962` already reasons about exactly this file). Guard (ii) is **not optional**: with the empty-diff guard alone, the identical failure returns the moment any later milestone legitimately edits a production file on its own branch — including AG-21's own pre-authorized contingency — because `nonTestChanged` becomes non-empty and both loops fire again.
  - **Why the failure is real and not a branch artefact.** `wantPreExisting = {loop.go, harness.go, compaction.go}` (`hooks_test.go:1975`) and its presence loop `t.Errorf("expected pre-existing non-test file %q to have changed, it did not", …)` (`:1990`) demand that three files AG-20 happened to touch be touched **again** by every descendant branch. On a branch cut from `main` after AG-20 merged, the merge base is `main` and the `src/agent/` diff carries none of the three, so all three errors fire. Re-verified in this worktree; the coordinator reproduced it at `54476ded` (passing) plus one `git commit --allow-empty` (failing with the identical three errors at `hooks_test.go:1990`).
  - **`t.Logf` and fall-through, NOT `t.Skip`, decided against the recorded ruling rather than around it.** `sdd-verify`'s MINOR-3 ruling is committed in source at `cancellation_test.go:240-252`: where a test's behavioral core has already run by the time the vacuous branch is reached, the branch MUST be `t.Logf` + `return`, never `t.Skip`, because *"marking the whole test 'skipped' would hide real, completed coverage from anyone scanning for it."* The same reasoning applies here with the direction reversed: this block sits **first**, and the diff-independent half **after** it — `extractHasSuffixEntries` over both filters (`:1995-2010`) — is real, completed, branch-independent coverage that a `t.Skip` would abandon and misreport as absent. `scope_fence_test.go`'s `t.Skip` at `:251-252` and `:258-270` is legitimate only because nothing but the scan follows it there. The scoping therefore logs and **falls through**, so the filter-entry half still runs.
  - **Teeth kept, enumerated rather than asserted.** The filter-entry half runs unconditionally on every branch, so both filters must still name AG-20's four files by exact suffix (`:1998-2010`); `TestHooks_ScopeFence_ByteUnchangedFilesAndNoNewKind` (`hooks_test.go:1514-1544`) keeps enforcing `R-HKS-010`'s byte-unchanged set, the empty `ai/` diff and the empty `agenttest/` diff on every branch; `TestTurn_SubstrateUntouched` (`loop_test.go:1322-1347`) keeps enforcing the substrate filter itself; and `S-LSK-031` keeps forcing the two filters byte-in-sync (`scope_fence_test.go:142-158`).
  - **Teeth necessarily lost, stated rather than glossed.** The branch-local exact-changed-set claim — that only those files changed, and that all three pre-existing ones did — is no longer asserted off AG-20's branch. It was never true off it. This is the identical semantics the shipped `S-TLS-020` fix already accepted (`scope_fence_test.go:258-270`). The `allowed[]` half is scoped **deliberately and on the record**: left live on a descendant branch it would falsely reject any later milestone's legitimate, delta-backed production change. The cross-branch "no unauthorized production edit" property is **not** lost — it remains owned by `TestTurn_SubstrateUntouched` and the byte-unchanged guard, both of which run unconditionally.
  - **The release is bounded precisely.** The edit is confined to adding those two guards and their log lines around the existing block. No assertion inside the block is deleted or weakened; the filter-entry half, the sorted-set comparison and every other line of `hooks_test.go` are unedited. `hooks_test.go` needs no new filter entry: it has been an exact-suffix entry in both filters since AG-20 (`hooks_test.go:1998-2002`).
  - **A second, unrelated correction to this same scenario's own text (sdd-verify round-2, MAJOR-3).** The verbatim reproduction above carries a false clause: "*neither names `failure.go`, `cost_events.go`, `cost_events_test.go`, `stream_check_test.go` or `reconstruction_test.go`*". Measured directly against the shipped filters: `/failure.go` **is** present in both (`loop_test.go:877`, `loop_hook_test.go:944`, legitimately released by AG-11), and `/cost_events.go` **is** present in both (`loop_test.go:978`, `loop_hook_test.go:1032`, legitimately released by AG-16). Only `cost_events_test.go`, `stream_check_test.go` and `reconstruction_test.go` are genuinely absent. The nearest implementing assertion (`hooks_test.go:2079-2084`) checks `countString(...) > 1` — i.e. no **duplicate** entry — which defends none of the five names against a false absence claim. `sdd-archive` MUST promote this scenario's clause corrected to name only the three genuinely-absent files, with a `(Previously: the clause named five files as absent from both filters, including two — `failure.go` and `cost_events.go` — that were already present, legitimately, since AG-11 and AG-16; the claim was false when AG-20 wrote it and no test ever defended it. Corrected by `sdd-verify`/`sdd-apply` round 2, AG-21.)` note attached to this scenario. `S-LSK-034` below is authored fresh by AG-21 and carries the corrected clause directly, never the inherited false one.

- **S-LSK-033** — **AG-20: the narrow release is bounded, and the fixed property cannot silently regress.** Given the merge base of the AG-20 branch with `origin/main`, when `git diff` is taken over `scope_fence_test.go` and over `cancellation_test.go`, then each diff is **non-empty** (the release fired, confirming this scenario is not vacuous); in `scope_fence_test.go`, the only changed lines convert one `if <diff-variable> == "" { … }` branch from a call to `t.Fatal` to a call to `t.Skip`, with every other line — every other assertion — byte-unchanged; in `cancellation_test.go`, the only changed lines convert its own per-path `if <diff-variable> == "" { … }` branch from a call to `t.Fatal` to `continue`, add a `scannedAny` flag set on the first non-empty path, and add a SEPARATE terminal branch — `t.Logf(...)` followed by `return`, matching this same file's own pre-existing `baseRefIsHEAD` branch a few lines above rather than diverging from it, never `t.Skip` — that fires only when NEITHER path had anything to scan, with every other assertion in the file byte-unchanged; when `TestScopeFence_S_TLS_020_SeamRidesBesidePolicySlotNotInsideIt` is run on a branch whose own diff against the merge base is empty for `scheduler.go`, then it reports `SKIP`, never `FAIL`; when `TestCancellation_NestedRunCancelsLeafFirst` is run on a branch whose own diff is empty for BOTH `delegation_seam.go` and `scheduler.go`, then it reports `PASS`, never `FAIL` and never `SKIP` — its behavioral core (the nested-cancellation-tree assertions preceding this branch) already ran and passed by the time the branch is reached, so marking the whole test "skipped" would misreport completed coverage as absent; and when the package's own source is scanned — comments stripped first, so a long explanatory comment inside the guarded branch cannot widen the gap the scan has to cross — for the shape `if <diff-variable> == "" { … t.Fatal`, across `scope_fence_test.go`, `cancellation_test.go` and every file AG-20 adds — `hooks.go`, `hooks_test.go`, `hooks_harness_test.go` and `hooks_compaction_test.go`, enumerated by name in the guard's own list rather than left to inference *(widened in `sdd-verify` round 2, W-2b: the list previously carried only one of the four, so this sentence over-claimed; the GUARD was widened to make the claim true rather than the claim narrowed to the guard)* — then the shape occurs **nowhere**, and this is DEMONSTRATED rather than merely claimed: the shape was planted back at each of the three sites in turn during `sdd-verify`'s correction round, the scan reported it every time, and each plant was reverted — so a future edit that reintroduces a fatal on an empty diff is caught mechanically rather than discovered on the next descendant branch. *(AG-21 update: unchanged in claim and byte-unchanged in source. AG-21's release scopes a **presence** loop, not an `if diff == "" { t.Fatal }` floor, so this guard's scanned shape is unaffected — and the scan is what proves that, rather than this sentence.)*

## ADDED Requirements

### R-LSK-009 — AG-21 requests NO substrate release, and every new test file enters BOTH filters by exact filename suffix

**AG-21 requests NO release from `R-LSK-004`** — the AG-15, AG-17, AG-19 and AG-20 shape. By default AG-21 creates only `package agent_test` files under `backend/agent/src/agent/` and modifies no production source at all (`R-CNH-008`). If D1's pre-authorized contingency fires and a production file is edited, that edit is delta-backed in its own right and takes its own release if the file is on the forbidden list; **this requirement pre-grants none.**

**Every new test file AG-21 adds under `backend/agent/src/agent/` MUST gain one entry in EACH of the two substrate filters, by exact filename suffix, in the same commit as the file that first needs it.** The obligation is mechanical, not stylistic: `TestTurn_SubstrateUntouched` (`loop_test.go:1322-1347`) diffs `backend/agent/src/agent/` against the merge base, strips it through `filterOutLoopFiles` (`loop_test.go:837`, exact-suffix entries from `:855`) and fails on anything left — *"substrate was edited (R-LSK-004 violated)"* (`:1345-1347`). An unnamed new file is exactly what is left.

Both filters SHALL widen by one **exact filename suffix** for each file AG-21 introduces. **No wildcard, no prefix match, no directory-level relaxation**, and the two filters SHALL carry an **identical** entry set — `S-LSK-031` (`scope_fence_test.go:142-158`) forces that identity, and AG-20's own half additionally pins its entries as exact suffixes (`hooks_test.go:1938-1941`, asserted `:1998-2010`). Adding to one filter and not the other is a defect this requirement forbids by name.

The concrete filenames are fixed by this change's `tasks.md` and MUST be identical in both filters; the design's planned set is `/combined_state_fixtures_test.go`, `/combined_matrix_test.go`, `/slow_consumer_pressure_test.go`, `/cross_run_state_test.go` and `/combined_leak_sweep_test.go`. **Any further file AG-21 adds — including one added by a fired contingency — takes the identical treatment**, which is why this requirement enumerates the obligation rather than a count.

`loop_test.go` and `loop_hook_test.go` are themselves **already** exact-suffix entries in both filters, so editing them to carry the new entries is **mandatory and is NOT a release**. The edit is **confined to the two filter lists**: no assertion in either file changes. **`failure.go`, `cost_events.go`, `cost_events_test.go`, `stream_check_test.go` and `reconstruction_test.go` are each absent from AG-21's additions**, and each absence is deliberate: AG-21 MUST NOT edit any of them, and a filter entry would remove the guard that says so. **No `agenttest` entry is added** — the filters do not govern that directory, and `agenttest` is byte-unchanged (`R-CNH-002`).

#### Scenarios

- **S-LSK-034** — **AG-21: no release taken, the widening is exact and in both filters, and each entry is proven necessary.** Given the merge base of the AG-21 branch with `origin/main`, when `git diff` is taken over `backend/agent/`, then every file named forbidden by `R-LSK-004` is byte-unchanged; the diff under `backend/agent/src/ai/` and under `backend/agent/src/agenttest/` is **empty**; the `go.mod`/`go.sum` diff is empty; the event-kind guard passes at its committed kind count with AG-21 registering none; and every production Go source under `src/agent/` is byte-unchanged unless a delta in this change names it. And when both substrate filters are read, then each names **every** test file AG-21 introduces by **exact filename suffix**; the two filters carry an **identical** entry set; neither contains a wildcard, a prefix match or a directory pattern; neither names `cost_events_test.go`, `stream_check_test.go` or `reconstruction_test.go` (`failure.go` and `cost_events.go` ARE named in both, legitimately, released by AG-11 and AG-16 respectively — this scenario does not claim their absence, only AG-21's own five new entries and the three files above); neither names any path under `agenttest`; and the diff of `loop_test.go` and `loop_hook_test.go` differs **only within each filter's entry list**, with no assertion in either file changed. **AND the widening is proven load-bearing rather than assumed (bite, RED-first):** given ONE of AG-21's new entries temporarily deleted from `filterOutLoopFiles` only, when the suite runs, then **two independent** failures are recorded — `TestTurn_SubstrateUntouched` fails with *"substrate was edited (R-LSK-004 violated)"* because the un-filtered new file remains in the stripped diff (`loop_test.go:1345-1347`), **and** `S-LSK-031`'s identical-entry-set assertion fails because the two filters now differ (`scope_fence_test.go:152-158`) — proving each entry is necessary and that the in-sync guard has teeth. RED-recorded with `-count=1` before the entry is restored.

## MODIFIED Explicit non-requirements

The list is reproduced in full; **three lines are back-annotated as closed and none is removed.**

- **No edit** to any substrate file in `R-LSK-004`. AG-04/05/06 rule engine, registry, descriptor vocabulary, tests stay untouched. *(Still true at AG-21, which requests no release — `R-LSK-009`.)*
- **No new top-level Go deps.** No `go.mod`/`go.sum` edit. No AG-03 guard edit. *(Still true at AG-21: `goleak` stays rejected, no ADR is written, and `go.mod`/`go.sum` are byte-unchanged — `R-CNH-008`.)*
- **Tools, hooks, errors beyond typed pass-through, permission, retry, context-check, cost events** — AG-08…AG-18.
- **Value-form `Harness`** — AG-13. AG-07 ships the function form only (D1). **CLOSED by AG-13**: the value-form `Harness` ships in this change and is owned by `R-RUN-001` in [`../agent-run-driver/spec.md`](../agent-run-driver/spec.md). AG-07's function form is unchanged and remains the only public one-turn surface.
- **Multi-turn state** — AG-21. AG-07 is stateless across calls (per `R-LSK-002`). **CLOSED by AG-21**, and closed by an **enumerated inventory with an absence assertion** rather than by a citation. AG-13 owns one run's iteration; state that outlives a **run** is now settled: the only state outliving a run is caller-owned or already enumerated by a shipped requirement, and the harness itself retains nothing. The inventory lives on `R-RUN-001` (`agent-run-driver`), and the proof is `S-CNH-014` (the absence half over a uniquely minted run-1 artifact, with an anti-ghost floor and a mandatory defeat test), `S-CNH-015` (the legitimate carry and the inventory row by row) and the defeat bite `S-CNH-016`. **The constraint on this capability is unchanged and still binding**: `Turn` remains stateless across calls per `R-LSK-002`, and nothing in this closure gives the one-turn surface memory.
- **Iteration of the model ↔ tools ↔ model cycle** — AG-13. AG-09 ships ONE cycle; AG-13 iterates. **CLOSED by AG-13**: iteration ships here, in the harness, by repeated `Turn` invocation — owned by `R-RUN-002`. `Turn` itself still schedules exactly one cycle per invocation (`R-LSK-006`, unweakened).
- **Test-convenience wrappers** in `backend/agent/src/agenttest` — AG-23. AG-07 uses `agenttest.Script` directly (per D4). *(Still open at AG-21, and deliberately: AG-21 uses the existing fixtures and widens `agenttest` by nothing — `R-CNH-002`, `S-CNH-006`. The wrappers remain AG-23's.)*
- **CI** — every gate runs when a human runs `make test` in `backend/agent/`. *(Still true at AG-21. Note that `make test` carries no `-count=1`, so AG-21's own evidence gate is `go test -race -count=1 ./...` with the wall-clock duration recorded — `NFR-CNH-002`.)*
