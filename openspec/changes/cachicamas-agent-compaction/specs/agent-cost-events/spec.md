# Delta for `agent-cost-events` — AG-18 adds ONE row to the per-path table; the iff sentence ships unchanged

> **Change**: `cachicamas-agent-compaction` · **AG-18** (Layer 2, Wave 4), `0003:1655-1760`
> **Modifies**: `agent-cost-events` ([`../../../../specs/agent-cost-events/spec.md`](../../../../specs/agent-cost-events/spec.md)) — `R-CST-001` (`spec.md:40-65`) only.
> **ADDITIVE, NOT A RELAXATION — and that distinction is this delta's whole substance.** The proposal (A8) framed this as an either/or: scope `R-CST-001`'s iff to model turns, or re-decide the dedicated bracket. `sdd-design` resolved it a third way (AD-3): the bracket stands **and** the iff is satisfied literally on every arm by construction. **Nothing is relaxed here, so nothing is owed in its place.** The iff sentence ships byte-unchanged; only the enumerated per-path table grows, because that table claims to enumerate every path and would otherwise be silently incomplete.
> **`stream_check.go` stays byte-unchanged**, verified verbatim in this worktree at `stream_check.go:161`: `if d.Placement == PlacementTurn && !turnOpen { return violation(...) }`. A dedicated compaction turn bracket satisfies that rule with no validator edit; that is why the bracket exists.
> **`R-CST-007`'s scope fence is not an obstacle**: its text is scoped to "**This capability**" — AG-16's own change — so it constrains AG-16, not AG-18. Verified by reading it in this worktree.
> **Ownership**: the compaction bracket, its event order and its explicit accumulation are owned by [`../agent-compaction/spec.md`](../agent-compaction/spec.md) (`R-CMP-003`, `R-CMP-008`). This delta owns only what the cost contract must now enumerate.

## MODIFIED Requirements

### R-CST-001 — A `cost_turn` is emitted iff the turn closes non-aborted, inside its own bracket, always labelled final

For **every** turn that closes non-aborted, the system MUST emit a `cost_turn` event carrying that turn's five token figures — input, output, cache-read, cache-write and reasoning — mapped from the `ai.Usage` the turn's `ai.Completion` reported, on the sink, **inside the turn bracket the turn opened and before that bracket's `turn_end`**. The rule is stated as an iff so that neither half is inferred: a non-aborted close always emits one, and an aborted close never does.

The event's `CostLabel` MUST **always** be `Final`. Per-turn usage is complete whenever it is known at all, because `ai.Completion` is the sole, at-most-once, terminal usage carrier. The `Estimate` half of the label axis is run-scoped and belongs to `R-CST-005`; a per-turn `Estimate` is unconstructible behavior under this requirement, not merely unused.

The per-path rule, enumerated rather than left to be inferred (design DD2):

| Path | `cost_turn`? | Why |
|---|---|---|
| Normal completion, on either `Turn` call path | **Yes**, `Final`, exact figures | the turn's usage was reported |
| Provider closes with no `Completion` (`S-ATT-012`) | **Yes**, `Final`, **all five figures absent** | nothing was ever reported; a first-class `R-CST-002` absence case, **not** an error and **not** five zeros |
| Mid-stream fatal (`loop.go:396-430`) | **No** | the turn closes aborted; `Completion` is `Terminal`, so no usage exists |
| Pre-stream abort (`R-LSK-001`'s three paths) | **No** | no data source exists (`provider_failure.go:320-330` carries no usage) |
| Cancellation mid-turn (AG-14 wind-down) | **No** | routes through the fatal path; `turn_end(Aborted)` with no invented figures |
| **AG-18's compaction bracket** (`R-CMP-008`) — a turn bracket opened by the compaction operation rather than by a `Turn` call | **Follows the same iff, on all three of its arms.** Completion obtained and committed → **Yes**, `Final`, from that completion's `ai.Usage`, bracket closes non-aborted. No completion (provider error or run cancellation) → **No**, bracket closes **aborted** carrying a typed failure. Completion obtained then rejected (non-`Stop` finish, or commit validation failure) → **Yes**, bracket closes non-aborted with its outcome mapped from the completion's own finish reason | the iff's unit is a **turn bracket**, and the compaction bracket is one; the spend on the third arm is real spend and is reported rather than hidden, which is what keeps the sentence true instead of making it convenient |

**The compaction row is additive and the iff sentence above is unchanged.** It is added because the table claims to enumerate every path: a bracket that closes without appearing in it would leave the enumeration silently incomplete, which is this repository's known spec-staleness shape. It is **not** a scoping of the word "turn" to model turns, and no reader may take it as one.

**The fold is explicit at the compaction emission site, and the contrary claim is false.** The accumulating read at `harness.go:567-576` — re-verified in this worktree — runs inside a goroutine draining `turnSink`, a channel created per attempt at `harness.go:563` and passed **only** to `Turn(...)` at `harness.go:590`. Compaction events are emitted on the harness's own lane and never traverse it, so a compaction `cost_turn` reaches the run cumulative **only** because its emission site performs the accumulation itself, against the same run-scoped accumulator. Any statement that the existing forwarder folds compaction spend automatically is **FALSE** and MUST NOT be repeated in a task, a comment or a later spec.

The placement is a validator obligation, not a convention: `cost_turn` is registered `PlacementTurn` (`event.go:329-331`), so `CheckStream` MUST accept the recorded cost-bearing stream **unmodified**, with `stream_check.go` **byte-unchanged**. The fix lands on the emitter, never on the validator (`R-RUN-003`, `agent-run-driver/spec.md:127`).

`Turn`'s exported signature MUST NOT change (`agent-turn-termination/spec.md:113`, `NFR-ATT-004`). The usage reaches the emission site through the turn's own accumulator, never through a returned value. **AG-18 adds no new `CostLabel`, no new payload field and no new event kind**; `cost_events.go`, `cost_events_test.go` and `cost_usage.go` are byte-unchanged.

(Previously: the per-path table enumerated five paths, all of them `Turn`-call paths, and the requirement made no statement about a turn bracket opened by anything other than a `Turn` call, nor about where a non-`Turn` bracket's spend is folded.)

#### Scenarios

- **S-CST-001** — **Charter AG-16.1 scenario 1, exactness half.** Given a multi-turn run whose provider scripts a distinct `ai.Usage` on each turn's `ai.Completion`, every one of the five figures present and pairwise distinct across turns — cache-read, cache-write and reasoning included, not only input and output — when the run is driven and the stream is recorded, then each turn bracket carries a `cost_turn` positioned after that turn's content events and **before** its `turn_end`; each such event's five figures equal that turn's scripted `ai.Usage` figure for figure; each carries `CostLabelFinal`; and `CheckStream` accepts the recorded stream unmodified with `stream_check.go` byte-unchanged.
- **S-CST-002** — **Charter AG-16.1 scenario 1, the no-`Completion` path.** Given a provider scripted to close its stream without ever emitting an `ai.Completion` (`S-ATT-012`'s shape), when `Turn` runs and the stream is recorded, then the turn still carries a `cost_turn` before its `turn_end`; every one of its five figures **reports absence**; no figure reports a present zero; the turn is not reported as failed by the emission; and `CheckStream` accepts the stream unmodified.
- **S-CST-003** — **The aborted half of the iff.** Given, in turn, a mid-stream fatal failure, each of `R-LSK-001`'s three pre-stream failure conditions, and an interrupt fired mid-turn, when each stream is recorded, then **no** `cost_turn` appears in the aborted turn's bracket in any of them; each bracket still closes exactly as its owning requirement already requires; and `CheckStream` accepts each stream unmodified. Cross-referenced to `R-ATT-005`, `R-LSK-001` and `R-CAN-002`.
- **S-CST-004** — **The label axis at turn scope.** Given every `cost_turn` observed by `S-CST-001`, `S-CST-002` and `R-CST-004`'s retry-bearing run, when each event's label is read, then each is `Final`, and no `cost_turn` anywhere on any recorded stream in this capability's suite carries `Estimate`.
- **S-CST-015** — **AG-18: the compaction bracket satisfies the iff on all three arms.** Given three runs — one whose compaction completes and commits, one whose compaction provider errors before any completion, one whose compaction completion returns a non-`Stop` finish reason — when each stream is recorded, then run 1's compaction bracket closes non-aborted and carries exactly one `cost_turn` labelled `Final` carrying that completion's usage figure for figure; run 2's closes **aborted** with a typed failure and carries **no** `cost_turn`; run 3's closes non-aborted and carries a `cost_turn`; the iff holds in both directions across all three; and `CheckStream` accepts all three streams unmodified with `stream_check.go` byte-unchanged. Cross-referenced to `R-CMP-003`.
- **S-CST-016** — **AG-18: the compaction spend reaches the run cumulative, by an explicit fold.** Given a run carrying both model turns and one successful compaction, when the run's `cost_session` is read, then its figures equal the sum over **every** `cost_turn` on the recorded stream, the compaction's included; and given a scratch tree in which the compaction emission site's accumulation statement is removed, when this scenario runs, then it FAILS short by exactly the compaction's scripted usage — the fold is asserted, not inherited.
