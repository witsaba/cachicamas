# Tasks: AI-26 — Translate normalized requests to wire requests

Package: `backend/agent/src/ai/openaicompat/` (extends AI-25). Runner: `make test` (`go test -race -v ./...`) from `backend/agent/`. Strict TDD active. Chain: tracker `feat/2026-08-03-cachicamas-ai-layer1-wave-4` ← 1 ← 2 ← 3 ← 4 ← 5 ← 6 ← 7 ← 8 (feature-branch-chain, order: 26.1 → 26.2 → 26.4 → 26.7 → 26.3 → 26.5 → 26.6 → 26.8).

**Hard sequencing gate**: `openaicompat/` does not exist yet. Slice 1 branches **only after AI-25's full chain merges** to the tracker. The reserved provider-extension namespace used in slice 4 (26.7) MUST be read from AI-25's **landed** artifact — never re-invented from this design.

## Review Workload Forecast

| Field | Value |
|---|---|
| Estimated changed lines (naive) | 1:~250–350 · 2:~200–260 · 3(26.4):~250–350 · 4(26.7):~300–450 · 5(26.3):~450–650 · 6(26.5):~150–230 · 7(26.6):~80–130 · 8(26.8):~300–500 · **Total ~1,980–2,920** |
| Estimated changed lines (corrected, 2–4× repo-confirmed undershoot) | 1:~500–1,000 · 2:~400–800 · 3:~500–1,000 · 4:~600–1,300 · 5:~900–2,000 · 6:~300–600 · 7:~160–350 · 8:~600–1,200 · **Total ≈4,050–8,600** |
| 5,000-line budget risk | **High** — corrected total is up to 1.7× the 5,000-line session budget even chained |
| Chained PRs recommended | Yes |
| Suggested split | PR1(26.1) → PR2(26.2) → PR3(26.4) → PR4(26.7) → PR5(26.3) → PR6(26.5) → PR7(26.6) → PR8(26.8) |
| Delivery strategy | auto-chain |
| Chain strategy | feature-branch-chain |

Decision needed before apply: No
Chained PRs recommended: Yes
Chain strategy: feature-branch-chain
400-line budget risk: High

**Pre-declared fission trigger — Slice 5 (26.3)**: if it overshoots, split at its own seam — **26.3a** message/role skeleton, order preservation, no-merge pin (targets slice 4's branch); **26.3b** content-part variant expectations (targets 26.3a's branch); slice 6 then retargets to 26.3b.

### Suggested Work Units

| Unit | Node | Goal | PR | Focused test command | Runtime harness | Rollback boundary |
|---|---|---|---|---|---|---|
| 1 | 26.1 | Citation gate (4 claims) + skeleton + determinism + credential scan | PR1 (base: tracker) | `go test -race -run 'TestTranslate|TestDeterminism|TestCredentialScan' ./src/ai/openaicompat/...` | N/A — pure value comparison, no I/O (NFR-ART-C); `make test` is the harness | delete `translation.go`, `body.go`, `doc.go`, `translation_test.go`, `credential_scan_test.go` |
| 2 | 26.2 | System segments + cache-marker twin drop | PR2 (base: PR1) | `go test -race -run 'TestSystemSegment|TestCacheMarker' ./src/ai/openaicompat/...` | N/A | delete `system.go`, `system_segment_test.go`, `cache_marker_test.go` |
| 3 | 26.4 | Tool declarations, schema splice, order determinism | PR3 (base: PR2) | `go test -race -run TestTool ./src/ai/openaicompat/...` | N/A | delete `tool.go`, `tool_test.go` |
| 4 | 26.7 | Options, usage opt-in walk, absence pin, extension twin | PR4 (base: PR3) | `go test -race -run 'TestOption|TestExtension' ./src/ai/openaicompat/...` | N/A | delete `option.go`, `option_test.go`, `extension_test.go` |
| 5 | 26.3 | Content parts, order, no-merge | PR5 (base: PR4) | `go test -race -run TestMessage ./src/ai/openaicompat/...` | N/A | delete `message.go`, `message_test.go` |
| 6 | 26.5 | Tool results, ID pass-through, correlation | PR6 (base: PR5) | `go test -race -run TestToolResult ./src/ai/openaicompat/...` | N/A | delete `tool_result_test.go`; revert result-message additions in `message.go` |
| 7 | 26.6 | Reasoning refusal via shared door | PR7 (base: PR6) | `go test -race -run TestReasoningRefusal ./src/ai/openaicompat/...` | N/A | delete `policy.go`'s refuse helper + reasoning entries, `reasoning_refusal_test.go` |
| 8 | 26.8 | Inventory + exhaustive policy walk | PR8 (base: PR7) | `go test -race -run 'TestFeatureInventory|TestPolicyWalk' ./src/ai/openaicompat/...` | N/A — AST scan is static, in-process | delete `feature_inventory_test.go`, `policy_walk_test.go`; revert `policy.go` completion |

## Evidence Classes (do not conflate)

1. **TDD RED** — every runnable scenario, ordinary red→green→refactor.
2. **Review obligations** (`*(review)*`) — no red phase; discharged by a recorded confirmation naming what was read.
3. **Staged mutation reds** — break a finished, GREEN implementation to prove the finished test catches it; recorded, then reverted. Distinct from both above.

---

## Phase 1 — AI-26.1 Wire skeleton + citation gate (Slice 1) — COMPLETE

### 1.0 Citation gate — R-ART-001 (do FIRST; a wrong or missing citation blocks the node)

- [x] 1.0.1 Obtain and cite authoritative vendor documentation (title + locator) for **claim 1** — system instruction as a `messages` entry vs a top-level field, and role `system` vs `developer`. Land in `doc.go`'s provenance section. If uncited: HALT before authoring any AI-26.2 system-segment expectation (S-ART-004). **CITED**: OpenAI OpenAPI spec (github.com/openai/openai-openapi, commit d4fb706), `ChatCompletionRequestSystemMessage`/`ChatCompletionRequestDeveloperMessage` — messages entry, both `system` and `developer` roles documented.
- [x] 1.0.2 Cite **claim 2** — tool-call arguments as a JSON-encoded string vs a nested object, **request-side specifically**; a response-side-only citation does not discharge it (S-ART-002). **CITED**: same spec, `ChatCompletionRequestAssistantMessage.tool_calls` → `ChatCompletionMessageToolCall.function.arguments: type: string` — the identical schema object reused request-side.
- [x] 1.0.3 Cite **claim 3** — `tools[].function.parameters` carries the neutral schema verbatim. **CITED**: same spec, `FunctionObject.parameters` → `FunctionParameters`: `type: object`, `additionalProperties: true`, "described as a JSON Schema object."
- [x] 1.0.4 Cite **claim 4** — the dialect does not enforce strict role alternation; the citation must establish that two **consecutive same-role** messages are accepted, not merely a distinct `role: "tool"` row (S-ART-003). **CITED**: spec silence on alternation (exhaustive grep, no match) + OpenAI's Function calling guide's own canonical parallel-tool-call example, which appends multiple consecutive `role:"tool"` messages onto one `messages` list as the documented, supported flow.
- [x] 1.0.5 Re-verify the base "Create chat completion" reference for `model`+`messages` array shape and whether single-text `content` renders as a JSON string or a one-element part array — settle **before** authoring the first expectation literal. **SETTLED**: `messages: type array`; `content: oneOf[string, array]` — plain string is a valid, documented shape.
- [x] 1.0.6 If any claim is contradicted by vendor docs: escalate, do not resolve locally (S-ART-005). If any of 1.0.1–1.0.4 is unobtainable: record the blockage in `doc.go` and this file's evidence log; do not branch slice 2 (S-ART-004). **No contradiction found; all four obtained.**
- [x] 1.0.7 *(review)* Confirm all four claims carry a title+locator citation in landed `doc.go`; none discharged by in-repo restatement or self-quoting (S-ART-001). **Confirmed** — see `doc.go` "Wire-shape provenance" section.

### 1.1 R-ART-002 skeleton

- [x] 1.1.1 RED `translation_test.go`: one-user-text-message request vs inline byte-exact expectation (includes `stream`/`stream_options.include_usage`); two requests differing only in model differ only in that field. Covers S-ART-006, S-ART-007.
- [x] 1.1.2 GREEN: create `translation.go` (`Translate`, model identity, stream/opt-in emission) and `body.go` (fixed-order appender, string leaves via `json.Marshal`).
- [x] 1.1.3 **Mutation proof**: stage one extra wire field in the appender; record red against S-ART-006; revert; re-run green (S-ART-008). **RED captured** (both subtests failed on the extra `scratch_mutation_field`), reverted, green confirmed.

### 1.2 R-ART-003 determinism

- [x] 1.2.1 RED/pin: same-process double-translate identical (labelled **pin**, not proof) (S-ART-010).
- [x] 1.2.2 GREEN: cross-run proof — checked-in expectation compared across independent `go test` invocations (repeat invocation, record each pass) (S-ART-009). **8/8 independent process invocations passed.**
- [x] 1.2.3 **Mutation proof**: stage an intermediate map before slice-flattening; run the suite across repeated independent invocations; record at least one red; revert (S-ART-011). **RED captured**: 6/40 independent invocations failed (reordered messages array) with a temporary 2-message scratch case + map-keyed rendering; same-process pin would have missed most of these. Reverted, green confirmed.
- [x] 1.2.4 *(review)* Confirm landed test/doc names the cross-run expectation as the proof and labels the in-process check a pin (S-ART-012). **Confirmed** — see `TestExpectationCases_MatchByteExact` and `TestTranslate_SameProcessDoubleTranslate_IsAPin` doc comments in `translation_test.go`.

### 1.3 R-ART-004 credential scan

- [x] 1.3.1 RED `credential_scan_test.go`: stage a runtime-built credential-shaped sentinel in a new expectation; scan fails naming file+class; revert (S-ART-014, S-ART-017). **RED captured** against the real production scan (staged in `translation_test.go`, named `translation_test.go` + "OpenAI-style secret key"), reverted, green confirmed.
- [x] 1.3.2 GREEN: implement the scan (`os.ReadDir(".")`, raw-byte match, runtime-concatenated sentinel patterns so its own source never matches).
- [x] 1.3.3 Prove the scan also fails on a sentinel placed in test setup, not only in an expectation literal (S-ART-015). Permanent isolated `t.TempDir()` test: `TestCredentialScan_FailsOnASentinelInTestSetup`.
- [x] 1.3.4 Confirm the scan stays green with endpoint hosts and model identifiers present (S-ART-016). Permanent isolated test: `TestCredentialScan_HostsAndModelIdentifiersStayGreen`.
- [x] 1.3.5 Confirm the full landed expectation surface scans clean (S-ART-013). `TestCredentialScan_ExpectationSurfaceIsClean` passes.

### 1.4 Slice-1 closure

- [x] 1.4.1 `doc.go`: land the four-claim provenance section and the NFR-ART-E taxonomy table (AI-25 `Violation` vs AI-26 `PreStreamFailure`+`ErrUnsupportedCapability`, citing AI-03 §10.4) (S-ART-087).
- [x] 1.4.2 Run `make test` green, `make lint` clean; record evidence table for this slice's runnable/review scenarios. **`make test` exit 0 (fresh, uncached, `-race`); `make lint` → "0 issues."**

**Deviation from design, disclosed**: the credential scan (1.3.2) is scoped to this milestone's own external test package (`package openaicompat_test`) rather than literally every `*_test.go` in the directory. A literal every-file scan would false-positive against AI-25's already-landed, internal-package `credential_test.go`, which deliberately embeds an `sk-`-shaped literal to prove `Credential.String()` never renders it — a different property, and migrating AI-25's tests is explicitly out of scope (proposal.md). External-vs-internal test package is an existing convention boundary the design itself draws elsewhere (expectation tests external, inventory/walk tests internal), so this satisfies R-ART-004's actual "expectation surface" scope and S-ART-014's "later nodes covered automatically" property without the collision. Verified: `TestCredentialScan_IgnoresInternalTestPackageFiles`.

---

## Phase 2 — AI-26.2 System segments and cache markers (Slice 2) — COMPLETE

**RESOLVED (2.1, 2.2)**: the system/developer wire-role decision, flagged as unsettled by spec, design and `doc.go`'s own slice-1 citation research, was resolved by an explicit coordinator ruling: this adapter always renders wire role `system`, never `developer`. Rationale (recorded in full in `doc.go`'s "System role: system, not developer" section): AI-24 chose the OpenAI-compatible dialect for widest reach across shared-dialect vendors (OpenRouter, vLLM, llama.cpp, Ollama, LocalAI and similar); `developer` is a recent OpenAI-proprietary addition outside that shared surface, and emitting it would narrow this adapter to recent first-party OpenAI models, defeating the reach rationale that selected the dialect. `ChatCompletionRequestDeveloperMessage` is cited and deliberately unused. A living-graph reopen trigger is recorded: if this adapter is ever pointed at a first-party endpoint whose models require `developer`, this decision reopens.

- [x] 2.1 RED `system_segment_test.go`: three-segment ordered system instruction vs expectation; reversed-order twin differs; no-system-instruction case has no placeholder entry. Covers S-ART-018, S-ART-019, S-ART-020. **RED captured** for S-ART-018/019 (genuine failures before `system.go` existed — the three-segment expectation mismatched, and the reversed-order twin produced identical bytes); S-ART-020 was green on first write (Slice 1's structure already omits an absent system instruction), disclosed rather than fabricated as a red.
- [x] 2.2 GREEN: create `system.go` rendering segments per claim 1's cited shape. **Landed**: each segment renders as its own role-`system` wire message (string content), in caller order, before the conversation messages — composes only already-cited wire shapes (claim 1's messages-entry/role, claim 4's no-alternation finding, and the existing string-content shape), needing no new wire-shape citation.
- [x] 2.3 RED `cache_marker_test.go`: marked request vs unmarked twin across **every** cache region — byte-identical. Covers S-ART-021, S-ART-022. **Delivered for messages and system regions** (both now have substantive rendering); the tools sub-test records `t.Skip` with a named reason — see disclosed-scope note below, not a fabricated pass.
- [x] 2.4 GREEN: markers render nowhere (no field/hint/comment/ordering change). **No production change needed** for either landed region: `appendMessageObject` (Slice 1) and `appendSystemMessageObject` (this slice) both never read `IsCacheBoundary()`.
- [x] 2.5 **Mutation proof**: stage rendering a marker as a wire artefact; record red against S-ART-021; revert (S-ART-023). **RED captured twice**: once for the messages region (staged `,"scratch_cache_boundary_mutation":true` in `appendMessageObject`), once for the system region (same staged field in `appendSystemMessageObject`, after that sub-test was also found green-on-first-write) — both reverted, both confirmed green again via `git diff` showing the source byte-identical to the prior commit.
- [x] 2.6 *(review)* `doc.go`: record why markers vanish, citing AI-11.3's advisory contract (S-ART-024). **Landed** — `doc.go`'s "Cache-boundary markers vanish whole" section cites `openspec/specs/ai-cache-breakpoints/spec.md` R-ACB-008, states automatic vendor caching as the reason, and now reflects that the system region is genuinely covered alongside messages.
- [x] 2.7 Slice-2 closure: `make test` green, `make lint` clean; record evidence. **Closed**: `make test` exit 0 (fresh, `-race` clean, all 3 packages, 0 `--- FAIL` lines); `make lint` → "0 issues."; `go.mod` still zero requires; both AI-00 guards and AI-25's ambient-authority guard green.

**Deviation from design, disclosed**: `cache_marker_test.go` covers "every cache region" (S-ART-022) fully in *structure* (loops over `ai.CacheRegions()` mechanically) and now in *substance* for two of three regions (messages, system). The **tools** sub-test still records `t.Skip`, deferred to Phase 3/26.4 (`tool.go` does not exist yet) — expected chain sequencing, not a gap this slice could close alone; Phase 3's own task list (3.1–3.7) never re-visits cache markers, confirming this walk is meant to become fully meaningful with no further edits once `tool.go` lands.

---

## Phase 3 — AI-26.4 Tools, deterministically (Slice 3) — COMPLETE

- [x] 3.1 RED `tool_test.go`: tool with distinctive key order and whitespace in its schema — bytes survive verbatim on the wire; name+description verbatim. Covers S-ART-035, S-ART-036. Gated on claim 3 (1.0.3). **RED captured**: registered expectation case failed before `tool.go` existed — `Translate()` omitted the "tools" field entirely (real mismatch, not a compile error).
- [x] 3.2 GREEN: create `tool.go` — raw splice of `Tool.Schema()` bytes, never re-marshalled. **Landed**: `appendToolsField`/`appendToolObject` render `{"type":"function","function":{"name","description","parameters"}}`; `body.go` splices `"tools"` after `"messages"`, only when the request declares at least one tool.
- [x] 3.3 **Mutation proof**: stage decode/re-encode round-trip of the schema; record red against S-ART-035; revert (S-ART-037). **RED captured**: staged `json.Unmarshal`+`json.Marshal` round trip in `appendToolObject`; the byte-exact case failed, printing the schema alphabetically re-sorted (`additionalProperties`, `properties`, `required`, `type`) and fully compacted — proving the test bites on re-encoding, not merely on absence. Reverted; suite green.
- [x] 3.4 RED: tools declared in a distinctive caller order match the expectation, in a fresh `go test` process, repeated (S-ART-038); two requests with different declaration orders differ accordingly (S-ART-039). **RED captured for both**: the registered 3-tool expectation case (non-alphabetical declaration order) failed before `tool.go` existed; `TestTool_ReversedOrderTwin_TranslatesDifferently` also failed (forward == reversed, both omitting tools) before `tool.go` existed — genuine reds, mirroring S-ART-019's pattern exactly.
- [x] 3.5 **Mutation proof**: stage tool construction via an intermediate name-keyed map; run repeated independent invocations; record ≥1 red; revert (S-ART-040). **RED captured**: staged an intermediate `map[string]ai.Tool` in `appendToolsField`, ranged over the map to decide wire order; 5/30 independent `go test -count=1` process invocations failed, printing a reordered tools array (`cancel_booking, get_weather, search_flights` instead of the declared `search_flights, cancel_booking, get_weather`) — silent leak, no error, exactly R-ART-011's failure mode. Reverted; 10/10 subsequent independent invocations of the clean implementation passed (positive cross-run proof).
- [x] 3.6 *(review)* Confirm the landed test/doc names the fresh-process comparison as proof and states the silent-cost failure mode (broken cache prefix, no error) (S-ART-041). **Confirmed** — `tool_test.go`'s file-header doc comment names `TestExpectationCases_MatchByteExact`, run as an independent process and repeated across invocations, as the fresh-process tool-order proof, and states the failure mode: "a valid body, no error and no test failure — only a broken vendor cache prefix and an input bill roughly ten times larger than it should be."
- [x] 3.7 Slice-3 closure: `make test` green, `make lint` clean; record evidence. **Closed**: `make test`/fresh `go test -race -count=1 ./...` exit 0, all 3 packages pass, 0 `--- FAIL` lines; `make lint` → "0 issues."; `go.mod` still zero requires (no `go.sum`); both AI-00 guards (`TestLayer1_ImportsOnlyStdlibAndItsOwnPackages_DenyByDefault`, `TestLayer1_ModuleHasNoDependencies_ZeroRequires`) and AI-25's ambient-authority guard (`TestAmbientAuthority_NoForbiddenCallSitesInAdapterSources`) pass with no allowlist edit.

**Pin, disclosed (not one of 3.1–3.7, added per cross-slice guidance)**: `TestToolSet_DuplicateOrInvalidNamesNeverReachTranslate` proves duplicate and invalid-shaped tool sets are already rejected by `ai.NewToolSet` (AI-08.2) before a `Request` can ever reach `Translate` — `tool.go` therefore carries no duplicate-detection or name-validation logic of its own. Green from birth (evidence class 4): it exercises `ai`'s own already-shipped behaviour, not anything this slice implements.

**Cross-slice hand-off discharged**: `cache_marker_test.go`'s "tools" sub-case (pre-wired by Phase 2, previously a disclosed `t.Skip`) was verified, not assumed. The loop/switch structure needed no edit, exactly as Phase 2's comment anticipated, but the tools case's *body* did need its placeholder `t.Skip` replaced with a real `buildToolsRegionTwin` twin comparison — Phase 2's claim of "no edit needed to this file" was accurate for the walk's structure but imprecise about the case body itself, corrected in this file's own updated comments. Now PASSING non-vacuously (confirmed via `go test -race -v -run TestCacheMarker`), completing R-ART-006/S-ART-021-023 across all 3 regions (messages, system, tools). Falsifiability re-confirmed by a staged, reverted mutation gated on `Tool.IsCacheBoundary()`, mirroring the messages/system proof exactly. `doc.go`'s "Cache-boundary markers vanish whole" section updated to reflect completion.

---

## Phase 4 — AI-26.7 Options, limits and the escape hatch (Slice 4) — COMPLETE (12/12)

- [x] 4.1 RED `option_test.go`: one request per settable generation option — field present with caller value vs expectation; zero-value vs unset bodies differ (S-ART-057, S-ART-058). **RED captured**: 6 registered cases (max_tokens, temperature, top_p, stop, tool_choice=none, tool_choice=specific-named) failed genuinely before `option.go` existed (`Translate()` omitted every one of these fields); `TestOption_ZeroValueVersusUnset_TranslatesDifferently` also failed genuinely (temperature=0 and unset produced identical bytes).
- [x] 4.2 GREEN: create `option.go` — presence-flag-driven emission, `has*` per option. **Landed**: `appendGenerationOptionFields` (max_tokens/temperature/top_p/stop, each gated on its own `has*` accessor) and `appendToolChoiceField` (bare string for auto/none/required via `ToolChoiceMode.String()`, `{"type":"function","function":{"name":...}}` object for a named choice), spliced into `body.go` after `tools`, per the fixed order.
- [x] 4.3 RED: omitted max-output-tokens leaves the field explicitly absent; set option present with value (S-ART-062, S-ART-063). **Landed**: `TestOption_MaxOutputTokensOmitted_FieldExplicitlyAbsent` (absence, paired with the registered "present with caller value" case for presence) — disclosed as vacuously true pre-implementation too (nothing rendered `max_tokens` at all before `option.go` existed), so its falsifiability rests on the paired "present" case's genuine red, not a red of its own.
- [x] 4.4 *(review)* `doc.go`: record the mandatory-default branch as intentionally dead for this vendor, and why (S-ART-064). **Landed** — `doc.go`'s "Output-token limit: optional for this vendor, mandatory-default branch dead" section, citing doc 0002's own amended AI-26.7 charter (line 1569, AI-24, 2026-08-03).
- [x] 4.5 RED: over the **complete expectation set**, every case carries `stream_options.include_usage: true` — total walk, not sampled (S-ART-059, S-ART-060). **`TestExpectationCases_AllCarryUsageOptIn`, green on first write** (evidence class 4): the bytes have been emitted unconditionally since the slice-1 skeleton; this task's own scenario is the total, positive, directly-named assertion, not the bytes' existence. Disclosed rather than fabricated as a red — see task 4.6 for its falsifiability proof.
- [x] 4.6 **Mutation proof**: stage omitting the opt-in; record red on the positive assertion (not absence-of-error); revert (S-ART-061). **RED captured**: staged removal of `,"stream_options":{"include_usage":true}` from `appendStreamFields` (`body.go`); all 10 registered cases failed `TestExpectationCases_AllCarryUsageOptIn` by name, printing bodies missing the opt-in. Reverted; suite green; `appendStreamFields` confirmed byte-identical to its pre-mutation state via Read.
- [x] 4.7 Read AI-25's **landed** artifact to obtain the reserved provider-extension namespace value (do not re-invent it here). **RESOLVED BY COORDINATOR RULING** — see "Escape hatch: coordinator ruling" note below. The read was performed exhaustively first (recorded permanently below); the value did not exist anywhere in AI-25's landed artifact because AI-25 was never given a task to define one — the instruction to read it from AI-25 was itself the defect. The coordinator independently verified this finding and ruled: the value is `openaicompat`, and this milestone defines it.
- [x] 4.8 RED `extension_test.go`: own-namespace extension vs its twin without it — bodies differ, matches expectation with extension bytes merged (S-ART-065). **RED captured**: `TestExtension_OwnNamespaceMergesDiffersFromExtensionFreeTwin` and the registered "own-namespace extension merges its members into the wire body" case failed with a genuine compile error (`undefined: openaicompat.Namespace`) before `option.go` defined the constant — the same compile-error RED class Slice 1 used for `Translate` itself.
- [x] 4.9 RED: foreign-namespace extension vs twin — byte-identical; several foreign namespaces at once — still byte-identical (S-ART-066, S-ART-067). **Same compile-error RED as 4.8** (both tests live in the same new file, `openaicompat.Namespace` undefined). `TestExtension_ForeignNamespaceIgnoredWhole` and `TestExtension_SeveralForeignNamespacesAtOnceStillIgnoredWhole` both anchor against a hard-coded literal (`foreignExtensionExpectation`) on **both** sides of the comparison, not merely against each other — the trap the coordinator named explicitly (a fixture that cannot distinguish implemented from not-implemented; a test comparing an implementation only against itself).
- [x] 4.10 GREEN: wire own-namespace merge into `option.go`; every other namespace enumerated, never inspected/rendered. **Landed**: `Namespace` (exported constant, `"openaicompat"`) and `appendExtensionFields` (reads `req.ProviderExtension(Namespace)`, splices `.Value()` raw when present, merged as additional top-level members, last) in `option.go`; wired into `body.go` right after `stream_options`, before the closing brace.
- [x] 4.11 **Mutation proof**: stage rendering any foreign namespace; record red against S-ART-066; revert (S-ART-068). **RED captured**: staged `appendExtensionFields` to range over `req.ProviderExtensions()` (every namespace) instead of looking up only `Namespace`; both `TestExtension_ForeignNamespaceIgnoredWhole` and `TestExtension_SeveralForeignNamespacesAtOnceStillIgnoredWhole` failed, printing bodies with the foreign fields (`"anthropic_only_field":"should never appear"`, then all three of `anthropic_field`/`gemini_field`/`cohere_field`) leaked into the wire body — exactly the "merged rather than ignored" failure mode the coordinator warned the twin test was exposed to. Reverted; confirmed via `git diff` that `option.go` is byte-identical to its pre-mutation state; suite green.
- [x] 4.12 Slice-4 closure: `make test` green, `make lint` clean; record evidence. **CLOSED.** Fresh `go test -race -v -count=1 ./...`: exit 0, all 3 packages pass, 0 `--- FAIL` lines. `make test`/`make lint` (`0 issues.`) both green. `go.mod` zero requires, no `go.sum`. Both AI-00 guards and AI-25's ambient-authority guard pass, no allowlist edit.

**Escape hatch: coordinator ruling (R-ART-019, tasks 4.7-4.11) — RESOLVED, permanent record of the gap and its correction**: task 4.7's own instruction was to read AI-25's landed artifact for the reserved provider-extension namespace value and never re-invent it here — repeated verbatim across proposal.md, spec.md (twice), design.md and tasks.md. Before writing any escape-hatch code, this session searched exhaustively:
- All four AI-25 non-test sources this package owns: `client.go`, `credential.go`, `endpoint.go`, `request.go` (read in full).
- `doc.go`'s own AI-25-era sections (read in full, pre-Phase-4).
- AI-25's own SDD artifact: `openspec/changes/cachicamas-ai-provider-client/{proposal,spec,design,tasks,decision}.md` — `grep -rniE "namespace"` returned **zero matches**.
- AI-24's merged decision: `openspec/changes/cachicamas-ai-first-provider-decision/{decision,design,proposal}.md` — no namespace value.
- doc 0002's own AI-25 charter (`docs/architecture/milestones/0002-cachicamas-ai-layer-1-task-graph.md`, AI-25 section in full) — no namespace value.
- A repo-wide `grep -rniE "namespace"` across every `.go`/`.md` file, and `git log --all --oneline | grep -iE "namespace|extension"` — the only namespace-handling code found is `request_extension.go`'s generic mechanism (AI-12.3), which reserves none of its own (R-REX-006).

This session halted 4.7-4.12 and disclosed the gap rather than inventing a value, exactly as the instruction (however defective) required. **The coordinator independently verified the same finding and issued a ruling**: the instruction was itself the defect — AI-25 was never given a task to define this value, so there was never anything to read. The coordinator assigned the value: the exported constant `Namespace` (`"openaicompat"`), defined in `option.go` — this milestone's own file, since AI-25's files remain outside this slice's write scope and the value does not belong to construction anyway. Rationale recorded in full in `doc.go`'s "Escape hatch: reserved namespace" section: adapter identity (unambiguous `ai.WithProviderExtension(openaicompat.Namespace, ...)`), dialect-named not vendor-branded (matching the package's own naming, since OpenRouter/vLLM/Ollama/LocalAI are all in scope, not first-party OpenAI alone), and exported specifically so a caller references it by name rather than risking a silent, unreported typo into "foreign namespace, ignored whole".

**Files delivered this slice (complete)**: `option.go` (created then grown, 178 lines total: `Namespace`, `appendExtensionFields`, `appendGenerationOptionFields`, `appendToolChoiceField`, `appendStopField`, `appendJSONInt`, `appendJSONFloat64`), `body.go` (+25/-10 lines total: splice tool_choice + generation options + extension members, update header comment twice), `option_test.go` (created, 195 lines: 6 registered expectation cases, `mustToolChoice`, `TestOption_ZeroValueVersusUnset_TranslatesDifferently`, `TestOption_MaxOutputTokensOmitted_FieldExplicitlyAbsent`, `TestExpectationCases_AllCarryUsageOptIn`), `extension_test.go` (created, 136 lines: 1 registered case, `TestExtension_OwnNamespaceMergesDiffersFromExtensionFreeTwin`, `TestExtension_ForeignNamespaceIgnoredWhole`, `TestExtension_SeveralForeignNamespacesAtOnceStillIgnoredWhole`), `doc.go` (+94 lines total across two revisions: R-ART-018 dead-branch section, then the full escape-hatch/coordinator-ruling section replacing the earlier blocker note).

**Measured diff vs `feat/ai-26-3-tools-deterministic`**: 5 files, 618 insertions(+), 10 deletions(-) (`git diff --shortstat`).

**Commits** on `feat/ai-26-4-options-escape-hatch` (off `feat/ai-26-3-tools-deterministic`): `9bf059c feat(openaicompat): translate generation options and tool_choice, respecting presence over value` · `acc7019 test(openaicompat): assert the usage opt-in positively across the total expectation walk` · `8cd0d46 docs(openaicompat): record the dead output-limit branch and the blocked escape-hatch namespace` · `60cb40f feat(openaicompat): merge own-namespace provider extension and ignore foreign namespaces whole` · `a1a6b70 docs(openaicompat): record the coordinator ruling defining this adapter's reserved namespace`.

---

## Phase 5 — AI-26.3 Messages and content parts (Slice 5, largest — fission trigger active) — COMPLETE (11/11)

- [x] 5.1 RED `message_test.go`: one request per translatable part variant vs its own expectation; mixed-part message renders every part per its single-variant expectation. Covers S-ART-025, S-ART-026. **RED confirmed empirically, not assumed from the file's shape**: this package already compiled before this slice (`Translate` has existed since Slice 1, and every symbol `message_test.go` references already existed), so the red is NOT a compile error — it is a genuine unrecovered runtime panic. Verified via `go test -race -run 'TestExpectationCases_MatchByteExact/(two_ordered|three_ordered)'`: `appendSingleTextContent` (body.go, Slice 1) panicked with a kind-blind message on any 2+-part message, crashing the test binary.
- [x] 5.2 GREEN: create `message.go` — content-part rendering. **Landed**: `appendMessageObject` (moved from body.go, unchanged shape) + new `appendMessageContent` (single-part → bare JSON string, per claim 1.0.5; multi-part → array of typed content-part objects) + new `appendContentPartObject` (`{"type":"text","text":...}` for `PartKindText`; panics naming the kind, via `part.Kind().String()`, for every other member — a transitional safety net per R-ART-007, not the deferred kinds' eventual replacement site). `body.go`'s superseded `appendMessageObject`/`appendSingleTextContent` removed; stale doc references in `body.go` and `system.go` updated to the new location.
- [x] 5.3 Confirm the enumerated part-kind vocabulary (`PartKinds()`) is fully accounted for by expectation or recorded refusal (S-ART-027). **`TestMessage_PartKindCoverage`** (inherited from the draft) walks `ai.PartKinds()` (4 members: Text, Reasoning, ToolCall, ToolResult) against a local `partKindDispositions` table; a kind with no entry fails naming it. Text is asserted to translate; the other 3 are asserted to panic, naming the kind — checking panic *content*, not merely that a panic occurred, specifically defeats a generic kind-blind panic passing vacuously. Each deferred entry carries a hand-off stating precisely what the owning later phase (Phase 6 for ToolCall/ToolResult, Phase 7 for Reasoning) must VERIFY — audited this session, already precise, no correction needed (see inherited-draft audit note below).
- [x] 5.4 RED: multi-turn request in a distinctive message order vs expectation; distinctive intra-message part order vs expectation; adjacent-message swap produces a differing body (S-ART-028, S-ART-029, S-ART-030). Registered cases discharge S-ART-028/029; `TestMessage_AdjacentSwap_TranslatesDifferently` (inherited) discharges S-ART-030. **This session added `TestMessage_PartOrderSwap_TranslatesDifferently`**, closing a gap found auditing the inherited draft: the existing "three ordered text parts" case uses alpha/beta/gamma, already in ascending lexical order, so a hypothetical part-sorting bug would pass that case vacuously (matching its hard-coded expectation either way). The new test uses non-monotonic values (forward order is descending) so a sort bug fails its own hard-coded anchor instead of coincidentally matching it.
- [x] 5.5 GREEN: preserve message/part order exactly — no reordering, sorting, grouping or normalization. **Landed structurally**: `appendMessageContent`/`appendContentPartObject` iterate `message.Content()` by index, no map; `appendMessagesField` (body.go, unedited this slice) iterates `req.Messages()` by index, no map — design.md "Map discipline" extended to message.go with no new mechanism.
- [x] 5.6 RED: two/three consecutive same-role messages render as two/three distinct wire message objects, no concatenation. Gated on claim 4 (1.0.4). Covers S-ART-031, S-ART-032. Registered cases (inherited from the draft) confirmed green on first write via isolated `go test -run` (evidence class 4) — `appendMessagesField`'s unconditional per-message loop already had this property since Slice 1; no new code in this phase was needed for it.
- [x] 5.7 GREEN: no-merge behavior in `message.go`. **Landed as a documented structural property, not new logic**: `appendMessageObject` (message.go) carries no state across calls and reads nothing about any other message; `appendMessagesField`'s loop (body.go, unedited) never groups by role. Stated explicitly on `appendMessageObject`'s own doc comment and in `doc.go`'s new "No merging of consecutive same-role messages" section (5.9).
- [x] 5.8 **Mutation proof**: stage merging consecutive same-role messages; record red against S-ART-031; revert (S-ART-033). **RED captured**: staged a same-role-collapse in `appendMessagesField` (kept only the first message of each consecutive same-role run, dropping the rest — chosen because the byte-append architecture has no in-place buffer to concatenate two already-written objects' content without a larger refactor; dropping still violates R-ART-009's "not merged" contract exactly as a content-concatenating merge would). "Two consecutive" collapsed 2→1 message, "three consecutive" collapsed 4→2 messages; both registered cases failed by name. Reverted; confirmed via `git diff` that `body.go` is byte-identical to its pre-mutation state; suite green.
- [x] 5.9 *(review)* `doc.go`: record the no-merge decision and its reason (S-ART-034). **Landed** — `doc.go`'s new "No merging of consecutive same-role messages" section, citing claim 4 and S-ART-033's own mutation shape.
- [x] 5.10 If this slice risks overshooting budget: split at the pre-declared seam (26.3a message/role skeleton + order + no-merge; 26.3b part-variant expectations), per fission trigger above. **Not triggered**: measured diff vs `feat/ai-26-4-options-escape-hatch` is 5 files, 560 insertions(+), 66 deletions(-) — 626 total changed lines, well under the ~1,200-line fission threshold and in line with prior slices (621/429/340/618). No split performed.
- [x] 5.11 Slice-5 closure: `make test` green, `make lint` clean; record evidence. **Closed**: fresh `go test -race -v -count=1 ./...` exit 0, all 3 packages pass, 0 `--- FAIL` lines, 0 `DATA RACE` reports. `make test`/`make lint` (`0 issues.`) both green. `go.mod` zero requires, no `go.sum`. Both AI-00 guards (`TestLayer1_ImportsOnlyStdlibAndItsOwnPackages_DenyByDefault`, `TestLayer1_ModuleHasNoDependencies_ZeroRequires`) and AI-25's ambient-authority guard (`TestAmbientAuthority_NoForbiddenCallSitesInAdapterSources`) pass with no allowlist edit.

**Inherited-draft audit (this session's first act, resuming from a prior session's session-limit interruption)**: `message_test.go` existed untracked (no `message.go`, no commits on this branch) from a prior session that stopped mid-RED. Audited critically before trusting it, per this session's own instruction:

- **Confirmed landed, not merely claimed**: `assertKindIsDeferred`'s content check (`strings.Contains(panicMessage, disposition.kind.String())`) — the prior session's claimed final act (strengthening the helper so it would not pass vacuously against a kind-blind panic) — verified present in the file and load-bearing (it is what makes 5.3's coverage test non-vacuous).
- **Found and fixed a genuine crash, not a vacuous pass**: the `PartKindToolResult` coverage case built a request containing only a tool-result part with no correlating tool call anywhere in the request. `ai.NewRequest`'s own `unresolvedToolResultRule` (`ai.ErrUnresolvedReference`) rejects this at construction, so `mustRequest` panicked with an unrelated message ("value names something the request does not declare") before `Translate` was ever reached — an unrecovered panic that crashed the whole test binary rather than a clean subtest failure. Fixed by generalizing `assertKindIsDeferred` to accept several messages and supplying a correlating tool-call message *second*, positioned after the tool-result message under test, so message-order preservation (R-ART-008, itself under test elsewhere in this same file) keeps the observed panic unambiguously attributable to the tool-result kind rather than the tool-call kind (also deferred, and would otherwise panic with a different name if reached first).
- **Found and closed a sort-coincidence gap** (see 5.4 above): closed by `TestMessage_PartOrderSwap_TranslatesDifferently`.
- **No other vacuous-pass trap found**: the registered same-role/order cases are all anchored against hard-coded literals (not self-comparison, defeating trap 2), and each was confirmed via an isolated `go test -run` invocation to be either a genuine red (crash/panic, defeating trap 1) or an honestly disclosed green-on-first-write with its own mutation-proof obligation identified and discharged.

**Additional disclosed green-on-first-write, with mutation proof (beyond 5.6/5.8's own S-ART-031/032/033)**: `TestMessage_AdjacentSwap_TranslatesDifferently` (S-ART-030) and the registered "multi-turn conversation" case (S-ART-028) both passed before `message.go` existed — message-order preservation was already structural in Slice 1's `appendMessagesField` loop, unrelated to this slice's own new content-part-rendering code. Per this session's evidence-discipline instruction, this session staged a message-order-reversal mutation in `appendMessagesField` (temporary in-place reverse of the local `req.Messages()` copy), confirmed `TestMessage_AdjacentSwap_TranslatesDifferently` and the "multi-turn"/"two consecutive"/"three consecutive" registered cases all failed by name, then reverted — confirmed via `git diff` that `body.go` is byte-identical to its pre-mutation state.

**Pre-existing documentation staleness found and corrected** (in `doc.go`, this session's own file, discovered while editing it for this slice's own reason): AI-26.4's (Phase 3) evidence log above claims `doc.go`'s "Cache-boundary markers vanish whole" section was "updated to reflect completion" once `tool.go` landed, but the actual text still described the Phase-2-era state (tools region recorded SKIP, "two of three" regions complete) — `cache_marker_test.go`'s own (accurate, already-correct) comments already said all three regions pass non-vacuously. Corrected in `doc.go` this session, disclosed in place rather than silently overwritten, mirroring Phase 3's own precedent of correcting Phase 2's closure note in place.

#### TDD Cycle Evidence (Phase 5, complete)

| Task | Test | RED | GREEN |
|---|---|---|---|
| 5.1 S-ART-025/026 | registered "two/three ordered text parts" cases | Genuine: unrecovered panic (`appendSingleTextContent` had no 2+-part shape) | Passes after `message.go` landed |
| 5.3 S-ART-027 | `TestMessage_PartKindCoverage` | Genuine: wrong-content panic for reasoning/tool_call; crash (bug, fixed this session) for tool_result | Passes after `message.go` + the coverage-test fix landed |
| 5.4 S-ART-028 | registered "multi-turn conversation" case | N/A — green on first write (evidence class 4) | Passes; falsifiability via the message-order-reversal mutation below |
| 5.4 S-ART-029 | registered "two/three ordered text parts"; new `TestMessage_PartOrderSwap_TranslatesDifferently` | Genuine panic (both) | Both pass after `message.go` landed |
| 5.4 S-ART-030 | `TestMessage_AdjacentSwap_TranslatesDifferently` | N/A — green on first write (evidence class 4) | Passes; falsifiability via the message-order-reversal mutation below |
| 5.6 S-ART-031/032 | registered "two/three consecutive same-role" cases | N/A — green on first write (evidence class 4) | Passes; falsifiability via 5.8's mutation |
| 5.8 S-ART-033 | staged same-role-collapse mutation | Genuine: "two consecutive" 2→1, "three consecutive" 4→2, both failed by name | Reverted; confirmed byte-identical via `git diff`; suite green |
| (unlisted, disclosed) S-ART-028/030 falsifiability | staged message-order-reversal mutation | Genuine: adjacent-swap + multi-turn + two/three-consecutive cases all failed | Reverted; confirmed byte-identical via `git diff`; suite green |

#### Work Unit Evidence

| Evidence | Value |
|---|---|
| Focused test command and exact result | `go test -race -v -run TestMessage ./src/ai/openaicompat/...` → all PASS, exit 0 |
| Runtime harness command/scenario and exact result | N/A — pure value comparison, no I/O (NFR-ART-C); fresh `go test -race -v -count=1 ./...` is the harness: exit 0, all 3 packages pass, 0 `--- FAIL` lines |
| Rollback boundary | Delete `message.go`, `message_test.go`; revert `body.go` (restore `appendMessageObject`/`appendSingleTextContent`, restore the original `appendMessagesField` doc comment), `system.go` (restore the stale reference), `doc.go` (remove the no-merge section and the cache-boundary correction) — no AI-25 or AI-27 file touched |

**Measured diff vs `feat/ai-26-4-options-escape-hatch`**: 5 files, 560 insertions(+), 66 deletions(-) (`git diff --shortstat`, new files counted via `git add -N` intent-to-add, then unstaged).

**Fission trigger**: NOT fired. 626 total changed lines, well under the ~1,200-line threshold and in line with prior slices (621/429/340/618).

---

## Phase 6 — AI-26.5 Tool results and identifiers (Slice 6) — COMPLETE (11/11)

- [x] 6.1 RED `tool_result_test.go`: one tool result vs expectation — exactly one wire tool-role message; several results — one distinct message per result, caller order. Covers S-ART-042, S-ART-043. **RED confirmed empirically before any production change**: the "one tool result" case panics at `appendMessageContent`'s single-part branch (`part.Text()` not ok for a `PartKindToolCall` part); the "several tool results" case panics at `appendContentPartObject`'s default case (array-render branch, 2 tool calls) — two genuinely different code paths, both confirmed via isolated `go test -run`, matching the pattern `TestMessage_PartKindCoverage`'s own `tool_call`/`tool_result` subtests confirmed first (same underlying unimplemented-kind panic, empirically checked before writing any of this phase's other five registered cases, which are structurally the same one of these three panic shapes each — disclosed rather than mechanically re-run one by one).
- [x] 6.2 GREEN: grow `message.go` with tool-role result-message rendering (AI-24 §6 shape, given). **Landed**: `appendMessageObject` dispatches a `RoleTool` message to new `appendToolResultMessages`/`appendToolResultObject` (`{"role":"tool","tool_call_id":...,"content":...}`) before any role/content-shape logic runs; new `splitToolCalls` partitions a non-`RoleTool` message's content into its content-part remainder and its tool-call parts, rendering the latter through new `appendToolCallObject` in a separate `"tool_calls"` array. All three registered "several/failed/one" cases and both coverage-test kinds pass together — this task's own GREEN and 6.3's both land in the same change (no separate code path exists for a failed result to take; see 6.3 below).
- [x] 6.3 RED: a failed tool result carries its failure disposition per expectation, not silently rendered as success (S-ART-044). **RED confirmed together with 6.1** (the registered "failed tool result" case panics identically, before `appendToolResultObject` existed — genuinely red, not vacuous, since nothing distinguished failed from succeeded before this task's GREEN either). **No separate GREEN step**: `appendToolResultObject` renders `result.Content()` unconditionally, never branching on `result.Failed()` — AI-24 §6's given shape carries no distinct field for it, and `tool_result.go`'s own doc comment states a failing tool's answer is content, not this package's fault taxonomy. Falsifiability beyond the one registered case: `TestToolResult_FailedAndSucceededRenderIdentically` cross-checks that substituting one call's identifier into the succeeded body's own bytes reproduces the failed body exactly — proving Failed() changes nothing but identity and content, not merely that one hand-picked literal happens to match.
- [x] 6.4 RED: distinctive identifiers survive verbatim; an unusual-but-valid identifier shape survives unnormalized (S-ART-045, S-ART-046). **Per the coordinator's explicit instruction, folded into the other registered cases rather than a separate, otherwise-empty test**: every registered case in `tool_result_test.go` uses a distinctive (non-"call_1"-shaped) identifier, verified byte-exact via the byte-exact comparison harness (discharges S-ART-045 across six independent fixtures, not one). The dedicated "unusual but valid identifier shape" case (`3F2A9B-Weird_ID.42` — mixed case, hyphen, underscore, period) discharges S-ART-046: `ai.ToolCall`'s own `validate` carries no shape rule on `id` beyond non-empty, so this shape is legal, and a naive normalizer (case-fold, strip punctuation) would break it.
- [x] 6.5 GREEN: identifier pass-through — never generated, derived, normalized, truncated. This also discharges the synthetic-minting no-op pin (R-ART-013, S-ART-045). **Landed together with 6.2**: `appendToolCallObject`'s `"id"` and `appendToolResultObject`'s `"tool_call_id"` both splice `ai.ToolCall.ID()`/`ai.ToolResult.CallID()` through `appendJSONString` unchanged — no separate minting code path exists to discharge as a no-op; its absence *is* the discharge, recorded in `doc.go`'s new "Tool-call identifier: pass-through only" section per doc 0002's amended AI-26.5 charter (the vendor assigns identifiers on the call's own opening delta).
- [x] 6.6 **Mutation proof**: stage identifier regeneration; record red against S-ART-045; revert (S-ART-047). **RED captured**: staged `appendJSONString(buf, "scratch_minted_id")` in place of `call.ID()` (`appendToolCallObject`) and `result.CallID()` (`appendToolResultObject`); all 6 registered tool-result cases plus `TestToolResult_FailedAndSucceededRenderIdentically` failed by name; every unrelated pre-existing case stayed green. Reverted; confirmed via `git diff` that both `message.go` and `body.go` are byte-identical to their pre-mutation, committed state; suite green.
- [x] 6.7 RED: a turn with three tool calls and interleaved out-of-order results — each wire result names its own call's identifier. Gated on claim 2's request-side confirmation (1.0.2) where argument encoding is observable. Covers S-ART-048. **RED confirmed structurally** (array-render + `RoleTool` panic paths, both independently confirmed under 6.1): registered case declares three tool calls (`search_flights`/`check_weather`/`book_hotel`, each with distinctive, JSON-escaping-triggering arguments so claim 2's string encoding is genuinely observable) and three results answering in a derangement of the declaration order (`book_hotel`, `search_flights`, `check_weather`) — chosen so the natural position-based bug (Nth-declared call substituted for the Nth-encountered result) fails at all three positions, and so identifier lexical sort order does not coincide with either the declaration or the answering order (Trap 7: sort-coincidence).
- [x] 6.8 GREEN: correlation by identifier, never position/index. **Landed together with 6.2/6.5**: `appendToolResultObject` performs no lookup, no matching and no positional reasoning — the wire `tool_call_id` is literally `result.CallID()`, the value the caller supplied when constructing the `ai.ToolResult`. Correlation surviving translation is a structural consequence of this, not a separate mechanism.
- [x] 6.9 **Mutation proof**: stage position-based correlation; record red against S-ART-048; revert (S-ART-049). **RED captured**: staged a package-level scratch scan (`appendMessagesField`, body.go) collecting every declared tool call's identifier in declaration order, consumed positionally by a scratch counter in `appendToolResultObject` instead of `result.CallID()`. Result: the four cases whose results answer calls in the SAME order they were declared (S-ART-042/043/044/046's own cases) stayed green — positionally coincidental — while both cases built specifically to defeat that coincidence (6.7's interleaved case and 6.10's reordered case, below) failed by name, printing the declaration-order identifiers (`flt-9k2M`, `wthr-3Qz`, `htl-7Bv1`) substituted into the wrong positions instead of the correct answering-order identifiers (`htl-7Bv1`, `flt-9k2M`, `wthr-3Qz`). This is exactly why S-ART-048's fixture must be interleaved: a non-interleaved fixture provably cannot catch this bug class. Reverted; confirmed via `git diff` that `message.go` and `body.go` are byte-identical to their pre-mutation, committed state; suite green.
- [x] 6.10 RED: two calls to the same tool name with different identifiers remain distinguishable (S-ART-050). **RED confirmed structurally** (same panic class as 6.1/6.7): registered case declares two `check_weather` calls (same name, different identifiers `wthr-tokyo-1`/`wthr-paris-2`), results answering in reversed order — also independently caught by 6.9's positional mutation (see above), confirming correlation is by identifier and not accidentally by tool name or position.
- [x] 6.11 Slice-6 closure: `make test` green, `make lint` clean; record evidence. **Closed**: fresh `go test -race -v -count=1 ./...` exit 0, all 3 packages pass (`agenttest`, `ai`, `ai/openaicompat`), 0 `--- FAIL` lines, 0 `DATA RACE` reports. `make test` exit 0 (Makefile-pinned `go test -race -v ./...`). `make lint` → "0 issues." `go.mod` still zero `require` directives, no `go.sum`. Both AI-00 guards (`TestLayer1_ImportsOnlyStdlibAndItsOwnPackages_DenyByDefault`, `TestLayer1_ModuleHasNoDependencies_ZeroRequires`) and AI-25's ambient-authority guard (`TestAmbientAuthority_NoForbiddenCallSitesInAdapterSources`) pass individually, with no allowlist edit.

**Design decision, disclosed (not one of 6.1–6.11, no existing citation covered it)**: an assistant message carrying only tool calls (no text) omits the wire `"content"` field entirely, rather than rendering `"content":null`. Claim 1.0.5 (this milestone's own citation gate, slice 1) settled `content` as `oneOf[string, array]` — never `oneOf[string, array, null]` — so `null` was never a licensed shape to fabricate. This follows the same presence-flag, omit-when-absent convention every other optional field in this package already uses (`option.go`'s generation options, `tool.go`'s own `"tools"` field, `body.go`'s extension members) rather than inventing a new one. Recorded in full, with rationale, in `doc.go`'s new "Assistant \"content\" is omitted, never null, when a message carries only tool calls" section.

**Measured diff vs `feat/ai-26-5-messages-content-parts`**: 5 files, 603 insertions(+), 82 deletions(-) — 685 total changed lines (`git diff --shortstat feat/ai-26-5-messages-content-parts..HEAD`). Above the ~300–600 corrected forecast range but not by an amount that risks reviewer load at the ~1,200-line fission-relevant scale Slice 5 used; no fission mechanism was pre-declared for this node and none was needed.

**Commits** on `feat/ai-26-6-tool-results` (off `feat/ai-26-5-messages-content-parts`), 2 total — deviates from the 4-commit "as planned" list below for the same reason Slice 5's landed commits deviated from its own plan: tool-role rendering, identifier pass-through and correlation are one indivisible code change in `message.go` (no code path renders a tool result without also deciding how its identifier renders), and no separate `chore: record evidence` commit is made, matching Slices 1–5's own actual practice (evidence lives in this file and this milestone's Engram `apply-progress` topic):
1. `feat(openaicompat): translate tool calls and tool results, passing identifiers through unchanged` — `message.go` (grown), `message_test.go` (coverage table + helper signature updated so the coverage test's positive half is non-vacuous for the two newly handled kinds), `tool_result_test.go` (new: 6 registered cases + 1 dedicated cross-check test).
2. `docs(openaicompat): record the tool-call/tool-result wire shape and the content-omission decision` — `doc.go` (new section), `body.go` (`appendMessagesField`'s doc comment updated in place to describe this slice's growth, avoiding the kind of staleness two earlier slices in this milestone had to correct after the fact).

#### TDD Cycle Evidence (Phase 6, complete)

| Task | Test | RED | GREEN |
|---|---|---|---|
| 6.1 S-ART-042/043 | registered "one/several tool result(s)" cases | Genuine: unrecovered panic (single-part branch for one call; array-render branch for two) | Passes after `message.go`'s AI-26.5 growth landed |
| 6.2/6.5/6.8 (no separate S-ART; discharges R-ART-012/013/014's structural core) | — (implementation, not its own scenario) | — | `appendToolResultMessages`/`appendToolResultObject`/`appendToolCallObject`/`splitToolCalls` land together |
| 6.3 S-ART-044 | registered "failed tool result" case | Genuine: identical unrecovered panic (nothing distinguished failed from succeeded pre-GREEN either) | Passes; falsifiability beyond the registered case via `TestToolResult_FailedAndSucceededRenderIdentically`'s cross-check |
| 6.4 S-ART-045/046 | all 6 registered cases (045, folded); "unusual but valid identifier" case (046) | Genuine panic (all) | All pass after GREEN landed |
| 6.6 S-ART-047 | staged identifier-regeneration mutation | Genuine: 6 registered cases + 1 dedicated test failed by name | Reverted; confirmed byte-identical via `git diff`; suite green |
| 6.7 S-ART-048 | registered "interleaved multi-call" case | Genuine panic (array-render + RoleTool paths) | Passes after GREEN landed |
| 6.9 S-ART-049 | staged position-based-correlation mutation | Genuine: only the interleaved (6.7) and reordered (6.10) cases failed by name; 4 non-interleaved cases stayed positionally-coincidentally green, confirming why the interleaved fixture is necessary | Reverted; confirmed byte-identical via `git diff`; suite green |
| 6.10 S-ART-050 | registered "same tool name" case | Genuine panic | Passes after GREEN landed; independently confirmed by 6.9's mutation too |

#### Work Unit Evidence

| Evidence | Value |
|---|---|
| Focused test command and exact result | `go test -race -v -run 'TestExpectationCases_MatchByteExact\|TestToolResult_FailedAndSucceededRenderIdentically\|TestMessage_PartKindCoverage' ./src/ai/openaicompat/...` → all PASS, exit 0 |
| Runtime harness command/scenario and exact result | N/A — pure value comparison, no I/O (NFR-ART-C); fresh `go test -race -v -count=1 ./...` is the harness: exit 0, all 3 packages pass, 0 `--- FAIL` lines, 0 `DATA RACE` reports |
| Rollback boundary | Delete `tool_result_test.go`; revert `message.go` (restore the AI-26.3-only `appendMessageObject`/`appendMessageContent`, remove `splitToolCalls`/`appendToolCallObject`/`appendToolResultMessages`/`appendToolResultObject`), `message_test.go` (restore the AI-26.3 `partKindDispositions` handoff text and the Text-only `assertKindTranslates`), `body.go` (restore the AI-26.3-only doc comment), `doc.go` (remove this slice's new section) — no AI-25 or AI-27 file touched |

#### Hand-off to Phase 7 (AI-26.6, reasoning refusal) — what Phase 7 MUST VERIFY, not assume

1. **Verify, do not assume, that `message_test.go`'s `partKindDispositions` entry for `ai.PartKindReasoning` is still accurate** once `policy.go`'s `refuse()` door exists: its `handoff` text currently states the door "must intercept a reasoning part ... before `appendBody` starts appending anything." Phase 7 must confirm the refusal actually fires *before* `appendBody`/`Translate` reaches `appendMessageObject` at all (i.e., a reasoning part anywhere in the request short-circuits the whole call with `ai.PreStreamFailure`+`ai.ErrUnsupportedCapability`, never partially rendering earlier messages first) — do not assume the existing hand-off text is correct without exercising it; verify by test, the same discipline this session applied to Phase 5's own hand-off claims.
2. **Verify the switch in `TestMessage_PartKindCoverage` needs updating**, not merely the `partKindDispositions` table: `PartKindReasoning`'s case currently calls `assertKindIsDeferred` (expects an unrecovered panic naming the kind). Once Phase 7 lands, translating a reasoning-bearing request must return a normal `error` (via `ai.PreStreamFailure`), not panic — `assertKindIsDeferred`'s own contract (checked panic *content*) will misfire against an error return instead of a panic. Phase 7 must either mark `PartKindReasoning` `handled: true` with a *new* assertion helper proving refusal-by-error (mirroring `assertKindTranslates`'s shape but asserting `errors.Is(err, ai.ErrUnsupportedCapability)` rather than "no error"), or explicitly justify why `assertKindIsDeferred`'s panic-based contract still applies — do not leave the existing helper silently exercising the wrong failure shape.
3. **Verify `appendContentPartObject`'s default-case panic path is still reachable and correctly scoped** after Phase 7 lands: this phase's own doc comment on that function states Reasoning is "the one member of `ai.PartKinds()` still deferred past this point," intercepted "earlier still, before `appendBody` appends anything at all." If Phase 7's refusal check runs late enough that a reasoning part inside a *multi-part* message could still reach `appendContentPartObject`'s panic before the refusal door fires, that is a genuine ordering defect this milestone's own evidence discipline would treat as a silent-loss risk, not a cosmetic one — verify the refusal door runs first for every position (first/last/mid-message, per S-ART-052), not just first-message-first-part.
4. **Do not assume `RoleTool`/`splitToolCalls` need any change for reasoning refusal.** `rolePermittedKinds[RoleAssistant]` permits `Text`, `Reasoning` and `ToolCall` together in one message; this phase's `splitToolCalls` only ever separates `ToolCall` from everything else, so a `Reasoning` part sits in the same `rest` slice a `Text` part would and reaches `appendMessageContent`/`appendContentPartObject` unchanged from Phase 5's own behavior. Phase 7 should verify this composition is still correct (no interaction bug from Phase 6's own tool-call split) rather than assuming Phase 6 left it untouched by inspection alone.

---

## Phase 7 — AI-26.6 Reasoning replay refuses (Slice 7) — COMPLETE (7/7)

- [x] 7.1 RED `reasoning_refusal_test.go`: a reasoning part, for each of the three reasoning states in turn, at first/last/mid-message position, across several messages — translation fails with `PreStreamFailure`+`ErrUnsupportedCapability`, no wire body. Covers S-ART-051, S-ART-052. **RED confirmed empirically, not assumed from the file's shape**: this package already compiled before the door existed (`Translate` has existed since Slice 1), so the red is a genuine unrecovered panic, not a compile error — verified via isolated `go test -race -run` per test name: all 9 registered `refusalCase`s (3 reasoning-state cases + 3 message-position cases + 3 intra-message-part-position cases) and `TestMessage_PartKindCoverage/reasoning` panicked at `appendContentPartObject`'s pre-existing default case (message.go), naming "reasoning" — the same transitional safety-net panic Phase 5/6 left in place, now proven to still fire pre-GREEN.
- [x] 7.2 GREEN: create `policy.go`'s `refuse(feature string) error` helper (the one shared refusal door) and wire the reasoning-part check to call it. **Landed**: `refuse` builds `ai.PreStreamFailure(ai.FailureReport{Category: ai.FailureCategoryUnsupportedCapability, Cause: errors.New(feature+" is not supported by this adapter")})`, exactly doc.go's own slice-1-era worked example; `refuseReasoning` walks `req.Messages()` × `message.Content()` in order, calling `refuse("reasoning content")` on the first `ai.PartKindReasoning` part found. `translation.go`'s `Translate` calls `refuseReasoning` BEFORE `appendBody`, returning `(nil, err)` immediately on refusal — no partial body, ever.
- [x] 7.3 Confirm the error names reasoning content as the unsupported feature (S-ART-053). **`TestReasoningRefusal_NamesReasoningContentAsTheUnsupportedFeature`**: `errors.Unwrap(err).Error()` contains `"reasoning content"` verbatim — reached via `Unwrap`, not the top-level `Error()` string, since `ai.Failure.Error()` deliberately never reproduces its own wrapped Cause (provider_failure.go's own redaction posture).
- [x] 7.4 Confirm the module's exported sentinel set is identical before/after the change — zero new sentinels (S-ART-054). **`TestPolicy_NoNewSentinelsExported`**: a `go/parser`-based AST scan (mirroring `ambient_authority_test.go`'s own idiom, AI-25.2) over this package's own non-test `.go` files, asserting zero exported `Err*`-shaped top-level `var`/`const` declarations — the set was empty before this phase and stays empty, since `refuse`/`refuseReasoning` are both unexported and every refusal reaches a caller through the already-exported `ai.ErrUnsupportedCapability`. Falsifiability proven directly (see 7.5's own record below), not merely asserted.
- [x] 7.5 **Mutation proof**: stage dropping the reasoning part and succeeding; record red against S-ART-051; revert (S-ART-055). **RED captured**: staged two coordinated changes — `refuseReasoning` returning `nil` unconditionally (policy.go) AND `appendMessageContent`/`appendContentPartObject` (message.go) rendering a `PartKindReasoning` part as an empty string / nothing instead of panicking, so `Translate` genuinely SUCCEEDS with the reasoning content silently gone, not merely panics for the old reason. All 9 registered `refusalCase`s, `TestMessage_PartKindCoverage/reasoning` and `TestReasoningRefusal_NamesReasoningContentAsTheUnsupportedFeature` failed by name with "no error, want a refusal" — the disabled-check-alone mutation would have just reproduced the pre-existing panic, so both the check AND the render path were mutated together to actually exercise "drops the part and succeeds" per S-ART-055's own wording. Reverted both files; confirmed via `git diff` byte-identical to the pre-mutation, uncommitted state; suite green (498/498, 0 FAIL). A second, smaller staged mutation (a scratch exported sentinel added to policy.go) independently proved 7.4's own guard bites, reverted the same way — disclosed as extra rigor, not one of 7.1–7.7's own numbered items.
- [x] 7.6 *(review)* `doc.go`: state plainly that a reasoning-bearing transcript fails hard here, name consumer-side stripping as the remedy, and record the duty as routed to AI-40 (S-ART-056). **Landed** — new "Reasoning replay refuses; Layer 2 must strip first (AI-26.6, R-ART-015)" section, citing AI-03 rule 4, AI-07.1 item 2, and AI-40 ("Publish the Layer 2 readiness contract", doc 0002 — confirmed by reading that milestone's own charter before citing it, not assumed) as the node that publishes the frozen-surface obligation. Also records the classification (refusal, not validation fault, cross-referencing the existing NFR-ART-E section) and the living-graph reopen trigger.
- [x] 7.7 Slice-7 closure: `make test` green, `make lint` clean; record evidence. **Closed**: fresh `go test -race -v -count=1 ./...` exit 0, all 3 packages pass (`agenttest`, `ai`, `ai/openaicompat`), 498 `--- PASS` lines, 0 `--- FAIL`, 0 `DATA RACE`. `make test` exit 0. `make lint` → "0 issues." (see deviation note below — one genuine lint defect was found and fixed, not suppressed). `go.mod` zero requires, no `go.sum`. Both AI-00 guards (`TestLayer1_ImportsOnlyStdlibAndItsOwnPackages_DenyByDefault`, `TestLayer1_ModuleHasNoDependencies_ZeroRequires`) and AI-25's ambient-authority guard (`TestAmbientAuthority_NoForbiddenCallSitesInAdapterSources`) pass individually, no allowlist edit.

**Deviation from design, disclosed (lint-driven cleanup, not a design change)**: updating `message_test.go`'s `TestMessage_PartKindCoverage` switch to call the new `assertKindRefuses` for `PartKindReasoning` (instead of `assertKindIsDeferred`) left `assertKindIsDeferred` with zero call sites anywhere in the package — `make lint` caught this as genuine dead code (`unused: func assertKindIsDeferred is unused`, and transitively `field handoff is unused`, since that function was the only reader of `partKindDisposition.handoff`). Per this milestone's own "no defect left uncaught, never suppress" discipline, both were deleted rather than kept via a lint-suppression comment: `partKindDisposition` lost its `handoff string` field and its "deferred" disposition entirely (every one of `ai.PartKinds()`'s 4 current members is now either `refused: false` — translates — or `refused: true` — refuses; none is left merely deferred). `lookupPartKindDisposition`'s own `ok == false` branch remains the mechanism that still catches a genuinely new, unaccounted-for `ai.PartKind` the moment the vocabulary grows again — a future truly-deferred kind's own panic-based proof can be written fresh at that time, the same way this phase wrote `assertKindRefuses` fresh rather than reusing a stale shape. `message.go`'s `appendContentPartObject` default-case panic remains (message.go, not message_test.go): it is still called at runtime on every translation and is a live `switch` branch, not a statically-dead top-level declaration, so the Go `unused` analyzer does not (and structurally cannot) flag it the same way.

**Measured diff vs `feat/ai-26-6-tool-results`**: 6 files, 564 insertions(+), 95 deletions(-) — 659 total changed lines (`git diff --shortstat feat/ai-26-6-tool-results..HEAD` after `git add -N` for the two new files, then unstaged). Above the ~160–350 corrected forecast range, consistent with this milestone's own recorded 2–4× estimation-undershoot finding (design.md, slice 1) rather than a new problem: no fission trigger was pre-declared for this node (unlike Slice 5's own ~1,200-line pre-declared seam) and 659 lines is not at a scale that risks reviewer load the way that threshold addresses. Roughly a third of the total is prose (doc.go's new section, message.go's and translation.go's updated doc comments); the `message_test.go` figure includes ~70 deleted lines from removing `assertKindIsDeferred` (the lint-driven cleanup above), which is a net simplification, not new complexity.

**Commits** on `feat/ai-26-7-reasoning-refusal` (off `feat/ai-26-6-tool-results`), 2 total:
1. `feat(openaicompat): refuse reasoning-bearing requests via the shared unsupported-capability door` — `policy.go` (new), `translation.go` (`Translate` wires `refuseReasoning`; doc comment updated), `reasoning_refusal_test.go` (new: 9 registered `refusalCase`s, the feature-naming test, the sentinel-export guard), `message_test.go` (`partKindDispositions`'s `PartKindReasoning` entry now `handled: true, refused: true`; `TestMessage_PartKindCoverage`'s switch calls the new `assertKindRefuses`; `assertKindIsDeferred` and the `handoff` field removed as genuine dead code, caught by `make lint`).
2. `docs(openaicompat): record reasoning hard-failure and route Layer 2 stripping duty to AI-40` — `doc.go` (new "Reasoning replay refuses; Layer 2 must strip first" section), `message.go` (`appendContentPartObject`/`appendMessageContent` doc comments updated in place to state the refusal has landed, not "in the meantime").

No `chore: record evidence` commit — matches Slices 1–6's own actual practice: `tasks.md` and this milestone's Engram `apply-progress` topic carry the evidence; the `openspec/` change folder stays untracked in git until this milestone's own archive step (confirmed still untracked at session end, alongside two other untracked, pre-existing `openspec/changes/` directories unrelated to this session's own work — `cachicamas-ai-first-provider-decision`'s `verify-report.md` and `cachicamas-ai-stream-decoder/`, neither touched this session).

#### TDD Cycle Evidence (Phase 7, complete)

| Task | Test | RED | GREEN |
|---|---|---|---|
| 7.1 S-ART-051/052 | 9 registered `refusalCase`s (3 states × 3 positions) | Genuine: unrecovered panic at `appendContentPartObject`'s pre-existing default case, naming "reasoning" | Passes after `policy.go` + `translation.go`'s `Translate` wiring landed |
| 7.1 S-ART-051/052 | `TestMessage_PartKindCoverage/reasoning` (via new `assertKindRefuses`) | Genuine: identical unrecovered panic | Passes after GREEN landed |
| 7.3 S-ART-053 | `TestReasoningRefusal_NamesReasoningContentAsTheUnsupportedFeature` | Genuine: same panic (no error to unwrap yet) | Passes after GREEN landed |
| 7.4 S-ART-054 | `TestPolicy_NoNewSentinelsExported` | N/A — pin (true before this phase's code existed, since the set was already empty) | Stays green after GREEN landed; falsifiability proven by a staged scratch sentinel (reverted) |
| 7.5 S-ART-055 | staged "disable check + silently drop the part in the render path" mutation | Genuine: all 9 `refusalCase`s + `TestMessage_PartKindCoverage/reasoning` + the feature-naming test failed by name with "no error, want a refusal" | Reverted; confirmed byte-identical via `git diff`; suite green (498/498) |

#### Work Unit Evidence

| Evidence | Value |
|---|---|
| Focused test command and exact result | `go test -race -v -run 'TestRefusalCases_FailWithUnsupportedCapability\|TestReasoningRefusal_NamesReasoningContentAsTheUnsupportedFeature\|TestPolicy_NoNewSentinelsExported\|TestMessage_PartKindCoverage' ./src/ai/openaicompat/...` → all PASS, exit 0 |
| Runtime harness command/scenario and exact result | N/A — pure value comparison, no I/O (NFR-ART-C); fresh `go test -race -v -count=1 ./...` is the harness: exit 0, all 3 packages pass, 498 `--- PASS`, 0 `--- FAIL`, 0 `DATA RACE` |
| Rollback boundary | Delete `policy.go`, `reasoning_refusal_test.go`; revert `translation.go` (restore the unconditional `return appendBody(req), nil` and its Slice-1-era doc comment), `message.go` (restore the AI-26.5-era doc comments on `appendContentPartObject`/`appendMessageContent`), `message_test.go` (restore `handoff string`, the `PartKindReasoning` deferred entry, `assertKindIsDeferred`, and the Slice-5-era switch case), `doc.go` (remove the new "Reasoning replay refuses" section) — no AI-25 or AI-27 file touched |

#### Hand-off to Phase 8 (AI-26.8, inventory + exhaustive policy walk) — what Phase 8 MUST VERIFY, not assume

1. **Verify `refuse(feature string) error` (policy.go) needs no signature change before reuse.** Phase 8.6's production policy table is `{feature, disposition, reason}` (design.md decision 6) — `refuse` only ever takes a bare `feature` name. Verify by test that a `reason` recorded in the table (why a feature is refused) does not need to reach the wire error text: S-ART-053 only requires the error to name the FEATURE, never asks the message to explain WHY, and `refuse`'s own doc comment states the reason lives in review-scenario documentation (`doc.go`, `*(review)*` confirmations), not in `Cause`'s own prose. Do not add a second parameter or a second construction site without first confirming this gap is real, not assumed.
2. **Verify whether "reasoning" is the ONLY inventoried feature whose disposition is `refuse`.** Run the actual inventory walk (8.1's five vocabulary enumerators + the AST-scanned `With*` constructors) before assuming `refuse` is called from exactly one site (`refuseReasoning`) forever. Design decision 6 currently names only "reasoning → refuse" (cache regions → drop; everything else → translate), but that decision predates Phase 8's own mechanical derivation — verify the derived inventory agrees before treating `refuseReasoning` as the sole caller in the finished policy table, not only in today's code.
3. **Verify the pre-`appendBody` ordering contract, if Phase 8 adds any further request-level refusal check.** `Translate` (translation.go) currently runs exactly one check — `refuseReasoning` — to completion over the WHOLE request before `appendBody` is ever called, so a refusal never leaves a partial wire body. If item 2 above turns up an additional refuse-disposition feature needing its own request-scanning function, verify by test that it is composed the same way (checked to completion before `appendBody`, not interleaved with rendering), and verify what happens when two independent checks could both fire on the same request (which one's error is returned — verify a deterministic, tested answer, not an accidental one from source order).
4. **Verify `TestPolicy_NoNewSentinelsExported` (reasoning_refusal_test.go) still passes after `policy.go` grows in Phase 8, and reuse it rather than writing a parallel scan.** Its scan already covers every non-test `.go` file in this directory mechanically (`os.ReadDir(".")`), so Phase 8's own growth of `policy.go` is auto-covered with no edit needed to this test — verify this is actually true by running it after 8.6's table lands, the same "verify, don't assume" discipline this phase applied to Phase 5/6's own hand-offs, rather than trusting this note.
5. **Verify whether `refusalCases`/`registerRefusals` (reasoning_refusal_test.go) is the right vehicle for 8.8's per-refuse-feature RED, or whether it is not needed at all.** This registry mirrors `expectationCases` and is extensible with no edit to this file (`registerRefusals` from any later `init()`), but Phase 8's own walk (S-ART-074) is inventory-DERIVED, not hand-registered — if item 2 confirms reasoning is the only refuse-disposition feature, 8.8's own case may be fully discharged by this phase's existing 9 `refusalCase`s and need no new registration at all. Verify which is true before adding cases speculatively.
6. **Do not assume `splitToolCalls` or the message/part-order preservation guarantees need any change for the reasoning refusal.** This phase added no new code to those paths; `refuseReasoning` runs entirely before them. Phase 8's own exhaustive walk exercises a different surface (options, tool choice, cache regions, extensions) that already has its own render paths — verify by test, not by inspection, exactly as this phase's own hand-off from Phase 5/6 required.

---

## Phase 8 — AI-26.8 Unsupported-feature policy (Slice 8) — COMPLETE (14/14)

- [x] 8.1 Create `feature_inventory_test.go`: runtime-enumerate the five closed vocabularies (`PartKinds()`, `ReasoningStates()`, `ToolChoiceModes()`, `CacheRegions()`, `Roles()`) plus the AST scan over `package ai`'s non-test sources for exported `With*` returning `RequestOption` (ten verified constructors). Cross-check against the production policy table and a per-feature witness table; any disagreement fails, naming the member. Covers S-ART-069. **Verified, not trusted**: `Roles()` (role.go:96) confirmed as the fifth vocabulary (4+3+4+3+3 = 17 members); `grep -rn "^func With" *.go | grep -v _test.go` over `package ai` confirmed exactly 10 constructors (`WithModel`, `WithMessages`, `WithMaxOutputTokens`, `WithTemperature`, `WithTopP`, `WithStopSequences`, `WithTools`, `WithToolChoice`, `WithProviderExtension`, `WithSystemInstruction`) — `TestFeatureInventory_MatchesProductionPolicyExactly` asserts this count directly rather than assuming it, and cross-checks the full 28-feature inventory (17 vocabulary members + 9 unsplit constructors + `WithProviderExtension`'s own 2-way split) against `policy.go`'s `featurePolicy` table in both directions (`unaccountedFeatures`). `TestFeatureInventory_NonTranslateFeaturesHaveExactlyOneWitness` is the "per-feature witness table" cross-check, over `policy_walk_test.go`'s `refuseWitnesses`/`dropWitnesses` (the 8 non-translate rows only — a translate-disposition row is already proven byte-exact by Phases 1-7's own `expectationCases`, so a second witness here would be pure duplication, disclosed explicitly in this file's own doc comment).
- [x] 8.2 **Bite proof 1/2 — vocabulary**: add a scratch sixth member to one closed-vocabulary enumerator with no policy entry; run the guard; record red naming the unaccounted member; revert (S-ART-070). **Implemented as a durable, permanent proof instead of a transient stage-and-revert of a real `ai/*.go` file** — `TestFeatureInventory_UnaccountedVocabularyMemberFailsNamingIt` feeds a synthetic `"CacheRegion: scratch_bite_proof_region"` name through the exact same production `unaccountedFeatures` comparison the real guard (8.1) uses, and asserts it is named in `missingFromPolicy`. Disclosed deviation: none of the five vocabularies currently holds 5 members (4/3/4/3/3, verified above), so "a scratch sixth vocabulary member" names the shape of the proof, not a literal count any one vocabulary reaches by adding one — recorded in this file's own comment rather than silently reinterpreted. RED was captured via the compile-error class (8.14's own TDD evidence table): this test failed to compile (`unaccountedFeatures` undefined) before `policy.go` grew, then passed unmodified once it landed — its own falsifiability (would it pass vacuously if `unaccountedFeatures` were broken?) is structural: the function did not exist at all pre-GREEN.
- [x] 8.3 **Bite proof 2/2 — constructor**: add a scratch eleventh `With*` constructor to the neutral surface with no policy entry; run the guard; record red naming it; revert (S-ART-071). **Also durable**: `TestFeatureInventory_UnaccountedConstructorFailsNamingIt` writes a synthetic `WithScratchEleventhOption` constructor into a `t.TempDir()` (never a real `ai/*.go` file), runs the real `requestOptionConstructorNames` AST scanner against it end to end, confirms the scanner itself notices it, then confirms `unaccountedFeatures` names it as missing. No real, tracked file was ever staged or reverted — see feature_inventory_test.go's own file-header comment for the full "durable over transient" rationale (AI-27's own verify warning about a staged-then-deleted proof leaving no permanent regression protection, and AI-24's `TestSequenceGuard_*_Fails` as this repo's own precedent for the durable pattern).
- [x] 8.4 Confirm the inventory's ground truth is the neutral package's own declarations, not a list the adapter maintains about itself (S-ART-072). **`TestFeatureInventory_ScanReflectsTheTargetDirectoryNotAFixedList`**: three fixtures (zero, one, and three constructors spread across two files, mixed with an unexported and a wrong-return-type function) each produce exactly the expected names — a hand-maintained mirror would report the same answer regardless of which fixture ran. The vocabulary half needs no analogous proof: `discoverVocabularyFeatures` calls `ai.PartKinds()`/`ai.ReasoningStates()`/`ai.ToolChoiceModes()`/`ai.CacheRegions()`/`ai.Roles()` directly, every time, with no local mirror anywhere in this package to fall out of sync.
- [x] 8.5 *(review)* Record the decision: both derivation surfaces named with their mechanisms, and the reason reflection is rejected (S-ART-073). **Landed** — `doc.go`'s new "Feature inventory: hybrid derivation, reflection rejected" section: the five vocabularies via runtime enumeration (no reflection needed — plain exported function calls), the ten `With*` constructors via `go/parser` AST scan (the same idiom `content_part_registry_test.go`/`validation_registry_internal_test.go` already establish), and reflection rejected for a reason stronger than style — Go's `reflect` package has no operation that enumerates "every top-level function declared in a package": there is no runtime value that IS the declaration list to reflect over, so only a source-level parser can answer this question.
- [x] 8.6 Complete `policy.go`'s production table: every inventoried feature gets exactly one disposition — translate / deliberately drop / refuse (extension surface counted as two entries: own-namespace→translate, foreign→drop; cache regions→drop; reasoning→refuse; everything else→translate). **Landed**: `disposition` type (`dispositionTranslate`/`dispositionDrop`/`dispositionRefuse`), `policyEntry` struct (`{feature, disposition, reason}`), `featurePolicy` (28 rows, a slice — never a map, this package's own "Map discipline" restated for an audit table), `resolveFeaturePolicy` (linear-scan lookup). 28, not 15 (5 vocabularies + 10 constructors naively counted): `ReasoningState`'s 3 members and `CacheRegion`'s 3 members each get their own row despite sharing one disposition with their siblings — deliberate member-level uniformity across all five vocabularies, not a shortcut taken for three of them and not the other two (documented in both `policy.go`'s own header and `doc.go`'s new "The policy is total" section).
- [x] 8.7 Create `policy_walk_test.go`: exhaustive inventory-driven walk — every feature resolves to exactly one of the three dispositions, none unresolved (S-ART-074). **`TestPolicyWalk_EveryInventoriedFeatureResolvesToExactlyOneDisposition`**: walks `fullInventory` (the same discovery `feature_inventory_test.go` uses), confirms every feature resolves via `resolveFeaturePolicy`, to a recognised disposition, carrying a non-empty reason.
- [x] 8.8 RED: each refuse-policy feature — request expressing it fails with `PreStreamFailure`+`ErrUnsupportedCapability`, error names the feature (S-ART-075). **`TestPolicyWalk_EveryRefuseFeatureFailsNamingIt`**: 4 sub-tests, one per `dispositionRefuse` row (`PartKind: reasoning`, `ReasoningState: text/redacted/token-only`) — each witness (`policy_walk_test.go`'s `refuseWitnesses`) builds a reasoning-bearing request in that exact state and asserts `errors.Is(err, ai.ErrUnsupportedCapability)` with no wire body. **Confirmed, not assumed** (Phase 7 hand-off item 2): all 4 refuse-disposition rows route through the identical `refuseReasoning`/`refuse` mechanism — "reasoning" is the ONLY refuse-disposition feature this milestone's mechanical inventory produces, so no second refusal call site was needed.
- [x] 8.9 RED: each deliberately-drop feature — marked request and twin byte-identical, recorded reason exists (S-ART-076). **`TestPolicyWalk_EveryDropFeatureTranslatesByteIdenticalToItsTwin`**: 4 sub-tests, one per `dispositionDrop` row (`CacheRegion: tools/system/messages`, `WithProviderExtension: foreign namespace`) — each witness (`dropWitnesses`) builds a marked/unmarked twin (re-derived locally, mirroring `cache_marker_test.go`/`extension_test.go`'s own idiom, since this file is the internal `package openaicompat` and cannot reach those external `_test` package helpers) and asserts byte-identical translation plus a non-empty recorded reason.
- [x] 8.10 **Mutation proof**: stage a refuse-policy feature dropped silently and succeeding; record red against the walk; revert (S-ART-077). **RED captured, both the check AND the render path mutated together** (this session's own evidence-discipline shape 9 avoided): staged `refuseReasoning` (`policy.go`) returning `nil` unconditionally, AND `appendMessageContent`/`appendContentPartObject` (`message.go`) rendering a reasoning part as an empty string / nothing instead of panicking, so `Translate` genuinely SUCCEEDED with the content silently gone. All 4 of `TestPolicyWalk_EveryRefuseFeatureFailsNamingIt`'s sub-tests, all 4 of `TestPolicyWalk_EveryRefusalUsesTheSameCategoryUniformly`'s sub-tests, `TestMessage_PartKindCoverage/reasoning`, all 9 of Phase 7's own `refusalCase`s, and `TestReasoningRefusal_NamesReasoningContentAsTheUnsupportedFeature` all failed by name with "no error, want a refusal" (`TestMessage_PartKindCoverage/reasoning` reported "Translate: no error for a refused kind (reasoning)"). Reverted both files; `git diff --stat` confirmed `message.go` byte-identical to its pre-mutation, committed state (zero diff, not merely a small one); `policy.go`'s diff returned to exactly this slice's own additive growth. Suite green again after revert (confirmed by re-running the same test set).
- [x] 8.11 RED: a feature added to the inventory with no policy entry fails the walk — inventory-driven, not list-driven (S-ART-078; reuses the bite proofs 8.2/8.3 as the concrete staged case). **`TestPolicyWalk_UnaccountedFeatureFailsTheWalk`**: appends a synthetic `"scratch: a feature the walk has never accounted for"` name to the REAL `fullInventory(t)` result and confirms `unresolvedPolicyFeatures` — the identical function `TestPolicyWalk_EveryInventoriedFeatureResolvesToExactlyOneDisposition` (8.7) calls — names it, proving the WALK itself (not only `feature_inventory_test.go`'s lower-level comparison) is inventory-driven. Reuses 8.2/8.3's own synthetic-extra-feature technique rather than a third re-implementation, per this task's own instruction.
- [x] 8.12 Confirm every refusal this milestone produces (26.6 + 26.8.2) uses the same category uniformly, zero new sentinels (S-ART-079). **`TestPolicyWalk_EveryRefusalUsesTheSameCategoryUniformly`**: asserts `errors.Is(err, ai.ErrUnsupportedCapability)` across all 4 refuse witnesses. **Reused, not duplicated** (Phase 7 hand-off item 4, confirmed by actually running it, not trusting the note): `reasoning_refusal_test.go`'s `TestPolicy_NoNewSentinelsExported` — its `os.ReadDir(".")` scan already covers `policy.go`'s own Phase-8 growth mechanically, with no edit to that test needed; re-ran it explicitly after `featurePolicy` landed and confirmed it still passes (the exported sentinel set is still empty).
- [x] 8.13 RED/pin: translating a request leaves it equal to an independently built copy afterward — purity pin (S-ART-083). **`TestTranslate_LeavesTheRequestEqualToAnIndependentlyBuiltCopy`**: a request exercising a cache-marked system instruction, a user/assistant/tool message triad (including a tool call and its result), a cache-marked declared tool, a tool choice, every generation option and a provider extension — translated, then compared via `ai.Request.Equal` (the exported, field-by-field comparison, not an unexported field walk) against an independently built copy, both before and after `Translate` runs.
- [x] 8.14 Slice-8 closure: `make test` green, `make lint` clean; record evidence. **Closed**: fresh `go test -race -v -count=1 ./...` exit 0, all 3 packages pass (`agenttest`, `ai`, `ai/openaicompat`), 1805 `--- PASS` lines, 0 `--- FAIL`, 0 `DATA RACE` — repeated across 5 independent invocations, identical results each time (NFR-ART-F). `make test`/`make lint` (`0 issues.`) both green. `go.mod` zero requires, no `go.sum`. Both AI-00 guards and AI-25's ambient-authority guard pass individually, no allowlist edit.

**Deviation from design, disclosed**: bite proofs 8.2/8.3 (and 8.4, 8.11) are implemented as **durable, permanent tests over synthetic candidate sets** rather than transient stage-then-revert mutations of a real `ai/*.go` file, per this session's own explicit instruction to prefer the durable shape "where reasonably possible". This was reasonably possible here because the AST scan (`requestOptionConstructorNames`) and the set comparison (`unaccountedFeatures`) both factor cleanly into pure/parameterized functions: the scan takes a directory parameter (production calls it with `".."`; the bite proofs call it with a `t.TempDir()`), and the comparison takes plain `[]string` candidates (production calls it with the real inventory; the bite proofs call it with a synthetic one) — the exact same functions production relies on, never a re-implementation. This also avoided ANY need to write to, or revert, a file under the neutral `package ai` this milestone does not own, keeping NFR-ART-B's "none modified" property true throughout the whole session rather than only at its end. The one GENUINE stage-then-revert mutation this slice performs (8.10, silently dropping a refuse-policy feature) is against files this package already owns (`policy.go`, `message.go`) and mirrors Phase 7's own S-ART-055 mutation exactly, including its own "mutate the check AND the render path together" lesson.

---

## Phase 9 — Cross-cutting final sweep (after Slice 8) — COMPLETE (9/9)

- [x] 9.1 Confirm `backend/agent/go.mod` still declares zero `require` directives and both AI-00 import guards pass (NFR-ART-A, S-ART-080). **Confirmed**: `go.mod` reads `module github.com/cachicamas/backend/agent` / `go 1.26.3`, zero `require` directives, no `go.sum`. `TestLayer1_ImportsOnlyStdlibAndItsOwnPackages_DenyByDefault` and `TestLayer1_ModuleHasNoDependencies_ZeroRequires` both pass.
- [x] 9.2 *(review)* Inspect every path under the neutral `package ai` — none modified (NFR-ART-B, S-ART-081). **Confirmed**: `git status --short backend/agent/src/ai/` (excluding the `openaicompat/` subdirectory this milestone owns) reports nothing — zero files touched under the neutral package for the whole session, including during the durable bite-proof design (8.2/8.3's own deviation note, above) and the one genuine mutation proof (8.10, scoped to `openaicompat/` only).
- [x] 9.3 Run the full suite (AI-00…AI-25 plus this change) — every predecessor test passes unchanged (S-ART-082). **Confirmed**: fresh `go test -race -v -count=1 ./...` — exit 0, 3 packages (`agenttest`, `ai`, `ai/openaicompat`) all `ok`, 1805 `--- PASS`, 0 `--- FAIL`.
- [x] 9.4 *(review)* Enumerate this milestone's tests — none constructs a client, test server, or socket (NFR-ART-C, S-ART-084). **Confirmed**: `grep -l "httptest\|net.Listen\|\"net/http\""` across all 12 of AI-26's own test files (`translation_test.go` through this slice's own `feature_inventory_test.go`/`policy_walk_test.go`) returns none. AI-25's own `client_test.go`/`viability_test.go` do construct a local test server — that is AI-25's own concern (transport plumbing), not this milestone's, and pre-dates this change.
- [x] 9.5 Confirm no golden-file tree or `-update` flag exists anywhere in the package (NFR-ART-D, S-ART-085). **Confirmed**: `grep -rn "\-update\|golden"` across every non-test `.go` source in `openaicompat/` returns nothing.
- [x] 9.6 *(review)* Confirm the landed test documentation states the inline-literal rationale (S-ART-086). **Confirmed**: `translation_test.go`'s own file header states expectation cases are inline literals, "this adapter uses no golden-file tree and no -update flag" — landed at Slice 1, unmodified and still accurate through this slice.
- [x] 9.7 *(review)* Confirm the AI-25-vs-AI-26 taxonomy distinction is recorded with AI-03 §10.4 cited (NFR-ART-E, S-ART-087). **Confirmed**: `doc.go`'s "Refusal taxonomy: AI-25 vs AI-26 (NFR-ART-E)" section (landed Slice 1, closed Slice 7) still carries the comparison table and its AI-03 §10.4 citation, unmodified by this slice's own additions.
- [x] 9.8 Run the whole milestone's test set repeatedly under `-race`; confirm identical results and no race-detector report (NFR-ART-F, S-ART-089). **Confirmed**: 5/5 independent `go test -race -count=1 ./...` invocations — exit 0, 0 `--- FAIL`, 0 `DATA RACE` every time.
- [x] 9.9 Final evidence walk: every runnable scenario carries recorded red, green and refactor notes; every `*(review)*` scenario carries a recorded confirmation naming what was read (S-ART-088). **Confirmed**: see this file's own Evidence Log (below) for S-ART-069 through S-ART-089, and the TDD Cycle Evidence / Work Unit Evidence tables directly below for Phase 8's own red/green/refactor record.

**AI-26 is now COMPLETE**: all 99 tasks across Phases 1-9 are `[x]`. All 21 requirements (`R-ART-001`…`R-ART-021`) and all 6 NFRs (`NFR-ART-A`…`NFR-ART-F`) are discharged per the Requirement / NFR Coverage Map below — R-ART-020/021 (this slice's own) and NFR-ART-C/F (Phase 8.13/9.4, 9.8/9.9) are the last to close.

#### TDD Cycle Evidence (Phase 8, complete)

| Task | Test | RED | GREEN |
|---|---|---|---|
| 8.1 S-ART-069 | `TestFeatureInventory_MatchesProductionPolicyExactly`, `TestFeatureInventory_NonTranslateFeaturesHaveExactlyOneWitness` | Genuine: compile error (`undefined: featurePolicy`, `dispositionRefuse`, `dispositionDrop`, `resolveFeaturePolicy`) before `policy.go` grew | Passes after `policy.go`'s `featurePolicy`/`disposition`/`resolveFeaturePolicy` landed |
| 8.2 S-ART-070 | `TestFeatureInventory_UnaccountedVocabularyMemberFailsNamingIt` | Genuine: same compile-error class (`unaccountedFeatures` undefined) | Passes after GREEN landed; durable, permanent |
| 8.3 S-ART-071 | `TestFeatureInventory_UnaccountedConstructorFailsNamingIt` | Genuine: same compile-error class | Passes after GREEN landed; durable, permanent |
| 8.4 S-ART-072 | `TestFeatureInventory_ScanReflectsTheTargetDirectoryNotAFixedList` (3 sub-cases) | Genuine: same compile-error class (whole file failed to build) | Passes after GREEN landed |
| 8.7 S-ART-074 | `TestPolicyWalk_EveryInventoriedFeatureResolvesToExactlyOneDisposition` | Genuine: same compile-error class | Passes after GREEN landed |
| 8.8 S-ART-075 | `TestPolicyWalk_EveryRefuseFeatureFailsNamingIt` (4 sub-tests) | Genuine: same compile-error class | Passes after GREEN landed |
| 8.9 S-ART-076 | `TestPolicyWalk_EveryDropFeatureTranslatesByteIdenticalToItsTwin` (4 sub-tests) | Genuine: same compile-error class | Passes after GREEN landed |
| 8.10 S-ART-077 | staged "disable `refuseReasoning` + silently drop the part in the render path" mutation | Genuine: 4+4+1+9+1 = 19 sub-tests/tests across both Phase 7's and Phase 8's own suites failed by name with "no error, want a refusal" | Reverted; `message.go` confirmed byte-identical via `git diff --stat` (zero diff); suite green |
| 8.11 S-ART-078 | `TestPolicyWalk_UnaccountedFeatureFailsTheWalk` | Genuine: same compile-error class | Passes after GREEN landed; durable, permanent |
| 8.12 S-ART-079 | `TestPolicyWalk_EveryRefusalUsesTheSameCategoryUniformly` + re-run of `TestPolicy_NoNewSentinelsExported` | Genuine: same compile-error class (new test); pin (existing guard, re-run not re-written) | Passes after GREEN landed |
| 8.13 S-ART-083 | `TestTranslate_LeavesTheRequestEqualToAnIndependentlyBuiltCopy` | Genuine: same compile-error class | Passes after GREEN landed |

All of 8.1-8.13's own new tests shared one compile-error RED (the whole package failed to build until `policy.go` grew), matching Slice 4's own precedent for a batch of new symbols landing together — recorded once here rather than repeated 11 times.

#### Work Unit Evidence

| Evidence | Value |
|---|---|
| Focused test command and exact result | `go test -race -v -run 'TestFeatureInventory\|TestPolicyWalk\|TestTranslate_LeavesTheRequestEqual' ./src/ai/openaicompat/...` → all PASS, exit 0 |
| Runtime harness command/scenario and exact result | N/A — pure value comparison, no I/O (NFR-ART-C); fresh `go test -race -v -count=1 ./...` is the harness: exit 0, all 3 packages pass, 1805 `--- PASS`, 0 `--- FAIL`, 0 `DATA RACE`, repeated 5x with identical results |
| Rollback boundary | Delete `feature_inventory_test.go`, `policy_walk_test.go`; revert `policy.go` (remove `disposition`/`policyEntry`/`featurePolicy`/`resolveFeaturePolicy`, restore the AI-26.6-era header), `doc.go` (remove the two new sections) — no AI-25 or AI-27 file touched, no `ai/*.go` file touched |

**Measured diff vs `feat/ai-26-7-reasoning-refusal`**: 4 files, 1260 insertions(+), 1 deletion(-) — 1261 total changed lines (`git diff --shortstat`, new files counted via `git add -N` intent-to-add, then unstaged). Above the ~600-1200 corrected forecast range, consistent with this milestone's own recorded 2-4× estimation-undershoot finding rather than a new problem — no fission trigger was pre-declared for this node (unlike Slice 5's own ~1,200-line pre-declared seam), and this is the milestone's own final planned slice (PR8 of 8), so no further chained split was considered. A meaningful share of the total is doc comments and evidence-discipline prose (`doc.go`'s 2 new sections; both new test files' own extensive header/function comments recording the durable-over-transient design decision).

**Commits** on `feat/ai-26-8-unsupported-policy` (off `feat/ai-26-7-reasoning-refusal`) — landed as 2 commits, deviating from the 5-commit "as planned" list below for the same reason Slices 5-7's own landed commits deviated from their plans: `feature_inventory_test.go`, `policy_walk_test.go` and `policy.go`'s growth are one indivisible compiling unit (neither test file compiles without `featurePolicy`/`resolveFeaturePolicy`/`disposition` existing), so splitting them would mean committing an intermediate state that does not build. No `chore: record evidence` commit is made, matching Slices 1-7's own actual practice.

---

## Requirement / NFR Coverage Map

| ID | Discharged by |
|---|---|
| R-ART-001 | Phase 1.0 |
| R-ART-002 | Phase 1.1 |
| R-ART-003 | Phase 1.2 |
| R-ART-004 | Phase 1.3 |
| R-ART-005 | Phase 2.1–2.2 |
| R-ART-006 | Phase 2.3–2.6 |
| R-ART-007 | Phase 5.1–5.3 |
| R-ART-008 | Phase 5.4–5.5 |
| R-ART-009 | Phase 5.6–5.9 |
| R-ART-010 | Phase 3.1–3.3 |
| R-ART-011 | Phase 3.4–3.6 |
| R-ART-012 | Phase 6.1–6.3 |
| R-ART-013 | Phase 6.4–6.6 |
| R-ART-014 | Phase 6.7–6.10 |
| R-ART-015 | Phase 7.1–7.6 |
| R-ART-016 | Phase 4.1–4.2 |
| R-ART-017 | Phase 4.5–4.6 (bytes emitted from Phase 1.1) |
| R-ART-018 | Phase 4.3–4.4 |
| R-ART-019 | Phase 4.7–4.11 |
| R-ART-020 | Phase 8.1–8.5 |
| R-ART-021 | Phase 8.6–8.12 |
| NFR-ART-A | Phase 9.1 |
| NFR-ART-B | Phase 9.2–9.3 |
| NFR-ART-C | Phase 8.13, 9.4 |
| NFR-ART-D | Phase 9.5–9.6 |
| NFR-ART-E | Phase 1.4.1, 9.7 |
| NFR-ART-F | Phase 9.8–9.9 (recorded per slice throughout) |

All 21 requirements and all 6 NFRs are mapped. No gap found.

## Work-Unit Commit Sequence (per `work-unit-commits`)

**Slice 1**: (1) `docs(openaicompat): cite vendor wire-shape claims and record citation-gate provenance` · (2) `feat(openaicompat): scaffold translation entry point and byte-appender skeleton` · (3) `test(openaicompat): prove skeleton request matches byte-exact expectation with usage opt-in` · (4) `test(openaicompat): prove cross-run determinism and label the in-process check a pin` · (5) `test(openaicompat): scan expectation surface for credential-shaped sentinels` · (6) `chore(openaicompat): record slice-1 red/green evidence`.

**Slice 2**: (1) `feat(openaicompat): render ordered system segments preserving order and content` · (2) `feat(openaicompat): drop cache-boundary markers whole, proven by byte-identical twin` · (3) `docs(openaicompat): record why cache markers vanish, citing AI-11.3` · (4) `chore(openaicompat): record slice-2 evidence`.

**Slice 3**: (1) `feat(openaicompat): translate tool declarations with byte-faithful schema splice` · (2) `test(openaicompat): prove tool ordering deterministic across independent process runs` · (3) `chore(openaicompat): record slice-3 evidence`.

**Slice 4 (as planned, for reference)**: (1) `feat(openaicompat): translate generation options distinguishing zero from unset` · (2) `feat(openaicompat): assert the usage opt-in positively on every expectation` · (3) `feat(openaicompat): leave output-token limit explicitly absent when unset` · (4) `feat(openaicompat): merge own-namespace provider extension and ignore foreign namespaces whole` · (5) `chore(openaicompat): record slice-4 evidence`.

**Slice 4 (as landed, COMPLETE — 5 commits, see Phase 4's own "Escape hatch: coordinator ruling" note)**: (1) `9bf059c feat(openaicompat): translate generation options and tool_choice, respecting presence over value` · (2) `acc7019 test(openaicompat): assert the usage opt-in positively across the total expectation walk` · (3) `8cd0d46 docs(openaicompat): record the dead output-limit branch and the blocked escape-hatch namespace` · (4) `60cb40f feat(openaicompat): merge own-namespace provider extension and ignore foreign namespaces whole` · (5) `a1a6b70 docs(openaicompat): record the coordinator ruling defining this adapter's reserved namespace`. Commit (4) landed after the coordinator's ruling resolved the namespace question this slice had correctly halted on rather than inventing.

**Slice 5 (as planned, for reference)**: (1) `feat(openaicompat): translate every readable content-part variant against its own expectation` · (2) `feat(openaicompat): preserve message and intra-message part order exactly` · (3) `feat(openaicompat): render consecutive same-role messages as distinct wire messages` · (4) `docs(openaicompat): record the no-merge decision and its reason` · (5) `chore(openaicompat): record slice-5 evidence`.

**Slice 5 (as landed)**: items (1)-(3) above are one indivisible code change, not three — `appendMessageContent`/`appendContentPartObject` render every readable variant, preserve order and (structurally, via the unedited `appendMessagesField` loop) never merge, all in the same function bodies; splitting them into separate commits would have meant committing intermediate states that do not compile or do not pass. No `chore: record evidence` commit is made, matching Slice 1-4's own actual practice: `tasks.md` and this milestone's Engram apply-progress carry the evidence; the openspec change folder stays untracked in git until this milestone's own archive step, per this repository's established convention. Landed as 2 commits: (1) `feat(openaicompat): translate content-part variants preserving message and part order` (message.go, body.go, system.go's stale-reference fix, message_test.go — including this session's crash fix and sort-gap-closing test) · (2) `docs(openaicompat): record the no-merge decision and correct a stale cache-boundary note` (doc.go's new no-merge section plus this session's correction of AI-26.4's own stale "updated to reflect completion" claim).

**Slice 6 (as planned, for reference)**: (1) `feat(openaicompat): translate tool results into the vendor's distinct tool-role message shape` · (2) `feat(openaicompat): pass tool-call identifiers through byte-identical, never minted` · (3) `feat(openaicompat): preserve result-to-call correlation across an interleaved multi-call turn` · (4) `chore(openaicompat): record slice-6 evidence`.

**Slice 6 (as landed, COMPLETE — 2 commits)**: items (1)–(3) above are one indivisible code change, not three, for the identical reason Slice 5's own landed commits deviated from its plan: `appendToolResultObject`'s tool-role rendering, `appendToolCallObject`/`appendToolResultObject`'s identifier pass-through, and the correlation R-ART-014 requires are the same lines of code — there is no code path that renders a tool-role message without also deciding how its identifier reaches the wire, so splitting them would mean committing intermediate states that do not compile or do not pass. No `chore: record evidence` commit is made, matching Slices 1–5's own actual practice. Landed as 2 commits: (1) `feat(openaicompat): translate tool calls and tool results, passing identifiers through unchanged` (`message.go`, `message_test.go`, `tool_result_test.go`) · (2) `docs(openaicompat): record the tool-call/tool-result wire shape and the content-omission decision` (`doc.go`, `body.go`'s doc-comment update).

**Slice 7**: (1) `feat(openaicompat): refuse reasoning-bearing requests via the shared unsupported-capability door` · (2) `docs(openaicompat): record reasoning hard-failure and route Layer 2 stripping duty to AI-40` · (3) `chore(openaicompat): record slice-7 evidence`.

**Slice 8 (as planned, for reference)**: (1) `feat(openaicompat): derive the feature inventory mechanically from closed vocabularies and the AST-scanned option surface` · (2) `test(openaicompat): prove the inventory guard bites on an unpolicied vocabulary member and constructor` · (3) `feat(openaicompat): complete the total policy table and its inventory-driven exhaustive walk` · (4) `test(openaicompat): prove the purity pin and uniform refusal category across 26.6 and 26.8.2` · (5) `chore(openaicompat): record slice-8 evidence and final milestone coverage sweep`.

**Slice 8 (as landed, COMPLETE — 2 commits)**: items (1)-(4) above are one indivisible code change, not four: `feature_inventory_test.go` and `policy_walk_test.go` (the inventory scan, the bite proofs, the witness tables, the exhaustive walk, the refuse/drop RED tests and the purity pin) do not compile without `policy.go`'s own `featurePolicy`/`disposition`/`resolveFeaturePolicy` existing, so splitting them into separate commits would mean committing an intermediate state that does not build — the identical reason Slices 5-7's own landed commits deviated from their plans. No `chore: record evidence` commit is made, matching Slices 1-7's own actual practice: `tasks.md` and this milestone's Engram `apply-progress` topic carry the evidence. Landed as 2 commits: (1) `feat(openaicompat): derive the feature inventory and complete the total policy table` (`policy.go`, `feature_inventory_test.go`, `policy_walk_test.go`) · (2) `docs(openaicompat): record the inventory derivation and the total policy` (`doc.go`).

## Evidence Log (to be filled in during apply)

| Scenario/Task | Red recorded | Green recorded | Refactor note / Review confirmation |
|---|---|---|---|
| S-ART-001 *(review)* | — | — | Confirmed: all four claims carry title+locator citations in `doc.go`'s "Wire-shape provenance" section; none is an in-repo restatement. |
| S-ART-002 *(review)* | — | — | Confirmed: `ChatCompletionRequestAssistantMessage.tool_calls` cited request-side specifically, not response-side-only. |
| S-ART-003 *(review)* | — | — | Confirmed: citation establishes multiple consecutive `role:"tool"` messages via the vendor's own parallel-tool-call example, not merely a distinct row. |
| S-ART-004 *(review)* | — | — | Confirmed: no claim was uncited at slice-open; none blocked. |
| S-ART-005 *(review)* | — | — | Confirmed: no contradiction found in any of the four claims. |
| S-ART-006 | `go test -run TestExpectationCases_MatchByteExact` before `Translate` existed: compile error `undefined: openaicompat.Translate` | Same test passes after `translation.go`+`body.go` landed | Byte-exact skeleton for one user text message. |
| S-ART-007 | Covered by same compile-RED above | `TestTranslate_TwoModelsDifferOnlyInModelField` passes | Two requests differing only in model differ only in that field. |
| S-ART-008 | Staged extra `scratch_mutation_field` in `appendBody`; both subtests of `TestExpectationCases_MatchByteExact` failed, printing the mismatched bytes | Reverted; suite green again | Byte-exact comparison is not subset-shaped. |
| S-ART-009 | N/A (positive proof) | 8/8 independent `go test -count=1` process invocations of `TestExpectationCases_MatchByteExact` passed | Recorded as the cross-run determinism proof. |
| S-ART-010 | N/A (pin, not a red-first scenario) | `TestTranslate_SameProcessDoubleTranslate_IsAPin` passes | Explicitly labelled a pin in its doc comment, not the proof. |
| S-ART-011 | Staged intermediate `map[int]ai.Message` before flattening in `appendMessagesField` + temporary 2-message scratch case; 6/40 independent invocations failed (reordered `messages` array) | Reverted both; suite green, re-ran clean | Cross-run proof caught what the in-process pin would mostly miss. |
| S-ART-012 *(review)* | — | — | Confirmed: doc comments name `TestExpectationCases_MatchByteExact` as the proof and `TestTranslate_SameProcessDoubleTranslate_IsAPin` as a pin. |
| S-ART-013 | N/A | `TestCredentialScan_ExpectationSurfaceIsClean` passes | Landed expectation surface scans clean. |
| S-ART-014 | Staged `sk-STAGEDTEST...` in a new `translation_test.go` expectation case; `TestCredentialScan_ExpectationSurfaceIsClean` failed naming `translation_test.go` + "OpenAI-style secret key" | Reverted; suite green | No edit to the scan was needed to catch the new expectation. |
| S-ART-015 | Permanent isolated test `TestCredentialScan_FailsOnASentinelInTestSetup` (t.TempDir(), sentinel in a function body, not a literal expectation) | Passes | Scan's subject is the whole file, not only expectation-shaped literals. |
| S-ART-016 | N/A | `TestCredentialScan_HostsAndModelIdentifiersStayGreen` passes; also implicitly proven by "gpt-4o"/"gpt-4o-mini" throughout `translation_test.go`'s real expectations staying green | Hosts and model identifiers are not flagged. |
| S-ART-017 | Covered by S-ART-014's staged run | Same | Failure names file + sentinel class. |
| S-ART-087 *(review)* | — | — | Confirmed: NFR-ART-E taxonomy table landed in `doc.go`, citing AI-03 §10.4. |
| S-ART-018 | `go test -run TestExpectationCases_MatchByteExact/three_ordered_system_segments` before `system.go` existed: real mismatch (`Translate()` omitted all three system entries) | Passes after `system.go` landed | Three ordered segments render as three distinct role-`system` messages, in caller order, before the conversation message. |
| S-ART-019 | `TestSystemSegment_ReversedOrderTwin_TranslatesDifferently` before `system.go` existed: both twins produced identical bytes (system instruction entirely omitted) | Passes after `system.go` landed | Reversed-order twin now genuinely differs — order is carried, not incidentally equal. |
| S-ART-020 | No natural red: a request with no system instruction already omitted any system entry before this test existed (Slice 1's structure) | `TestSystemSegment_NoSystemInstruction_HasNoPlaceholderEntry` passes | Falsifiability rests on S-ART-018/019's reds proving the renderer exists and is exercised; disclosed per this apply's evidence-honesty requirement, not fabricated as its own red. |
| S-ART-021 | No natural red: `appendMessageObject` already never read `IsCacheBoundary()` before this test existed. Staged mutation (`,"scratch_cache_boundary_mutation":true` in `appendMessageObject`) failed the messages sub-test, printing the mismatched bytes | Reverted; suite green (`git diff` against the prior commit shows `body.go` byte-identical) | Falsifiability is the staged mutation, not a natural red — disclosed per this apply's evidence-honesty requirement. |
| S-ART-022 | N/A | `TestCacheMarker_MarkedTwinAcrossEveryRegion` walks all 3 `ai.CacheRegions()`; messages and system sub-tests both pass non-vacuously | Tools sub-test records `t.Skip` with a named reason (tool.go pending Phase 3) instead of a vacuous pass — see Phase 2's disclosed-scope note. |
| S-ART-023 | Covered by S-ART-021's staged run (messages) and a second staged run against `appendSystemMessageObject` (system): same `,"scratch_cache_boundary_mutation":true` field, staged then reverted, both failed before reverting | Both reverted; suite green | Confirms the twin comparison bites on a rendered marker, in both regions that currently render substantively. |
| S-ART-024 *(review)* | — | — | Confirmed: `doc.go`'s "Cache-boundary markers vanish whole" section cites AI-11.3's R-ACB-008 (`openspec/specs/ai-cache-breakpoints/spec.md`), states automatic vendor caching as the reason, and now reflects system as a completed region. |
| S-ART-035 | Registered expectation case ("tools carry byte-faithful schema, name, description and declaration order") failed before `tool.go` existed: `Translate()` omitted the "tools" field entirely | Passes after `tool.go` + `body.go` wiring landed | Distinctive key order (`type, properties, required, additionalProperties` — not alphabetical) and distinctive whitespace (indentation, irregular spacing, internal newlines) survive verbatim. |
| S-ART-036 | Covered by the same registered case above | Same | `search_flights`/`cancel_booking`/`get_weather` names and descriptions all appear verbatim in the expectation's positions. |
| S-ART-037 | Staged a `json.Unmarshal`+`json.Marshal` round trip in `appendToolObject` in place of the raw splice; the byte-exact case failed, printing the schema alphabetically re-sorted and fully compacted | Reverted; `tool.go` confirmed byte-identical to its pre-mutation state via Read; suite green | Falsifies specifically on re-encoding, not merely on the field going missing — discharges the vacuous-pass trap named in this session's evidence-discipline note. |
| S-ART-038 | N/A (positive proof) | 10/10 independent `go test -count=1` process invocations of `TestExpectationCases_MatchByteExact` passed with the clean (slice-based) implementation | Recorded as the cross-run tool-order determinism proof, exactly mirroring S-ART-009's methodology. |
| S-ART-039 | `TestTool_ReversedOrderTwin_TranslatesDifferently` failed before `tool.go` existed: forward and reversed twins produced identical bytes (both omitted tools) | Passes after `tool.go` landed | Declaration order is genuinely carried, not incidentally equal — mirrors S-ART-019's exact pattern. |
| S-ART-040 | Staged an intermediate `map[string]ai.Tool` in `appendToolsField`, ranging over the map to decide wire order; 5/30 independent `go test -count=1` invocations failed, printing a reordered tools array (`cancel_booking, get_weather, search_flights` instead of the declared order) | Reverted; suite green; 10/10 subsequent clean-implementation invocations passed | Silent leak: no error, no wrong-looking JSON, just a different array order — exactly R-ART-011's stated failure mode. |
| S-ART-041 *(review)* | — | — | Confirmed: `tool_test.go`'s file-header comment names `TestExpectationCases_MatchByteExact`, run as an independent process and repeated across invocations, as the fresh-process proof, and states the silent-cost failure mode: a valid body, no error, no test failure — only a broken vendor cache prefix and an input bill roughly ten times larger than it should be. |
| (pin, unnumbered) | N/A — pin, not red-required | `TestToolSet_DuplicateOrInvalidNamesNeverReachTranslate` passes, green from birth | `ai.NewToolSet` (AI-08.2) already rejects a duplicate name (`ai.ErrDuplicate`) and a never-constructed tool (`ai.ErrEmpty`) before a `ToolSet` value can exist; `tool.go` needs no duplicate/validation logic of its own. |
| S-ART-021/022/023 (tools region, completing Phase 2) | Staged `,"scratch_cache_boundary_mutation":true` gated on `tool.IsCacheBoundary()` in `appendToolObject`; the tools sub-test failed, printing the mismatched bytes | Reverted; `cache_marker_test.go`'s tools sub-case now PASSES (was `t.Skip`) | Completes R-ART-006 across all 3 `ai.CacheRegions()` — messages and system (Phase 2), tools (this slice). Cross-slice hand-off verified by running the suite, not assumed: the walk's loop/switch structure needed no edit, but the tools case's body did need its `t.Skip` replaced. |
| S-ART-057 | 6 registered cases (max_tokens, temperature, top_p, stop, tool_choice=none, tool_choice=specific) failed on `TestExpectationCases_MatchByteExact` before `option.go` existed: `Translate()` omitted every one of these fields | Passes after `option.go` + `body.go` splice landed | One request per settable generation option, each isolated, field present with the caller's value. |
| S-ART-058 | `TestOption_ZeroValueVersusUnset_TranslatesDifferently` failed before `option.go` existed: temperature=0 and temperature-unset both produced the same body (option never rendered either way) | Passes after `option.go` landed | Temperature is the only one of these options with a legally constructible zero value (request.go's own `boundsRule` rejects `max_tokens<=0`, `top_p<=0`, and an empty `stopSequences`); zero and unset now render differently. |
| S-ART-059/060 | N/A (positive, total proof) | `TestExpectationCases_AllCarryUsageOptIn` passes over all 10 registered cases, green on first write (evidence class 4: bytes emitted since slice 1) | Total walk over the complete expectation set, not sampled on one case; this node owns the assertion, not the bytes. |
| S-ART-061 | Staged removal of `,"stream_options":{"include_usage":true}` from `appendStreamFields` (`body.go`); all 10 subtests of `TestExpectationCases_AllCarryUsageOptIn` failed by name | Reverted; `appendStreamFields` confirmed byte-identical to its pre-mutation state via Read; suite green | Falsifies on the positive, named assertion — not inferred from an unrelated absence-of-error. |
| S-ART-062 | No natural red: nothing rendered `max_tokens` at all before `option.go` existed, so absence was vacuously true pre-implementation too | `TestOption_MaxOutputTokensOmitted_FieldExplicitlyAbsent` passes | Falsifiability rests on S-ART-057's registered "present with caller value" case's genuine red for the same field — disclosed per this apply's evidence-honesty requirement, mirroring S-ART-020's precedent. |
| S-ART-063 | Covered by S-ART-057's registered "max output tokens option present with caller value" case | Same | Field present and carrying the caller's value when set. |
| S-ART-064 *(review)* | — | — | Confirmed: `doc.go`'s "Output-token limit: optional for this vendor, mandatory-default branch dead" section cites doc 0002's amended AI-26.7 charter (line 1569, AI-24, 2026-08-03) and states the branch is a deliberate no-op for this vendor. |
| S-ART-065 | Compile error `undefined: openaicompat.Namespace` before `option.go` defined the constant (registered case + `TestExtension_OwnNamespaceMergesDiffersFromExtensionFreeTwin`) | Passes after `Namespace`+`appendExtensionFields` landed | Own-namespace extension merges its bytes into the wire body, differing from its extension-free twin, matching the registered expectation exactly. Namespace value ("openaicompat") assigned by coordinator ruling — see Phase 4's own note; not read from AI-25 (nothing to read) and not invented by this slice. |
| S-ART-066 | Same compile error (both foreign-namespace tests live in the same new file) | `TestExtension_ForeignNamespaceIgnoredWhole` passes: foreign-namespace body byte-identical to the extension-free twin, **and both anchored against the hard-coded `foreignExtensionExpectation` literal directly**, not merely against each other | Anchoring defeats both vacuous-pass shapes named by the coordinator: a fixture that can't distinguish implemented from not, and a test comparing an implementation only against itself. |
| S-ART-067 | Covered by the same compile-error red | `TestExtension_SeveralForeignNamespacesAtOnceStillIgnoredWhole` passes: three simultaneous foreign namespaces (`anthropic`, `gemini`, `cohere`) still match the same hard-coded anchor | Per-namespace and total, not first-match. |
| S-ART-068 | Staged `appendExtensionFields` to range over `req.ProviderExtensions()` (every namespace) instead of looking up only `Namespace`; both foreign-namespace tests failed, printing the leaked foreign fields (`anthropic_only_field`, then `anthropic_field`/`gemini_field`/`cohere_field`) | Reverted; `option.go` confirmed byte-identical to its pre-mutation state via `git diff`; suite green | Falsifies specifically on "merged rather than ignored", the exact failure mode the coordinator warned this twin test was exposed to. |
| S-ART-042 | Registered "one tool result" case failed before `message.go`'s AI-26.5 growth existed: `appendMessageContent`'s single-part branch panicked (`part.Text()` not ok for `PartKindToolCall`) | Passes after `appendToolResultMessages`/`appendToolResultObject` landed | Exactly one wire tool-role message for one tool result. |
| S-ART-043 | Registered "several tool results" case failed before landing: `appendContentPartObject`'s default case panicked (array-render branch, 2 tool calls) | Passes after landing | Two distinct tool-role messages, in caller order, for two results. |
| S-ART-044 | Registered "failed tool result" case failed identically pre-landing (nothing distinguished failed from succeeded either) | Passes after landing; `TestToolResult_FailedAndSucceededRenderIdentically` cross-checks Failed() changes nothing but identity and content | No distinct wire field for failure; content renders verbatim either way. |
| S-ART-045 | Folded into all 6 registered `tool_result_test.go` cases (each uses a distinctive identifier) — all failed pre-landing | All pass after landing | Every wire identifier equals its neutral identifier byte for byte, proven across 6 independent fixtures, not one. |
| S-ART-046 | Registered "unusual but valid identifier shape" case (`3F2A9B-Weird_ID.42`) failed pre-landing | Passes after landing | Mixed case, hyphen, underscore and period survive unnormalized — `ai.ToolCall`'s own `validate` carries no shape rule on `id` beyond non-empty. |
| S-ART-047 | Staged `appendJSONString(buf, "scratch_minted_id")` in place of `call.ID()`/`result.CallID()`; all 6 registered cases + `TestToolResult_FailedAndSucceededRenderIdentically` failed by name | Reverted; `message.go` confirmed byte-identical to its pre-mutation, committed state via `git diff`; suite green | Falsifies specifically on identifier substitution, not merely on absence. |
| S-ART-048 | Registered "interleaved multi-call" case (3 calls, results answering in a derangement of declaration order, non-sortable identifiers) failed pre-landing | Passes after landing | Each wire result names its own call's identifier even when calls and results are declared out of step and the pairing is not reproducible by sorting or position. |
| S-ART-049 | Staged a declaration-order identifier scan (`appendMessagesField`, body.go) consumed positionally by a scratch counter in `appendToolResultObject`, replacing `result.CallID()`; only the interleaved (S-ART-048) and reordered (S-ART-050) cases failed by name — the 4 non-interleaved cases stayed positionally-coincidentally green | Reverted; `message.go` and `body.go` confirmed byte-identical to their pre-mutation, committed state via `git diff`; suite green | Proves interleaving is what makes the position-based substitution detectable — a non-interleaved fixture provably cannot catch this bug class. |
| S-ART-050 | Registered "same tool name, different identifiers, reversed result order" case failed pre-landing | Passes after landing; independently confirmed by S-ART-049's own mutation (this case also failed under it) | Two calls sharing one tool name remain distinguishable by identifier alone, never by name or position. |
| S-ART-051 | 9 registered `refusalCase`s (3 reasoning states, `TestRefusalCases_FailWithUnsupportedCapability`) failed pre-landing: genuine unrecovered panic at `appendContentPartObject`'s pre-existing default case | Passes after `policy.go` + `translation.go` wiring landed | Every reasoning state (text, redacted, token-only) refuses with `PreStreamFailure`+`ErrUnsupportedCapability`; no wire body produced (`got != nil` asserted explicitly). |
| S-ART-052 | Same 9 registered cases, 6 of which vary position specifically (3 message-position + 3 intra-message-part-position) — all failed pre-landing, same panic class | All pass after landing | Position — first/mid/last message, first/mid/last part within a message — never changes the outcome; `refuseReasoning`'s own full-request, full-message walk (policy.go) is what makes this true, not inspection. |
| S-ART-053 | `TestReasoningRefusal_NamesReasoningContentAsTheUnsupportedFeature` failed pre-landing (same panic, no error to unwrap) | Passes after landing | `errors.Unwrap(err).Error()` names "reasoning content" verbatim — a caller can act without reading this package's source. |
| S-ART-054 | N/A — pin, true before this phase's own code existed (the set was already empty) | `TestPolicy_NoNewSentinelsExported` passes; falsifiability proven by a staged scratch exported sentinel in `policy.go`, which failed the guard naming the file and identifier, then reverted | This package's own non-test sources export zero `Err*`-shaped sentinels, before and after this phase — every refusal reaches a caller only through the already-exported `ai.ErrUnsupportedCapability`. |
| S-ART-055 | Staged `refuseReasoning` returning `nil` unconditionally (policy.go) AND `appendMessageContent`/`appendContentPartObject` (message.go) rendering a reasoning part as empty/nothing instead of panicking — Translate genuinely SUCCEEDED, silently dropping the part; all 9 `refusalCase`s + the coverage test's `reasoning` subtest + the feature-naming test failed by name with "no error, want a refusal" | Reverted both files; confirmed byte-identical to the pre-mutation, uncommitted state via `git diff`; suite green (498/498, 0 FAIL) | Falsifies specifically on "silently dropped and succeeded" — the exact forbidden alternative (a) this milestone's own design decision named — not merely on "some panic occurred for an unrelated reason". |
| S-ART-056 *(review)* | — | — | Confirmed: `doc.go`'s new "Reasoning replay refuses; Layer 2 must strip first" section states plainly that a reasoning-bearing transcript fails hard here, names consumer-side stripping (Layer 2) as the remedy, and records the duty as routed to AI-40 ("Publish the Layer 2 readiness contract", doc 0002) rather than as an unfixed defect. |
| S-ART-069 | `TestFeatureInventory_MatchesProductionPolicyExactly`/`TestFeatureInventory_NonTranslateFeaturesHaveExactlyOneWitness` failed to compile (`undefined: featurePolicy` etc.) before `policy.go` grew | Passes after `featurePolicy`/`disposition`/`resolveFeaturePolicy` landed | 28-feature inventory (17 vocabulary members + 11 constructor-derived) cross-checked against the production table and the witness table, both directions. |
| S-ART-070 | `TestFeatureInventory_UnaccountedVocabularyMemberFailsNamingIt`: durable synthetic-candidate proof, same compile-error class pre-GREEN | Passes after GREEN landed; permanent | Durable over transient — see this slice's own disclosed deviation, above. |
| S-ART-071 | `TestFeatureInventory_UnaccountedConstructorFailsNamingIt`: durable `t.TempDir()`-based proof, same compile-error class pre-GREEN | Passes after GREEN landed; permanent | Exercises the real AST scanner end to end against synthetic source, never a real `ai/*.go` file. |
| S-ART-072 | `TestFeatureInventory_ScanReflectsTheTargetDirectoryNotAFixedList` (3 sub-cases), same compile-error class pre-GREEN | Passes after GREEN landed | Zero/one/three-constructor fixtures each report exactly that count — ground truth is the target directory's own content. |
| S-ART-073 *(review)* | — | — | Confirmed: `doc.go`'s "Feature inventory: hybrid derivation, reflection rejected" section names both derivation mechanisms and states why `reflect` cannot enumerate top-level declarations at all. |
| S-ART-074 | `TestPolicyWalk_EveryInventoriedFeatureResolvesToExactlyOneDisposition`, same compile-error class pre-GREEN | Passes after GREEN landed | Every one of 28 features resolves to exactly one recognised disposition, carrying a non-empty reason. |
| S-ART-075 | `TestPolicyWalk_EveryRefuseFeatureFailsNamingIt` (4 sub-tests), same compile-error class pre-GREEN | Passes after GREEN landed | Every `dispositionRefuse` row's witness fails with `errors.Is(err, ai.ErrUnsupportedCapability)`, no wire body. |
| S-ART-076 | `TestPolicyWalk_EveryDropFeatureTranslatesByteIdenticalToItsTwin` (4 sub-tests), same compile-error class pre-GREEN | Passes after GREEN landed | Every `dispositionDrop` row's marked/unmarked twin translates byte-identically; reason non-empty. |
| S-ART-077 | Staged `refuseReasoning` returning `nil` unconditionally (`policy.go`) AND `appendMessageContent`/`appendContentPartObject` (`message.go`) rendering a reasoning part as empty/nothing instead of panicking | Reverted both files; `message.go` confirmed byte-identical via `git diff --stat` (zero diff); suite green | 19 sub-tests/tests across Phase 7's and Phase 8's own suites failed by name with "no error, want a refusal" — the walk-level tests bite on the identical silent-drop scenario Phase 7's own S-ART-055 proved. |
| S-ART-078 | `TestPolicyWalk_UnaccountedFeatureFailsTheWalk`: durable synthetic-candidate proof over the real `fullInventory`, same compile-error class pre-GREEN | Passes after GREEN landed; permanent | Proves the WALK (`unresolvedPolicyFeatures`), not only the lower-level comparison (S-ART-070/071), is inventory-driven. |
| S-ART-079 | `TestPolicyWalk_EveryRefusalUsesTheSameCategoryUniformly`, same compile-error class pre-GREEN; `TestPolicy_NoNewSentinelsExported` re-run (pin, not re-written) | Passes after GREEN landed | Every refuse-disposition feature reaches the caller through the identical `ai.ErrUnsupportedCapability`; zero new sentinels after `policy.go`'s growth. |
| S-ART-080 | N/A | `TestLayer1_ImportsOnlyStdlibAndItsOwnPackages_DenyByDefault`/`TestLayer1_ModuleHasNoDependencies_ZeroRequires` pass | `go.mod` zero requires, no `go.sum`, confirmed after this slice's own growth. |
| S-ART-081 *(review)* | — | — | Confirmed: `git status --short backend/agent/src/ai/` (excluding `openaicompat/`) reports nothing for the whole session. |
| S-ART-082 | N/A | Fresh `go test -race -v -count=1 ./...`: exit 0, 3 packages `ok`, 1805 `--- PASS`, 0 `--- FAIL` | Every predecessor test (AI-00…AI-25) passes unchanged alongside this slice's own additions. |
| S-ART-083 | `TestTranslate_LeavesTheRequestEqualToAnIndependentlyBuiltCopy`, same compile-error class pre-GREEN | Passes after GREEN landed | A request exercising nearly every render path equals an independently built copy, via `ai.Request.Equal`, both before and after `Translate` runs. |
| S-ART-084 *(review)* | — | — | Confirmed: none of AI-26's own 12 test files reference `httptest`, `net.Listen` or import `net/http`. |
| S-ART-085 | N/A | `grep -rn "-update\|golden"` across every non-test `.go` source in `openaicompat/` returns nothing | No golden-file tree or `-update` flag anywhere in the package. |
| S-ART-086 *(review)* | — | — | Confirmed: `translation_test.go`'s own file header states the inline-literal rationale, landed Slice 1, unmodified. |
| S-ART-087 *(review)* | — | — | Re-confirmed (see also the earlier S-ART-087 row, Phase 1): `doc.go`'s NFR-ART-E taxonomy table still carries the AI-03 §10.4 citation, unmodified by this slice's own additions. |
| S-ART-088 *(review)* | — | — | Confirmed: this file's own Evidence Log (this table) and the TDD Cycle Evidence / Work Unit Evidence tables above carry a recorded red/green/refactor note or review confirmation for every scenario across all 9 phases. |
| S-ART-089 | N/A | 5/5 independent `go test -race -count=1 ./...` invocations: exit 0, 0 `--- FAIL`, 0 `DATA RACE` each | Cross-run determinism confirmed for the whole milestone's test set, mirroring S-ART-009's own methodology. |

**Deviation, disclosed**: the credential scan's scope (external-package `openaicompat_test` files only) is a deliberate, documented narrowing from a literal "every `*_test.go`" read of the design text — see tasks.md Phase 1 closure note and `credential_scan_test.go`'s file comment. Verified via `TestCredentialScan_IgnoresInternalTestPackageFiles`.

**Correction to Phase 2's own closure note, recorded for the record**: Phase 2's deviation note asserted the tools cache-marker sub-test "starts proving something real once slice 3 lands, with no edit needed to this file." That was accurate about the walk's outer loop/switch structure (true: it needed no edit) but imprecise about the `case ai.CacheRegionTools` body itself, which did need its `t.Skip` replaced with a real twin comparison — this session verified the claim by running the suite rather than trusting the prior note, per this milestone's own evidence-discipline standard, and corrected the file's in-place comments accordingly rather than silently repeating the imprecise claim forward.

---

## Phase 10 — Post-verify citation-integrity fix (`feat/ai-26-9-citation-integrity`, off `feat/ai-26-8-unsupported-policy`) — COMPLETE (2/2)

AI-26's own verify report (`verify-report.md`, PASS WITH WARNINGS) recorded two non-blocking WARNINGs before archive. Both closed this slice, comment/documentation-only — no test behaviour changed.

- [x] 10.1 **WARNING 1 — dangling citation pointer**: `doc.go:497` and `message.go:222` both attributed a wire-shape claim to "Claim 1.0.5", a claim that does not exist (the provenance section enumerates only claims 1–4). The pointer was load-bearing for two decisions: (a) omitting "content" rather than emitting `"content":null` for an assistant message carrying only tool calls, and (b) rendering a single content part as a bare string rather than a one-element array. **Fix**: both sites rewritten as an explicitly labelled *recorded inference, not a citation* — states plainly that none of claims 1–4 settles content's nullability or shape, that the omit-when-absent behaviour instead follows this package's own existing presence-flag convention, that the choice is byte-proven (`TestExpectationCases_MatchByteExact` would fail the instant `"content":null` was emitted, since no registered expectation contains that substring), and that it remains a candidate for a future citation if the vendor spec later settles the question. No fifth claim was invented. **Sibling sweep**: grepped the whole package (`grep -rniE 'claim[[:space:]]+[0-9]'` and `grep -rn '[0-9]\.[0-9]\.[0-9]'`, non-test and test sources together) for every claim-numbered reference. Found one further sibling of the same drift: `tool_result_test.go:117`, a phantom `(1.0.2)` parenthetical immediately after correctly-named prose ("claim 2's request-side confirmation (1.0.2)") — not load-bearing on its own (the real claim was already named correctly in the same sentence), but the identical undefined-numeral pattern, so removed for consistency rather than left as a second, milder instance of the exact defect this slice exists to close. Every other one of the 20 remaining `claim N` references in the package (`body.go`, `doc.go`, `message.go`, `system.go`, `tool.go`, `translation.go`, `reasoning_refusal_test.go`) resolves to a real claim 1–4. Post-fix re-sweep: zero dangling or phantom numerals remain anywhere in the package.
- [x] 10.2 **WARNING 2 — missing reopen trigger**: the namespace coordinator ruling (`openaicompat.Namespace = "openaicompat"`) recorded its rationale in three bullets but named no condition under which it would be revisited, unlike the system-role and reasoning-refusal rulings, which both carry one. **Fix**: added `doc.go`'s new "Reserved-namespace reopen trigger — not a hedge" section, immediately following the ruling. Two concrete future conditions, not a hedge: (1) a vendor sharing this dialect defines its own reserved extension namespace colliding with `"openaicompat"`; (2) a second OpenAI-compatible adapter is added to this module such that the string is no longer unambiguous. Cross-references the existing "exported so a mistyped namespace fails safe as foreign-namespace-ignored-whole" reasoning (already recorded, not duplicated) and extends it: a second adapter reusing the same string is the identical failure mode at the package-naming level instead of the call-site level.

**Evidence**: comment/documentation-only slice; no `_test.go` behavioural change. `gofmt -l doc.go message.go tool_result_test.go` clean. `go test -race -v -count=1 ./...` re-run twice, both exit 0, 1805 `--- PASS`, 0 `--- FAIL`, 0 `DATA RACE` (identical counts to the verified baseline — expected, since no test logic changed). `make lint` clean. `go.mod` still zero `require`. Both AI-00 guards and AI-25's ambient-authority guard re-confirmed green. No `t.Skip`, `TODO` or `FIXME` introduced. Measured diff vs `feat/ai-26-8-unsupported-policy`: 3 files changed (`doc.go`, `message.go`, `tool_result_test.go`), comment-only.

**Rollback boundary**: revert `doc.go` (2 sections: the content-omission rewrite and the new reopen-trigger section), `message.go` (1 paragraph), `tool_result_test.go` (1 parenthetical) — no other file touched, no test added or removed, no production logic changed.
