# Delta for `agent-loop-skeleton` — AG-19 requests NO substrate release, and its guard-filter widening is enumerated by name

> **Change**: `cachicamas-agent-delegation-readiness` · **AG-19** (Layer 2, Wave 5), `0003:1793-1862`
> **Modifies**: `agent-loop-skeleton` ([`../../../../specs/agent-loop-skeleton/spec.md`](../../../../specs/agent-loop-skeleton/spec.md)) — **by APPENDING one requirement**, `R-LSK-007`, alongside `R-LSK-004` (`spec.md:101-148`). `R-LSK-004` itself is **byte-unchanged**.
> **The proposal listed this delta as conditional on `doc.go` being edited. `sdd-design` DECLINED that edit (AD-7 / design "Open Questions"), so the release half does not fire — but the delta still ships, and for a different reason the proposal did not name.** `R-LSK-004`'s second discipline — the substrate guards' allowlists widening by **exact filename suffix only**, byte-in-sync across both filters, one entry per file the milestone introduces — binds **every** milestone that adds a file under `backend/agent/src/agent/`, release or no release. AG-19 adds `delegation_seam.go` and new test files. Without a recorded widening the substrate guards fail; with an unrecorded one, they widen by accident. That is an obligation, not a back-annotation.
> **Why an ADDED requirement rather than a MODIFIED `R-LSK-004`.** `R-LSK-004` is a ~48-line requirement carrying six milestones' release scopes and seven widening paragraphs. A `MODIFIED` block must reproduce it **in full** or the archive step silently drops whatever the block omits — a failure this repository has recorded. Appending a milestone-scoped requirement loses nothing, keeps `R-LSK-004` byte-unchanged, satisfies the append-only ID rule, and puts AG-19's conclusion where a reader greps for it. `R-LSK-007` is the next free requirement identifier (`R-LSK-001`…`R-LSK-006` are allocated); `S-LSK-031` is the next free scenario identifier (`S-LSK-001`…`S-LSK-030` are allocated, verified in this phase).
> **Ownership**: the seam is owned by [`../agent-delegation-readiness/spec.md`](../agent-delegation-readiness/spec.md). This delta owns only the substrate conclusion.

## Not modified, and why

| Not modified | Why |
|---|---|
| `R-LSK-004`'s forbidden list and all six recorded releases | **Byte-unchanged.** AG-19 modifies **`scheduler.go` only** and creates one production file plus test files; `scheduler.go` is **not** a member of the forbidden list — that list names `tool_event.go`, and `scheduler.go` has been legitimately modified since AG-13 |
| `doc.go` and `doc_contract_guard_test.go` | **Byte-unchanged, and the decision is recorded rather than incidental.** `sdd-design` declined the invariant-2 narrative edit after verifying that the sentence at `doc.go:45-50` does **not** go false when AG-19 ships — it already says AG-04 co-closes *with AG-19.1*. AG-18's release is **not** extended |
| `R-LSK-001`'s continuation seam and `S-LSK-001`'s length-equality sequence | **Untouched.** `S-LSK-001` asserts a direct one-turn `Turn` call with no harness and no tool publishing; AG-19 cannot reach it |
| `R-LSK-006` — one cycle per `Turn` invocation | **Confirmed.** A hosted child run drives its own `Turn` calls on its own lane; the parent's per-turn schedule-invocation count is unchanged |
| `R-LSK-002`'s statelessness and its three enumerated pinned tests | **Untouched.** All three construct a zero-value `TurnOptions` and no tool that publishes |

## ADDED Requirements

### R-LSK-007 — AG-19 requests NO substrate release, and its guard-filter widening is enumerated by name

**AG-19 requests NO release from `R-LSK-004`, and that conclusion is recorded rather than inferred — the AG-15 and AG-17 shape, not the AG-16 or AG-18 shape.** AG-19 modifies **`scheduler.go` only** and creates one production file (`delegation_seam.go`) in `src/agent` plus its new test files in `package agent_test`. **`scheduler.go` is not a member of `R-LSK-004`'s forbidden list**, and neither is any file AG-19 creates. Five consequences MUST be stated rather than left to be inferred:

1. **No `EventKind`, so no event file opens.** AG-19 registers **none** — AG-06.3 minted `subagent_started` and `subagent_ended`, and AG-19 is their **first caller**, closing the forecast the AG-16 and AG-18 annotations both carried. `event.go`, `event_descriptor.go`, `event_registry_test.go`, `delegation_events.go`, `permission_events.go`, `cost_events.go` and `compaction_events.go` MUST all be byte-unchanged, and the every-kind-constructible guard MUST pass at its committed kind count.
2. **No validator edit, because the admissibility rule is derived FROM the validator's own registry.** `stream_check.go` MUST be byte-unchanged. AG-19's rule refuses exactly what `CheckStream` would reject — bracket roles, at-most-once cardinality, terminals — plus one requirement-level carve-out (`cost_turn`) and one totality gate (registry membership). The fix is emitter-side by construction, never validator-side (`R-RUN-003`).
3. **No new package-wide guarantee, so the contract table stays closed.** AG-19 declares no `L2C` row: it adds no upward surface beyond the existing stream (`L2C-03`) and no exported `History` route (`L2C-07`). `doc.go` and `doc_contract_guard_test.go` MUST be byte-unchanged and `expectedLayer2ContractRows` MUST keep exactly the row set it carries at the merge base. **AG-15's failover seam and AG-17's strategy seam are the decisive precedents**: structurally identical — a caller-invisible seam with an inert default — and neither added a row.
4. **No cost, history or Layer 1 file opens.** `cost_events.go` (released for AG-16 only), `cost_events_test.go`, `cost_usage.go`, `history.go`, `sequence.go`, `run_events.go`, `turn_events.go`, `failure.go`, `reconstruction_test.go`, `go.mod` and `go.sum` MUST be byte-unchanged, as MUST every file under `backend/agent/src/ai/`. AG-11's, AG-14's, AG-16's and AG-18's releases are **not** extended to AG-19: every file named forbidden in `R-LSK-004` is forbidden again here.
5. **`agenttest` is byte-unchanged by default.** AG-19's fixtures — the delegating tool, the derived scope, the two-stream reconstruction helper — are test-local in `package agent_test`, on the AG-15/AG-16 test-local-wrapper precedent. Widening `agenttest` requires a recorded reason at apply time; it is not pre-granted here.

**The same widening discipline binds AG-19, enumerated by name rather than by count**, because a count goes silently false the moment a later milestone appends. `harness.go`, `harness_test.go`, `loop.go`, `scheduler.go`, `tool.go`, `doc.go` and `doc_contract_guard_test.go` are **already** named in both substrate filters (`filterOutLoopFiles`, `loop_test.go:837-1049` post-widening; `filterOutLoopHookFiles`, `loop_hook_test.go:907-1103` post-widening) and need no addition — **so AG-19 adds no pre-existing filename, because it releases none.** Both filters SHALL widen by one **exact filename suffix** for `/delegation_seam.go` and one per test file AG-19 introduces under `backend/agent/src/agent/` **that the filters' own population rule covers**, as determined by **reading the two filters' predicates at apply time** rather than assumed here. The concrete filenames are fixed by this change's `tasks.md` and MUST be identical in both filters.

No wildcard, no prefix match, no directory-level relaxation; the two filters SHALL remain **byte-in-sync** with each other; and any file AG-19 adds that is not so named MUST still fail the guards. **`cost_events.go`, `cost_events_test.go`, `stream_check_test.go` and `reconstruction_test.go` are each absent from AG-19's additions**, and each absence is deliberate: AG-19 MUST NOT edit any of them, and a filter entry would remove the guard that says so. Neither is any file under `backend/agent/src/ai/` or `backend/agent/src/agenttest/`.

#### Scenarios

- **S-LSK-031** — **AG-19: no release taken, and the widening is exact.** Given the merge base of the AG-19 branch with `origin/main`, when `git diff` is taken over `backend/agent/`, then every file named forbidden by `R-LSK-004` is byte-unchanged, `doc.go` and `doc_contract_guard_test.go` included; the diff under `backend/agent/src/ai/` is empty and the `go.mod`/`go.sum` diff is empty; the every-kind-constructible guard passes at its committed kind count with AG-19 registering none; and when both substrate filters are read, then each names `/delegation_seam.go` and each of AG-19's covered new test files by **exact filename suffix**, the two filters carry an **identical** entry set, neither contains a wildcard, a prefix match or a directory pattern, and neither names `cost_events.go`, `cost_events_test.go`, `stream_check_test.go` or `reconstruction_test.go`.
