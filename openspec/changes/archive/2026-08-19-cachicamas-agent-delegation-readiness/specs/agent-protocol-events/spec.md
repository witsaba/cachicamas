# Delta for `agent-protocol-events` — the delegation family's live-emission half CLOSES, after thirteen milestones constructible-but-unemitted

> **Change**: `cachicamas-agent-delegation-readiness` · **AG-19** (Layer 2, Wave 5), `0003:1793-1862`
> **Modifies**: `agent-protocol-events` ([`../../../../specs/agent-protocol-events/spec.md`](../../../../specs/agent-protocol-events/spec.md)) — the two non-requirement rows at `spec.md:154` ("Live emission") and `spec.md:157` ("Subagent harness mechanics").
> **BACK-ANNOTATION ONLY, and scenario-free.** No requirement is amended, no scenario is added or removed, and no count moves. **`R-APE-006` is UNAMENDED**: AG-06.3 registered `subagent_started` and `subagent_ended` with the descriptor shape their first caller needed, and AG-19 needed **no** amendment to that shape — the sixth consecutive demonstration that the registry is extensible without editing it.
> **Why this delta.** The "Live emission" row at `spec.md:154` names AG-19 as the delegation family's emitter and states, verbatim, *"The delegation family remains unemitted and remains AG-19's"*. AG-19 is the emission. Leaving that sentence unannotated after the merge leaves a reader unable to tell whether the family ever shipped an emitter — this repository's un-back-annotated-merge staleness shape, against which **no Go test defends**. `sdd-verify` MUST open each line cited here.
> **Ownership**: the seam and its admissibility rule are owned by [`../agent-delegation-readiness/spec.md`](../agent-delegation-readiness/spec.md) (`R-DEL-001`, `R-DEL-002`, `R-DEL-004`). This delta owns only the protocol's audit statement.

## Not modified, and why

| Not modified | Why |
|---|---|
| `R-APE-006` and the delegation family's registry rows | **Confirmed rather than amended.** `subagent_started` and `subagent_ended` register turn placement, no bracket role, non-terminal, cardinality *any* (`event.go:340-347`) — exactly the shape a first caller needs. `delegation_events.go` is **byte-unchanged** |
| The "No edit to `event_descriptor.go`, `stream_check.go`, `failure.go`, `sequence.go`" row | **Still true at AG-19**, which edits none of the four. Its admissibility rule is derived **from** the registry precisely so the validator needs no amendment |
| The token-only pin and the money prohibition | **Untouched.** AG-19 adds no payload field of any kind |
| Every count in this capability | **Unchanged.** AG-19 registers no `EventKind`, so the every-kind-constructible guard passes at its committed count |

## MODIFIED Explicit non-requirements

The list is reproduced only where AG-19 touches it; every other row is unchanged and none is removed.

- **Live emission** — **AG-10, AG-16, AG-18, AG-19**. AG-06 ships events constructible; the mechanisms that emit them land later. The cost family's half closed at AG-16; the permission family's at AG-10; the compaction family's at AG-18. **The delegation family's half is CLOSED by AG-19**, its first production emitter after thirteen milestones constructible-but-unemitted. Four facts are recorded here because a later reader will ask them of the protocol rather than of delegation:
  1. **The emission site is a test-only tool reaching through a production seam**, not a production subagent tool. The seam publishes *events* and names no subagent concept; every subagent concept lives in `package agent_test`, which production code cannot import. **No subagent tool ships in v1**, and that is enforced by the compiler rather than by convention (`0003:1794`, `0003:1803`).
  2. **`delegation_events.go` is byte-unchanged**, as are `event.go`, `event_descriptor.go` and `event_registry_test.go`. AG-06.3's shape needed no amendment to accept its first caller — in particular the parent identifier was **not** widened to other kinds, because `R-AEV-003`'s distinguishable no-parent state is exactly what a walkable tree relies on.
  3. **`subagent_started` and `subagent_ended` are PARENT-LANE events, one bracket per hosted child run**, opened before the child runs and closed only after the child's own sink closed — so on the parent's lane the ended event's index is strictly less than the parent's run-close index, which structurally proves the child finished first.
  4. **The family is emitted alongside up to 19 MIRRORED kinds**, admitted by a rule derived from the registry itself: refuse anything with a bracket role, anything `CardinalityAtMostOne`, anything terminal, anything with no registry row, and `cost_turn`. Six kinds are refused and nineteen admitted; the `cost_turn` refusal is the **only** requirement-level one (`R-CST-004`) rather than a stream-legality one, and that asymmetry must be restated wherever the rule is (`R-DEL-002`).
- **Subagent harness mechanics** — **AG-19**. AG-06.3 ships the events; the harness is AG-19's. **CLOSED by AG-19, with the mechanics/tool split stated, because the row's word "mechanics" invites the wrong reading.** AG-19 ships the **structural properties** a subagent tool would stand on — a nested run whose events are attributable by walking parents in one hop, siblings interleaving with no cross-talk under `-race`, nested cancellation inherited through the existing tree and completing leaf-first, cost as a consumer-side reconstruction with a production refusal guarding it, and a permission scope derived by composition. It ships **no** subagent tool, **no** subagent configuration and **no** depth limit; those stay post-v1 on this now-proven substrate. *(Still true and unchanged: parent-scoped cost aggregation is not a Layer-2 fold — AG-19 answers that with a permanent no, and the answer lives in `agent-cost-events`' `R-CST-007` as amended by this change.)*
