# Apply progress: AG-17 — Add the context strategy seam and token accounting (`cachicamas-agent-context-strategy`)

> Worktree `cachicamas-worktrees/ag17`, branch `feat/agent-layer2-wave4-ag17`, base `origin/main@b0de5bf6`. Strict TDD active (`cd backend/agent && make test`, evidence with `-count=1`). Single PR, `size:exception` pre-accepted.

## Status: ALL PHASES (0-12) COMPLETE. Change archived in this same PR.

## Task 0 — Foundation: RED baseline & substrate verification

- [x] 0.1 Grepped `backend/` for `ContextStrategy`, `ContextBudget`, `TokenAccounting`, `TokenSource`, `ContextPrompt`, `ContextVerdict` — **zero** hits confirmed before any AG-17 file was created.
- [x] 0.2 `doc_contract_guard_test.go`'s `expectedLayer2ContractRows` read: exactly **eight** rows, `L2C-01`…`L2C-08`, matching `doc.go`'s own contract paragraph count.
- [x] 0.3 `harness.go` re-opened around `:498`/`:530` (pre-edit): content matched design.md DD2's quoted before/after exactly, by content not by line number.
- [x] 0.4 `filterOutLoopFiles`/`filterOutLoopHookFiles` re-grepped at their true tails (`loop_test.go:837-982`, `loop_hook_test.go:907-1036` pre-edit) — recorded, not trusted from any citation.
- [x] 0.5 `agenttest.Provider.Stream` confirmed on the **pointer** receiver (`fake_provider.go:74`); `stubProviderWithTokenCounter`'s embedding shape (`agenttest_test` package, `provider_test.go:128-142`) read as the fixture template.
- [x] 0.6 `ai.TokenCounter` (`ai/provider.go:102-134`), `ai.TokenCount.Count()` (`ai/usage.go:44-65`), `buildLoopRequest` (`loop.go:711-726`) signatures confirmed to match design.md's citations exactly.

## Phase 1 — AG-17.2 foundation: types (commit `d5ecc37e`)

- [x] 1.1 RED: `context_strategy_test.go` (external, `package agent_test`) — `TestContextVerdict_HasNoFields` (S-CTX-005), `TestContextBudget_AbsentVsStatedZero` (S-CTX-009), `TestContextBudget_NegativeIsAbsentTotal` (S-CTX-010), `TestTokenAccounting_UnreadableWithoutSource` (S-CTX-014); `token_accounting_test.go` (internal, `package agent`) — `TestTokenSource_StringRendersDistinctly` (part of S-CTX-014). RED confirmed by `go test -race -count=1 -run '...' ./src/agent/...` (0.296s): `# github.com/.../agent [agent.test]` build failure, `undefined: TokenSource`/`TokenSourceUnavailable`/`TokenSourceReported`/`TokenSourceEstimated` (4 errors).
- [x] 1.2-1.3 GREEN: `context_strategy.go` created per DD1/DD3 — `ContextStrategy`, `ContextPrompt`, `ContextVerdict{}`, `NoOpContextStrategy{}` + `var _` guard, `ContextBudget`/`ContextBudgetOf`/`Limit()`. `token_accounting.go` shell created — `TokenSource` + `String()`, `TokenAccounting` + `Tokens()`.
- [x] 1.4-1.9 All GREEN on first run (`go test -race -count=1 -run '...' ./src/agent/...`, 1.355s → 2.343s across the fix cycle below): all five tests passed, including the internal `TokenSource` table (3 sub-tests).
  - **One TDD-cycle defect found and fixed via the test itself, not production code**: `TestTokenAccounting_UnreadableWithoutSource`'s first assertion used `reflect.TypeOf(agent.TokenAccounting{}).NumField() != 0`, which counts **all** fields (exported and unexported) — `TokenAccounting{tokens int64; source TokenSource}` genuinely has 2 fields, both correctly unexported. The assertion was wrong, not the production type. Fixed to iterate fields and check `f.IsExported()`, which is what "exposes no exported field" (R-CTX-008) actually means. Re-run confirmed GREEN.
- [x] 1.10 Bite **S-CTX-015** (RED-first, then revert): added `func (a TokenAccounting) TokensOnly() int64 { return a.tokens }` beside `Tokens()`. Re-ran `TestTokenAccounting_UnreadableWithoutSource`: **FAILED** — `agent.TokenAccounting declares 2 exported method(s), want exactly 1 (Tokens() (int64, TokenSource)) -- a second accessor would let a consumer bypass the provenance` (1.670s, real run). RED recorded, reverted; re-run confirmed GREEN (`gofmt -l` clean).

## Phase 2 — The estimate, in isolation (commit `4fc28ba6`)

- [x] 2.1 RED: `TestEstimateTokens_TableDriven` (S-CTX-016, 6 sub-cases: pure ASCII, CJK, the minimal valid transcript, tool-schema-bearing, system-instruction-bearing, mixed content parts) + `TestEstimateTokens_Determinism` (S-CTX-018) + `TestEstimateTokens_NoClockNoIO_SourceScan` (R-CTX-010's direct guard). RED confirmed (0.110s): `undefined: estimateBytesPerToken`/`estimateTokensPerMessage`/`estimateTokens` (10 errors, "too many errors").
- [x] 2.2 GREEN: `estimateTokens` implemented per DD5's exact accessor walk (system instruction segments; message text/tool-call-name+arguments/tool-result-content/reasoning-text; tool name+description+schema); `estimateBytesPerToken = 4`, `estimateTokensPerMessage = 4`, each documented with its rationale. **All tests passed on the first GREEN attempt** (2.847s, `-race -count=1`; 6/6 table sub-cases + determinism + source scan).
- [x] 2.3-2.4 Determinism asserted directly (three figures identical: twice over the same value, once over an independently re-constructed equal request). "Never converted to `ai.TokenCount`" verified fully as a diff check in Phase 7 (`TestNoRelease_SubstrateByteUnchanged`).

## Phase 4 — The exported `agenttest` fixture (commit `b4a211f1`)

Built ahead of Phase 3 per tasks.md's own allowance ("stub minimally or sequence after 3.x if needed") — the resolver's three-state test needs the real fixture, not a stub.

- [x] 4.1 RED: `counting_provider_test.go` (`package agenttest_test`) — 5 tests covering report, absent-count-with-nil-error, the R-AMP-019 decline fixture, capture order + fresh-slice mutation-safety, and the embedded `*Provider.Stream` delegation. RED confirmed (1.860s): `undefined: agenttest.NewCountingProvider`/`NewFailingCountingProvider`/`CountingProvider` (7 errors).
- [x] 4.2-4.3 GREEN: `counting_provider.go` — `CountingProvider` embedding `*Provider` (pointer, per `fake_provider.go:74`), `NewCountingProvider`/`NewFailingCountingProvider`, `CountTokens`/`CountRequests` (fresh slice via `slices.Clone`), both compile-time guards. **All 5 tests passed on the first GREEN attempt** (2.493s).

## Phase 3 — The accounting resolver (commit `89fe7b12`)

- [x] 3.1 RED: `TestResolveTokenAccounting_ThreeStates` (S-CTX-012, the four fixtures a-d using the now-real `CountingProvider`/`NewFailingCountingProvider`) + `TestResolveTokenAccounting_BuildFailureYieldsUnavailable` (R-CTX-006's build-failure branch). RED confirmed (0.267s): `undefined: resolveTokenAccounting` (5 errors). No import cycle between `package agent` (internal test) and `agenttest` — confirmed by this same RED output (a cycle would report a different error).
- [x] 3.2 GREEN: `resolveTokenAccounting` implemented per DD4's exact branch order. **All tests passed on the first GREEN attempt** (2.877s; rows a/b/c/d all correct, build-failure path confirmed 0 counting calls made).
- [x] 3.3 Bite **S-CTX-013** (RED-first, then revert): routed the advertised-counter non-conformance branch (`cerr != nil || !present`) through `estimateTokens`/`TokenSourceEstimated` instead of the zero value. Re-ran `TestResolveTokenAccounting_ThreeStates`: **FAILED** on both non-conformance rows — `resolveTokenAccounting(erroring advertised counter) = (24, estimated), want (0, TokenSourceUnavailable)` and the same for the nil-error/absent-count row, each also failing the zero-value equality check (1.736s, real run). RED recorded, reverted; `gofmt -l` clean after revert (one pre-existing doc-comment indentation issue from the original write was also normalized at this point, unrelated to the bite).

## Phase 5 — The seam consultation (commit `a7f630de`)

- [x] 5.1-5.9 RED: `context_strategy_test.go` extended with `TestHarness_ContextStrategy_ConsultedOncePerLogicalTurn` (S-CTX-001), `TestHarness_ContextStrategy_RetriedTurnConsultsOnce` (S-CTX-002), `TestHarness_ContextStrategy_PromptCarriesTranscriptAndBudget` (S-CTX-004), `TestHarness_ContextStrategy_NoOpInstalled_ZeroCompactionEvents` (S-CTX-006), `TestHarness_ContextStrategy_NilVsNoOpByteIdentical` (S-CTX-007). RED confirmed (0.658s): `unknown field ContextStrategy in struct literal of type agent.Harness` (4 errors across the 5 new tests).
- [x] GREEN: `Harness.ContextStrategy`/`ContextBudget` fields added after `Failover` (`harness.go:92-103` post-edit); the consultation block inserted immediately after `transcript := transcriptFromHistory(hist)`, guarded by `h.ContextStrategy != nil`, per DD2's exact quoted comment. **All 5 tests passed on the first GREEN attempt** (3.466s).
  - S-CTX-001's "consulted before that turn's first provider call" is proved mechanically, not by wall-clock interleaving: the recording strategy snapshots `len(provider.Requests())` **inside** `Resolve` itself (synchronous, ahead of `Turn`'s own `provider.Stream` call); the observed counts were `[0, 1]` — turn one consulted before any request, turn two consulted after exactly turn one's one request.
  - S-CTX-002's transcript-distinctness is proved by `len(prompt[0].Transcript) < len(prompt[1].Transcript)` (turn two's transcript is strictly longer, carrying turn one's committed entries).
  - S-CTX-004's clone proof uses a `mutatingContextStrategy` that overwrites and appends to the slice it receives; the provider's recorded requests stayed byte-identical (`ai.Request.Equal`) to the non-mutating run.
  - S-CTX-007's "byte-identical" comparison is `assertEventStreamsStructurallyIdentical`: full Kind()-sequence equality, turn-scoping equality, and per-kind payload equality (RunEnd/TurnEnd outcome+failure, ToolStart/ToolEndSuccess fields, MessageDeltaText fragments) at every position — with RunID/TurnID **values** deliberately excluded, since `mintHarnessRunID`/per-invocation `TurnID` mint fresh identities on every independent `Harness.Run` call by design (R-RUN-004's own provenance-distinctness). This is documented in the helper's own doc comment as the one exclusion; every other observable field is compared.
- [x] Bite **S-CTX-003** (RED-first, then revert): moved the consultation block from the turn boundary to immediately inside `for attempt := 1; ; attempt++ {`. Re-ran `TestHarness_ContextStrategy_RetriedTurnConsultsOnce`: **FAILED** — `recorder holds 3 consultation(s), want exactly 2 -- NOT 3 (R-CTX-001: the unit is the LOGICAL TURN, never the attempt)` (2.226s, real run). RED recorded, reverted to the turn-boundary placement.
- [x] Bite **S-CTX-008** (RED-first, then revert): added `NewCompactionStarted(runID, mintLoopTurnID(), "scratch-bite-compaction")` + `sendStamped` inside the guarded consultation block. Re-ran `TestHarness_ContextStrategy_NilVsNoOpByteIdentical`: **FAILED** — `event count differs: 17 vs 19, want equal` (the nil run stays at 17 since the strategy is never consulted; the NoOp run gains one stray event per each of its 2 consultations) (1.982s, real run). RED recorded, reverted.
- [x] **U2 runtime harness proof**: full `go test -race -count=1 ./...` run after Phase 5 landed — see "Full-suite evidence" below. First full run at this point legitimately **FAILED** two tests (`TestTurn_SubstrateUntouched`, `TestTurn_PreRequestHook_SubstrateUntouched`) because the two new `src/agent` production files were not yet exempted from the substrate filters — this is Phase 8's job, not a defect in Phase 5's own commit; recorded and resolved below.

## Phase 6 — AG-17.2's externally-observed scenarios (commit `33edd658`)

No new production code needed — Phase 3/4/5 already implement the underlying mechanism; this phase proves it from the fully external, driven-harness angle NFR-CTX-001 requires.

- [x] 6.1 `TestHarness_ContextStrategy_Accounting_DiscoveryAndFallback` (S-CTX-011): a counting-capable fixture reports its own figure (4321) with `Reported`, and the fixture's captured counting-call request carries the same message count as the recorded prompt's transcript, at both consultations; the existing non-counting fake, driven with the identical script, reports `Estimated` at both. `assertNoTokenCountingInterfaceDeclared` parses every non-test `.go` file in `src/agent/` via `go/ast`+`go/parser` and confirms no declared interface carries a `CountTokens` method. **Passed on the first run** (0.04s within the package's own run).
- [x] 6.2 `TestHarness_Accounting_PreHookDivergenceRecorded` (S-CTX-019): a request-mutating `PreRequestHook` appends one message; the counted (pre-hook) request and the sent (post-hook) request are asserted to **differ** — `sentRequests[0].Messages()` has exactly one more message than `countedRequests[0].Messages()` — never asserted equal.
  - **One assertion-quality defect found and fixed via the test, not the production docs**: the doc-comment-content check (`token_accounting.go`/`context_strategy.go` both state the figure is "for the pre-hook request", per R-CTX-011) initially used a case-sensitive `strings.Contains`, and the shipped doc comments use "FOR THE PRE-HOOK REQUEST" (uppercase, for emphasis). Fixed to `strings.Contains(strings.ToLower(...), "pre-hook request")`, which is the more robust, less brittle check and still a real behavioral assertion (it reads the actual file and fails if the semantic sentence is absent). Re-run confirmed GREEN.

## Phase 7 — Closed-sequence proof & substrate verification (commit `57637aa0`)

- [x] 7.1 `TestHarness_ContextStrategy_ClosedSequencesUnaffected` (S-CTX-020): a NoOp-installed multi-turn run's Kind() sequence is position-for-position equal to the nil-field run's, with zero compaction-family kinds on either. `TestTurn_WalkingSkeleton_EmitsContractEventOrder` (S-LSK-001) and `TestHarness_CostSession_FinalOnShutdownRun` (S-CAN-013) confirmed passing **unmodified** (`go test -race -count=1 -run '...'`, 1.345s).
- [x] 7.2 `TestNoRelease_SubstrateByteUnchanged` (S-CTX-021): `doc.go`, `doc_contract_guard_test.go`, `stream_check.go`, `event.go`, `event_descriptor.go`, `event_registry_test.go`, `compaction_events.go`, `cost_events.go`, `cost_usage.go`, `history.go` diffed empty against the merge base; `backend/agent/src/ai/` diff empty; `go.mod`/`go.sum` diff empty; `expectedLayer2ContractRows` confirmed at exactly 8 rows. **Passed on the first run** (0.13s).
- [x] 7.3 `-race -count=1` evidence recorded for every new test throughout this log. Grepped all four new/modified test files for `time.Sleep`: zero matches.
- [x] 7.4 Import-boundary and ambient-authority guards re-run and confirmed passing with zero change: `TestLayer2_ProductionClosure_ImportsOnlyLayer1AndStdlib_DenyByDefault`, `TestLayer2_TestClosure_AdmitsOnlyTheTestSubstrateBeyondProduction`, `TestLayer2_ProductionClosure_ContainsNoNetworkOrFilesystemPackage`, `TestLayer2Agent_NonTestSourcesCarryNoForbiddenCallSite`, `TestLayer2Agent_ForbiddenSetIsPackageScopedDenyByDefault`, plus `TestEventKindRegistration_EveryRegisteredKind_HasConstructorAndAccessor`, `TestEventKinds_ScopeFence_ExactlyRunAndTurnFamilies`, `TestLayer2DocContract_MatchesTheCommittedTable` — 8/8 PASS (1.459s).
- [x] 7.5 `git diff origin/main -- backend/agent/src/agent/`, non-test files: `{context_strategy.go, harness.go, token_accounting.go}`. Of these, `context_strategy.go` and `token_accounting.go` are **new** files, not pre-existing ones; the set of **pre-existing** non-test files that differ is exactly `{harness.go}`, matching S-LSK-027's own claim.

## Phase 8 — Substrate filter widening (commit `9287bf24`)

- [x] 8.1 Both `filterOutLoopFiles` (`loop_test.go`) and `filterOutLoopHookFiles` (`loop_hook_test.go`) widened by exact filename suffix at their re-grepped tails: `/context_strategy.go`, `/token_accounting.go`, `/context_strategy_test.go`, `/token_accounting_test.go`. No pre-existing filename added (AG-17 takes no release, the AG-15 shape). `cost_events.go`/`cost_events_test.go`/`stream_check_test.go` confirmed absent from both (no action needed — never added). No file under `src/agenttest/` added (confirmed these filters govern only `src/agent/`).
- [x] 8.2 Both filters' entry sets extracted (`grep -oE '/[a-zA-Z0-9_]+\.go'`) and diffed: **identical**, no wildcard/prefix/directory pattern anywhere. `go build ./...` clean immediately after.
- **Re-ran `TestTurn_SubstrateUntouched`/`TestTurn_PreRequestHook_SubstrateUntouched`**: both **PASS** (1.559s) — resolving Phase 5's legitimate interim failure.

## Full-suite evidence (the U2 runtime-harness proof and the final gate, not cached)

| Run | Command | Wall clock | Result |
|---|---|---|---|
| Baseline, pre-AG-17 | `go test -race -count=1 ./...` | 2:55.04 (`openaicompat` 171.898s) | **PASS**, 12/12 packages `ok` — safety net before any edit |
| After Phase 5 (seam wiring) | `go test -race -count=1 ./...` | 2:50.58 (`agent` 8.940s) | **FAIL** — `agent` package only; `TestTurn_SubstrateUntouched`/`TestTurn_PreRequestHook_SubstrateUntouched` failed because `context_strategy.go`/`token_accounting.go` were not yet exempted from the substrate filters. **Legitimate and expected** at this point in the sequence — resolved by Phase 8, not a defect |
| After Phase 8 (filters widened) | `go test -race -count=1 ./...` | 2:51.33 (`agent` 9.699s, `openaicompat` 170.752s) | **PASS**, 12/12 packages `ok`, EXIT:0 |
| After the ST1008 lint fix (Phase 12, final gate) | `go test -race -count=1 ./...` | 2:54.07 (`agent` 9.504s, `openaicompat` 173.467s) | **PASS**, 12/12 packages `ok`, EXIT:0 — the definitive, final-state evidence for task 12.1 |

No run in this table was cited as evidence while it finished in under ~10 seconds; every `openaicompat` subtotal above is a genuinely uncached run (~170s), matching the known repo baseline this change's launch context named.

## Phase 9 — Spec deltas: five back-annotations (commit `1f9a61ea`)

- [x] 9.1 `agent-run-driver` — row `:342` back-annotated **CLOSED by AG-17 — but only the check half**, plus five "still true at AG-17" parentheticals across the Explicit non-requirements table (cross-event identity, cost aggregation, multi-turn state, the Layer 1 edit prohibition, the new-`EventKind`/`TurnOutcome`/`History`-method prohibition, the `L2C-08` row question).
- [x] 9.2 `agent-history` — row `:260` back-annotated **CLOSED**, recording history needed **no** change; the compaction-removes-entries row and the `ai/validation.go` row each gained a short AG-17 parenthetical.
- [x] 9.3 **`agent-v1-scope` back-annotation — enforced by NO Go test, applied deliberately** (proposal risk 6, the single highest-likelihood skip in this change). `R-AGS-007` gained the full back-annotation naming seam 5 → AG-17.1 and seam 6 → AG-17.2's shipped mechanism; `S-AGS-023` gained its update parenthetical; `S-AGS-024`/`S-AGS-026` gained theirs. Manually confirmed both rows carry the annotation by direct re-read after writing.
- [x] 9.4 `agent-retry-failover` — the full "Back-annotation (AG-17, 2026-08-19)" paragraph landed inside `R-RTY-002` (both placements weighed, the shipped one recorded as what holds the pin); `S-RTY-002` gained its AG-17 note; `S-RTY-011`'s bite text is unchanged (nothing to annotate — it is about the loop-skeleton delta's own pre-stream close, not this seam).
- [x] 9.5 `agent-loop-skeleton` — header range extended to `S-LSK-028`; the "AG-17 requests NO release" paragraph (4 numbered consequences) and its filter-widening paragraph both landed inside `R-LSK-004`; `S-LSK-027`/`S-LSK-028` appended verbatim; the "(Previously: ...)" note updated to record AG-17's own landing, matching AG-14/15/16's own precedent of updating that note at each landing.
- [x] 9.6 All cited `file:line` pointers re-read against the shipped change. **Drift found and recorded, not corrected in the promoted specs** (this repo's established convention — citations are pinned to a stated base commit, not continuously rewritten as later edits shift line numbers; AG-11 through AG-16's own promoted specs already carry stale citations from their own landing for the identical reason):
  - `harness.go:498` (transcript resolution, design.md/R-CTX-001's own citation) → now `:512` (+14: 1 line from the new `slices` import, 13 from the two new `Harness` fields + their doc comments).
  - `harness.go:530` (the attempt loop) → now `:562` (+32: the above +14, plus +18 from the inserted consultation block).
  - `harness.go:91` (the `Failover` field, design.md DD2's own citation) → now `:92` (+1, from the new `slices` import alone — nothing was inserted before it).
  - `harness.go:518-529` (the R-RTY-002 argument comment `agent-retry-failover/spec.md` and `agent-run-driver/spec.md` both cite) → now starts at `:550` (+32, the same total shift as the attempt loop, since this comment sits immediately above it).

## Phase 10 — Documentation (commit `db232e0e`)

- [x] 10.1 Doc 0003's status line: "Wave 3 opens with AG-12…AG-16" → "Wave 3 complete (AG-12…AG-16), Wave 4 opens with AG-17"; counter 16→17 of 24; date bumped to 2026-08-19; one narrative sentence added naming what AG-17 closes, mirroring AG-13…AG-16's own added sentences.
- [x] 10.2 Line `:2175`'s completion checklist item ticked `[x]`.
- [x] 10.3 Confirmed no `L2C-0N` row owed (Phase 7.2's own test covers this); confirmed `backend/agent/README.md` does not exist (no file to grep, nothing to edit).
- [x] 10.4 Confirmed the R-18 traceability-spine row (`:2217`) already reads "seam 5 → AG-17.1 · seam 6 → AG-17.2" — no edit needed; the mapping was never wrong, only undischarged.

## Phase 11 — OpenSpec promotion + archive (commit `90cbaa66` + this commit)

- [x] 11.1 `openspec/specs/agent-context-strategy/spec.md` created verbatim from the change-dir source. Diffed line-for-line against the source: **only two intentional adjustments** — the "Becomes … at archive" framing line (now "Promoted to … at archive", matching AG-16's own promoted-capability phrasing) and the Sources line's relative links (now naming the archived path directly, since `../../proposal.md` etc. would resolve incorrectly once the change directory moves). One accidental bold-span transcription drift (`**AG-18** is their first caller` vs the source's `**AG-18 is their first caller**`) was found by this same diff and corrected to match verbatim.
- [x] 11.2 All five deltas' applications verified programmatically against their change-dir sources: 10 distinct back-annotated paragraphs/rows extracted and compared character-for-character (first 2000 chars each, which covers every one of them in full) — **all 10 MATCH**, nothing dropped or truncated.
- [x] 11.3 `git mv openspec/changes/cachicamas-agent-context-strategy/ openspec/changes/archive/2026-08-19-cachicamas-agent-context-strategy/` (AG-16 naming precedent). **Every one of the 10 moved files' `git hash-object` blob hash confirmed identical before and after the move** — cryptographic proof against the known sub-agent-artifact-truncation risk, not a visual diff or a trusted "moved" report.
- [x] 11.4 `archive-report.md` written (this same commit).
- [x] 11.5 This file.

**One process defect found and fixed during Phase 11**: the first archive commit (`d2ee37c4`) staged the `git mv` rename and the ST1008 lint fix, but the `tasks.md` checkbox edits (made via a Python script between the `git mv` and that commit) were never re-staged before committing — `git commit` only commits what is staged, and the earlier `git add` for that commit named only `context_strategy_test.go`. The commit therefore landed with `tasks.md` still showing 0 checked boxes despite the working tree carrying 57. Found by checking `git show HEAD --stat` (`tasks.md | 0`) immediately after that commit rather than trusting it, and fixed by staging and folding the correction into this commit.

## Phase 12 — Final gate

- [x] 12.1 `cd backend/agent && make test` (`go test -race -count=1 ./...`) — see "Full-suite evidence" above and its completion below. Never cited a cached (<10s) result as evidence.
- [x] 12.2 `golangci-lint cache clean && make lint` (pinned `v2.9.0`, installed fresh via `make tools` into `bin/` — the globally installed `golangci-lint` on this machine is `2.12.2`, a different, unpinned version, deliberately not used for this evidence). First run: **1 issue** — `context_strategy_test.go:511`, `ST1008: error should be returned as the last argument (staticcheck)` in the `S-CTX-007` test's local `run` closure. Fixed by reordering `(ai.FinishReason, error, []agent.Event)` → `(ai.FinishReason, []agent.Event, error)`. Re-run: **`0 issues.`**
- [x] 12.3 `gofmt -l backend/agent` — 15 pre-existing gofmt-dirty files reported (`compaction_events_test.go`, `cost_events_test.go`, `delegation_events_test.go`, `envelope_test.go`, `event_registry_test.go`, `permission_events.go`, `permission_events_test.go`, `permission_protocol_test.go`, `protocol_events_test.go`, `reconstruction_test.go`, `scheduler.go`, `scheduler_test.go`, `scripted_tool_test.go`, `tool.go`, `tool_test.go`) — **none of them is a file this change touches**; every file AG-17 created or modified is gofmt-clean. Pre-existing repo debt (the AG-16 archive-report's own precedent records the same class of finding), several of them substrate files this change is forbidden to edit regardless.
- [x] 12.4 `make build` clean (0.244s). `make vuln-check` (`govulncheck -json ./...`, pinned via `make tools`'s auto-install) — 170 OSV database entries streamed as progress, **0 findings with a reachable trace**; plain `govulncheck ./...` confirms in human-readable form: **"No vulnerabilities found."** Exit code 0.
- [x] 12.5 `tasks.md` self-check below.

## `tasks.md` self-check

All 64 checkbox items in `tasks.md` are `[x]`. The Coverage table's 26 rows (21 `S-CTX-*` scenarios incl. 4 bites, `S-AGS-023..026`, `S-RTY-002`/`S-RTY-011`, `S-LSK-020`/`S-LSK-027`/`S-LSK-028`, doc 0003, OpenSpec promotion) are each closed by the tasks/evidence recorded above.

## Deviations from tasks.md's literal phase order

1. **Phase 4 built before Phase 3's own numbering**, per tasks.md's own explicit allowance ("stub minimally or sequence after 3.x if needed") — the resolver's `TestResolveTokenAccounting_ThreeStates` needs the real `CountingProvider`/`NewFailingCountingProvider` fixtures to test genuinely, not a throwaway stub.
2. **S-CTX-009/S-CTX-010 (`ContextBudget`) placed in the external `context_strategy_test.go`, not the internal `token_accounting_test.go`** the tasks.md file-hosting summary table names. `NFR-CTX-001` (the actual requirement, more authoritative than the summary table) carves out only "the estimate's own table and the resolver's three-state table" for internal placement; `ContextBudget`'s own zero-value semantics are not one of those two, and placing them externally maximizes verifiability per that requirement's own stated default and preference.
3. **S-CTX-010's second half** ("given a harness with no budget field set, when a recording strategy is consulted, then the prompt's budget reports absence") needs the wired seam (Phase 5) to observe at all, so it was not written as an isolated Phase-1 test — consistent with the file's own top-of-file reconciliation note about which scenarios need wiring and which do not.

   **CORRECTION (post-verify).** This entry originally claimed the second half "is proved as a corollary inside `TestHarness_ContextStrategy_PromptCarriesTranscriptAndBudget`'s sibling assertions". **That claim was false and is retracted.** That test installs `ContextBudgetOf(4096)` and asserts `(4096, true)` (`context_strategy_test.go:449-452`) — the *opposite* input — so it cannot distinguish an absent budget from a stated limit of zero, and no other test asserted the unset-field case either. `sdd-verify` established the *behavior* was nevertheless correct (an overlay-only run with `ContextBudget{}` yielded `(0, false)`), so this was a **missing assertion and a false evidence record, not a shipped defect**. It is now closed properly by a real test: **`TestHarness_ContextStrategy_UnsetBudgetReachesStrategyAbsent`** (`context_strategy_test.go`), which drives a two-turn run from a `Harness` literal that **omits** the `ContextBudget` field and asserts `(0, false)` plus zero-value equality at *every* consultation. Proved load-bearing by defeat: stating `ContextBudgetOf(0)` instead makes it FAIL with `(0, true)`.
