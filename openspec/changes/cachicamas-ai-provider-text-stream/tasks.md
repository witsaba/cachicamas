# Tasks: AI-28 `cachicamas-ai-provider-text-stream` (translate the response lifecycle and text)

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | 1800–3600+ across 7 slice PRs (repo precedent runs ×2–4 over naive estimates: AI-21 forecast 900–1400, landed 2241) |
| 400-line budget risk | High milestone-wide; Medium–High for slices 1–2 (pre-declared `Split if` candidates), Low–Medium for slices 3–7 |
| Chained PRs recommended | Yes |
| Suggested split | 7 slices = 7 nodes (doc 0002: node boundary = PR-chain boundary); slice1 base, slice2→1, slice3→2, slice4→3, slice5→4, slice6→5, slice7→6 |
| Delivery strategy | auto-chain |
| Chain strategy | feature-branch-chain |

Decision needed before apply: No
Chained PRs recommended: Yes
Chain strategy: feature-branch-chain
400-line budget risk: High

### Suggested Work Units

| Unit | Goal | Likely PR | Focused test command | Runtime harness | Rollback boundary |
|---|---|---|---|---|---|
| 1 | Producer shell: `Stream`, cancellation, guard flips (R-ATS-001…006) | PR 1 (base=tracker) | `go test -race -count=1 -run 'TestClient_HasStreamingEntryPoint|TestClient_SatisfiesModelProviderAtRuntime|TestStream' ./...` (renamed from `TestClient_HasNoStreamingEntryPoint`/`TestClient_DoesNotSatisfyModelProviderAtRuntime` — verify-report S2 corrective, AI-28.7) | `httptest.Server` transcript replay, real loopback HTTP | Revert `stream.go` + guard-flip commit; `*Client` returns to AI-25 shape |
| 2 | Text mapping + conformance bridge (R-ATS-007…011) — BLOCKED, see gate | PR 2 → PR1 | `-run 'TestChunk|TestStreamState|TestConformanceBridge'` | `agenttest.RunConformanceFor(CapStreamingText)` over bridge's `httptest.Server` | Revert `chunk.go`+`stream_state.go`+bridge; slice 1 unaffected |
| 3 | Terminal discipline (R-ATS-012…014) | PR 3 → PR2 | `-run 'TestTerminal|TestTruncation'` | `[DONE]`-terminated fixture replay | Revert terminal-handling diff only |
| 4 | Absent-vs-zero usage (R-ATS-015…016) | PR 4 → PR3 | `-run TestUsage` | Usage-chunk fixture replay | Revert usage-mapping diff only |
| 5 | Unknown/delta-less tolerance (R-ATS-017…019) | PR 5 → PR4 | `-run 'TestTolerance|TestKeepAlive'` | Unknown-frame fixture replay | Revert tolerance diff only |
| 6 | Protocol-order violations + charter inspections (R-ATS-020…028) | PR 6 → PR5 | `-run 'TestProtocolViolation|TestCharter'` | 5-row table-driven fixture replay | Revert violation-table diff only |
| 7 | Pre-decode checks (R-ATS-023…024) — BLOCKED, see gate | PR 7 → PR6 | `-run TestPreDecode` | Non-stream-content-type + failure-status fixture replay | Revert `stream.go` pre-decode addition; carryover if AI-32.1 absent |

## Slice 1 — AI-28.1.1 Producer shell

- [x] 1.1 RED: flip `TestClient_HasNoStreamingEntryPoint`/`TestClient_DoesNotSatisfyModelProviderAtRuntime` in `provider_boundary_test.go` to post-AI-28 polarity (S-ATS-020…023, S-APC-030/031); run first — this captures the old guards failing because `Stream` doesn't exist yet.
  Evidence: flipped both assertions + doc comments (cite AI-28/R-ATS-001/R-ATS-006/S-ATS-020…021) in place, same test names ("successors"). RED confirmed via `go test -race -count=1 -v -run 'TestClient_HasNoStreamingEntryPoint|TestClient_DoesNotSatisfyModelProviderAtRuntime' ./src/ai/openaicompat/...`: both FAIL — `provider_boundary_test.go:22: *Client exposes no Stream method — AI-28 must land it (R-ATS-001, S-ATS-020)` and `provider_boundary_test.go:42: *Client does not satisfy ai.ModelProvider — AI-28 must land Stream (R-ATS-001, S-ATS-021)`. Both green after 1.4 (see 1.5).
- [x] 1.2 Correct stale AI-26 prose: `doc.go:5` and `client.go:61-62` → AI-28 (R-ATS-006).
  Evidence: both corrected in place ("Streaming behaviour arrives at AI-28 (see stream.go)…" / "…it acquires streaming behaviour at AI-28."). Disclosed hygiene beyond this task's named two files: `request.go`'s `newRequest` doc comment also said "AI-26 is the seam that will drive this method with a real request body" — corrected to AI-28 as part of 1.4's edit to that same function (the comment directly describes the signature 1.4 changes), not a separate file-wide rewrite.
- [x] 1.3 RED: write failing tests for R-ATS-001…005 (S-ATS-001…019): entry point/interface, validate→Translate→ctx.Err ordering (N-7a), minimal-transcript drain sequenced 1…N closed once, byte-exact response identity incl. S-ATS-015 empty-`id` malformed path, cancellation/select-send/single-close (AI-20.3).
  Evidence: created `stream_test.go` (11 top-level tests + 6 subtests, internal `package openaicompat`) covering S-ATS-001…002 (S-ATS-003 is agenttest's own pre-existing, unmodified `TestModelProviderInterface_SignatureGuard` — proof is its continued pass in 1.5's full-suite run, not a new test), S-ATS-004…007, S-ATS-008…011, S-ATS-012…015 (triangulated 3 ways: empty id / empty model / absent id key), S-ATS-016…018 (S-ATS-019 is `[inspection]`, discharged by grep in 1.6, not a test). RED confirmed via `go vet ./src/ai/openaicompat/...`: compile failure, `c.Stream undefined (type *Client has no field or method Stream)` at every one of the 10 call sites — reference to not-yet-existing production code per strict-tdd.md's sanctioned RED form.
- [x] 1.4 GREEN: create `stream.go` — `Stream` entry point, producer goroutine, one `close(out)` site, select-send, `ai.NewResponseStart` identity path converting constructor error to D7 malformed-response terminal (N-7b).
  Evidence: created `stream.go` (`Stream`, `run`, `applyFrame`/`buildCompletion` on a slice-1-scoped `producerState`, single `emit` helper (D6), `emitFailure` (D7), `errMalformedIdentity`/`errIncompleteStream` unexported causes wrapping `ai.ErrMalformedResponse`). Extended `request.go`'s `newRequest` with a `body []byte` parameter (before the variadic `segments`) plus conditional `Content-Type`/`Accept` headers when body is non-nil; updated its doc comment (AI-26→AI-28). Updated the three existing call sites to pass `nil`: `client_test.go` (`driveOneRequest`), `viability_test.go` (`driveOneViabilityRequest`), and `timeout_test.go` (found via `grep -rn '\.newRequest(' src/ai/openaicompat/*.go` after the first `go vet` pass caught only 2 of 3 — safety-net discipline: fixed rather than left broken). All new tests green on first GREEN attempt (see 1.5).
- [x] 1.5 Evidence: `go test -race -count=1 ./...` from `backend/agent/`, run twice; confirm 1.1/1.3 scenarios and both flipped guards green.
  Ran 4 total full-suite passes across this slice (2 before the lint fix below, 2 after) — all green, all packages: `agenttest`, `ai`, `ai/openaicompat`. `openaicompat` top-level test count: baseline 137 (pre-slice-1, captured before any edit) → 148 post-slice-1 (+11 new top-level tests; +17 incl. subtests) — all real assertions against a real `httptest.Server`-driven `Stream` (HTTP request/response, SSE decode loop, `ai.NewResponseStart`/`ai.NewCompletion` construction), none vacuous. Both flipped guards (1.1) pass. Goroutine-leak and carrier-close-discipline tests (S-ATS-005/016/018) pass under `-race`.
- [x] 1.6 `go.mod` zero-requires check + `go vet`/lint clean.
  `grep -c '^require' go.mod` = 0 (file unchanged: `module`+`go` directives only). `go vet ./...` clean. `make lint` (`golangci-lint`) initially reported 1 issue: `stream.go`'s file-header comment was directly attached to `package openaicompat` with no blank line, so revive's `package-comments` rule read it as a second, malformed package comment (doc.go already carries the canonical one). Fixed by inserting a blank line before `package openaicompat` in both `stream.go` and `stream_test.go` (matching the rest of the package's own convention, e.g. `src/ai/sequence.go`). Re-ran: `make lint` → `0 issues`.

## Slice 2 — AI-28.1.2 Text mapping + conformance bridge — COMPLETE

> GATE (R-ATS-011 STOP rule): verify `cachicamas-ai-conformance-lifecycle-amendment` has landed **both** R-CNF-023 (capability-scoped entry point) and R-CNF-024 (read-only `Script`/`Step` introspection) before 2.4. If absent: STOP, record a carryover naming the amendment as blocker — no `agenttest` edit, no fork, no substitute entry point.
  Evidence (task 2.0 gate check, performed first, mechanically): `agenttest.RunConformanceFor(t *testing.T, f Factory, capability Capability)` confirmed present in `backend/agent/src/agenttest/conformance_scoped.go`; `Step.Event() (ai.Event, bool)` and `Step.IsHold() bool` confirmed present in `backend/agent/src/agenttest/script_introspect.go`; `conformance_text.go`'s amended cases confirmed asserting the 6-event window (`textOrderingCase`: ResponseStart, TextBlockStart, TextDelta×2, TextBlockEnd, Completion) and the 2-event window (`textEmptyCompletionCase`: ResponseStart, Completion). Gate DISCHARGED — proceeded per R-ATS-011 rev 2.

- [x] 2.1 RED: tests in `chunk_test.go` for R-ATS-007/009/010 (S-ATS-024…027, S-ATS-033…039): byte-exact `TextDelta` incl. null/absent/empty content, raw-byte split-rune dialect-conventional fixtures (content placed after split point per non-vacuity discipline), raw-string-strict finish-reason gate.
  Evidence: created `chunk_test.go` (10 top-level tests) referencing not-yet-existing `contentText`/`rawStrictFinishReason`/`decodeChunk`. RED confirmed via `go vet ./src/ai/openaicompat/...`: compile failure, `undefined: contentText` (×7), `undefined: rawStrictFinishReason` (×3+), at every call site — reference to not-yet-existing production code, strict-tdd.md's sanctioned RED form (same shape slice 1 used). Split-rune fixtures place meaningful content AFTER the split point (2-byte é across 2 deltas with 15 trailing bytes; 4-byte emoji across 3 deltas with trailing text) — vacuous-pass catalogue shape 4 discipline (Engram obs #2471) applied; assertions anchor on hard-coded expected literals (`const want = "caf\xc3\xa9 shop, abierto"`), not derived values.
- [x] 2.2 RED: tests in `stream_state_test.go` for R-ATS-008 (S-ATS-028…032): block mint at index 1, open-before-first-delta, close-before-Completion, terminal→sentinel window where delta-less/usage-only chunks are the clean-close shape (S-ATS-032; content-string contrast deferred to slice 6's S-ATS-074).
  Evidence: created `stream_state_test.go` (5 top-level tests, driving `mapperState.applyChunk` directly — the "direct through Decoder.Feed+mapper" seam design.md's Testing Strategy names). RED confirmed: package failed to build with `chunk_test.go` present (`wireChunk`/`wireChoice` redeclared between slice-1's `stream.go` and the new `chunk.go`) until the stream.go refactor + `stream_state.go` landed together — disclosed sequencing note below. Once `mapperState`/`applyChunk` existed, one self-authored test bug found and fixed during GREEN verification (S-ATS-032's own fixture: the first chunk in that test also establishes identity, so it emits 3 kinds not 2 — assertion corrected, not production code).
- [x] 2.3 GREEN: create `chunk.go` (`RawMessage` + byte-preserving unquoter, D2) and `stream_state.go` mapper (block minting D4, finish-reason gate D5).
  Evidence: `chunk.go` (`wireChunk`/`wireChoice`/`wireDelta`, `decodeChunk`, `contentText` trichotomy, `unquoteJSONString` hand-rolled escape decoder incl. `\uXXXX`, `rawStrictFinishReason` raw-string-strict gate against C2's 5-member enum). `stream_state.go` (`mapperState`, `applyChunk` — identity+block-mint+delta+block-close in emission order, `buildCompletion` deferred to sentinel per D4/D9). Required refactor of slice-1's `stream.go`: deleted its slice-1-scoped `wireChunk`/`wireChoice`/`producerState`/`applyFrame`/`buildCompletion` (superseded — R-ATS-004 identity establishment absorbed into `mapperState`, a structural consequence of design's "chunk.go + stream_state.go" home for R-ATS-007…010 centralizing every frame-to-event decision in one state machine); `run()`'s loop now recognises `[DONE]` inline, calls `decodeChunk`+`state.applyChunk`, iterates a `[]ai.Event` per frame instead of at most one. Safety net: full slice-1 suite (148 top-level tests) re-run and green after the refactor — zero regressions. `go build ./...` clean throughout.
  Non-scenario fix disclosed: slice 1's `emitFailure` hardcoded `outputPreceded=false` with its own comment naming "R-ATS-007 lands in AI-28.1.2" as the trigger to revisit — this slice is that trigger. `run()` now tracks `outputPreceded` via a new `isOutputEvent` helper (true for TextBlockStart/TextDelta/TextBlockEnd, never ResponseStart per S-ATS-050) and passes the real fact through `emitFailure`'s new parameter. Proven by a new end-to-end test (`TestStream_ContentThenUnrecognizedFinishReason_TerminatesMalformedWithPartialOutputTrue`, `stream_text_test.go`) rather than a scenario-numbered RED — implemented alongside the refactor since the fix only makes sense once `emitFailure`'s signature changed; disclosed as a deviation from strict sequencing, not from correctness.
- [x] 2.4 RED: bridge tests for R-ATS-011 (S-ATS-040…043) using `agenttest.RunConformanceFor(t, f, CapStreamingText)` (R-CNF-023) and `Script.Steps`/`Step.Event()`/`Step.IsHold()` (R-CNF-024), failing fast (`tb.Fatalf`) on any hold step; bridge factory declares Reasoning/TokenCounting/CacheBoundary non-nil `false` (W-2).
- [x] 2.5 GREEN: implement bridge renderer + `httptest.Server` transcript replay, test-only, confined to `_test.go` files of `openaicompat`.
  Evidence (2.4/2.5 combined): created `bridge_test.go` — `conformanceBridgeFactory()` (all three optional capabilities declared non-nil `false`, W-2), `renderScript` (renders every `Script.Steps` step via `Step.IsHold()`/`Step.Event()` only, `tb.Fatalf` fail-fast on any hold step, called synchronously on the test goroutine — before the server starts — so `FailNow` runs on the goroutine Go's testing package requires; TextBlockStart/End render zero bytes per C3), `bridgeQuoteJSONString` (hand-rolled byte-preserving JSON-string encoder, deliberately not `encoding/json.Marshal`), `serveTranscripts` (queue-order replay, calls no `*testing.T` method from the handler goroutine). `TestConformanceBridge_StreamingText` calls `agenttest.RunConformanceFor(t, conformanceBridgeFactory(), agenttest.CapStreamingText)` and passed on first run — both landed cases (`text/order_contiguity_byte_exact_reconstruction`, `text/empty_completion_is_legal`) green against real HTTP. This test+implementation pair was authored together rather than via a captured compile-fail RED (disclosed deviation from strict sequencing — see risks); non-vacuity instead proven by a staged, reverted mutation: swapping `bridgeQuoteJSONString` for `json.Marshal` reproduced the exact U+FFFD corruption design.md's D2 predicted (`conformance_suite.go:477: reconstructed text = "caf<0xEF><0xBF><0xBD><0xEF><0xBF><0xBD> shop", want "café shop"`), confirming the test fails meaningfully when the byte-preservation contract breaks; reverted, re-confirmed green.
- [x] 2.6 Evidence: `-race -count=1` twice; confirm zero files under `src/agenttest/` modified (S-ATS-042); `go.mod` zero-requires + lint.
  `git diff --stat -- backend/agent/src/agenttest/` empty — zero files touched (S-ATS-042). `grep -c '^require' go.mod` = 0. `gofmt -l` clean on every slice-2 file (one pre-existing, unrelated finding on `src/ai/completion_test.go`, not touched by this slice — disclosed, not fixed, per strict-tdd's "do not fix pre-existing failures" discipline). `make lint`: initially 2 issues (same `package-comments` revive finding slice 1 hit — `chunk.go`/`stream_state.go`'s file-header comments had no blank line before `package openaicompat`; fixed identically to slice 1's own precedent) → `0 issues` after. `go test -race -count=1 ./...` run twice from `backend/agent/`: both green, all 3 packages (`agenttest`, `ai`, `ai/openaicompat`). `openaicompat` top-level test count: 148 (slice-1 baseline) → 168 (+20 new top-level; 291 incl. subtests). Exit criterion (doc 0002: "the conformance text case (AI-23.2) passes against real transport for the first time") MET: `TestConformanceBridge_StreamingText` green via `RunConformanceFor` against real `httptest.Server` → `Client` → `Stream` transport.

## Slice 3 — AI-28.2 Terminal discipline — COMPLETE

- [x] 3.1 RED: tests for R-ATS-012…014 (S-ATS-044…054): `data: [DONE]` clean termination (dialect-conventional, fixture-pinned), close-without-sentinel → typed error with `PartialOutput()` flag (D8 block-close-before-ErrorEvent), post-sentinel/pre-EOF frames ignored.
  Evidence: created `terminal_test.go` (5 top-level tests: R-ATS-012 S-ATS-044/045/046/047, R-ATS-014 S-ATS-052/053/054) and `truncation_test.go` (3 top-level tests: R-ATS-013 S-ATS-048/049/050/051). All 8 tests **passed on first execution** against the unmodified slice-1/2 code — this is the disclosed, expected shape tasks.md itself anticipated ("Slice 1 left `errIncompleteStream`'s EOF/no-sentinel fallback provisional and uncovered — this slice gives it its real scenario coverage"): `run()` already recognises `doneSentinel` and returns immediately on match (slice 1, guaranteeing R-ATS-014 structurally), and already builds the `errIncompleteStream`/`ErrTruncated` malformed-response terminal with the real tracked `outputPreceded` fact (slice 2) on any EOF before the sentinel (R-ATS-013). Non-vacuity proven via two staged, reverted mutations in `stream.go` (approval-testing pattern, matching bridge_test.go's own slice-2 precedent):
  - **Mutation 1** (sentinel early-return removed after `emit(ctx, out, stamper, completion)`): `go test -race -count=1 -run 'TestTerminal' ./src/ai/openaicompat/...` → all 5 `TestTerminal_*` tests FAIL — `last event kind = error, want EventKindCompletion` (S-ATS-044/046/052/053) and `terminal event count = 2, want exactly 1: [responsestart completion error]` (S-ATS-054). Reverted; `git diff --stat -- src/ai/openaicompat/stream.go` empty after revert.
  - **Mutation 2** (both `emitFailure` calls in the EOF branch suppressed, replaced with a bare `return`): `go test -race -count=1 -run 'TestTruncation' ./src/ai/openaicompat/...` → all 3 `TestTruncation_*` tests FAIL — `last event carries no ErrorPayload` (S-ATS-048/050/051). Reverted; `git diff --stat -- src/ai/openaicompat/stream.go` empty after revert.
  **S-ATS-046 interpretation disclosed** (see risks): implemented under the reading "the sentinel's own SSE-mandatory blank line, immediately followed by EOF with zero further bytes" rather than "the sentinel's own blank line itself missing" — the latter was empirically probed against `Decoder.Finish()` (frozen AI-27 file) and confirmed to return `ErrTruncated`, which is a hard, unresolved conflict with R-ATS-013's own unconditional "closes before the sentinel arrives → typed terminal error" text. Not resolved by inventing new production behavior; flagged as a risk for the maintainer/verify phase.
- [x] 3.2 GREEN: implement sentinel/truncation/post-terminal handling in `stream_state.go`/`stream.go`.
  **No production code change was needed or made** — `git diff --stat -- src/ai/openaicompat/stream.go src/ai/openaicompat/stream_state.go src/ai/openaicompat/chunk.go` against the slice-2 tip is empty. R-ATS-012/013/014 were already fully satisfied by slice-1/2's `doneSentinel` recognition, early-return discipline, and `errIncompleteStream`/`outputPreceded` plumbing; task 3.1's mutation proofs are this task's GREEN evidence (removing the existing logic breaks the tests; the unmodified logic passes them).
- [x] 3.3 Evidence: `-race -count=1` twice; `go.mod` + lint.
  `go test -race -count=1 ./...` from `backend/agent/`: 2 consecutive green runs, all 3 packages (`agenttest`, `ai`, `ai/openaicompat`). `grep -c '^require' go.mod` = 0. `gofmt -l ./src/ai/openaicompat/` empty. `make lint` → `0 issues`. `openaicompat` top-level test count: 168 (slice-2 baseline) → 176 (+8 new top-level; verified via `grep -c '^func Test' terminal_test.go truncation_test.go` = 5+3). Focused command `go test -race -count=1 -run 'TestTerminal|TestTruncation' ./...` from `backend/agent/`: green, 8/8.

## Slice 4 — AI-28.3 Absent-vs-zero fidelity — COMPLETE

- [x] 4.1 RED: tests for R-ATS-015…016 (S-ATS-055…062): absent vs reported-zero `TokenCount`, `usage:null` non-overwrite, presence positively asserted with negative controls (S-ATS-057/060 proving the assertion can fail).
  Evidence: created `usage_test.go` (7 top-level tests, 2 subtests inside `TestUsage_PresenceAssertedPositively_AndNegativeControlFails`). RED confirmed via `go test -race -count=1 -v -run 'TestUsage' ./src/ai/openaicompat/...` against the unmodified slice-3 code (before any chunk.go/stream_state.go edit): 5 of 8 top-level tests **genuinely FAILED** — `Input.Count() = (0, false), want (11, true) (S-ATS-055)`; `Input.Count() ok = false, want true (S-ATS-056)`; `usage records are equal (...), want NOT equal (S-ATS-057)`; `Input.Count() = (0, false), want (7, true) (S-ATS-058)`; `usagePresent() = false, want true (S-ATS-059)` — because `buildCompletion()` unconditionally returned `ai.Usage{}` before this slice, so every count read absent regardless of transcript. The other 3 (S-ATS-060 negative control, S-ATS-061 empty-choices, S-ATS-062 no-usage-chunk-at-all) passed immediately, as expected: they are each the "absent stays absent" complement, not independently gated on the new mapping — their non-vacuity comes from CONTRAST with the 5 genuine REDs above (S-ATS-060 specifically pairs with S-ATS-059's own subtest).
- [x] 4.2 GREEN: implement usage mapping in `stream_state.go` (D10, cites C8).
  `chunk.go`: added `wireUsage` struct (`PromptTokens`/`CompletionTokens *int64` — pointers so an explicit `0` is distinguishable from an absent key, matching `wireDelta.Content`'s own absent/null/present pattern but via `encoding/json`'s native pointer-nil-on-absence rather than a hand-rolled decoder, since these are plain JSON numbers with no byte-preservation concern); added `Usage *wireUsage` field to `wireChunk`; added `usageFromWire(wireUsage) ai.Usage` (maps `prompt_tokens`→`Input`, `completion_tokens`→`Output` only — `total_tokens` and the two nested detail objects are deliberately never decoded, per R-ATS-026's charter boundary: `ai.Usage` has no corresponding field and this milestone must not invent one). `stream_state.go`: added `mapperState.usage ai.Usage` field; `applyChunk` now applies `chunk.Usage` (when non-nil) unconditionally, BEFORE the `hasChoice` early-return — the dedicated usage chunk (C4) has an empty `choices` array and would otherwise never reach any per-choice logic — overwriting `s.usage` wholesale (never merging, D10: "last populated usage chunk wins"); `buildCompletion()` now passes `s.usage` instead of the hardcoded `ai.Usage{}`. All 8 tests green on first run after this change (`go test -race -count=1 -v -run 'TestUsage' ./src/ai/openaicompat/...`). Safety net: full suite (176 top-level pre-slice-4) re-run green — zero regressions, including slice-2's own `TestMapperState_TerminalThenDeltaLessAndUsageChunks_ClosesCleanly` (S-ATS-032) which already exercised the usage-chunk shape via `applyChunk` directly.
- [x] 4.3 Evidence: `-race -count=1` twice; `go.mod` + lint.
  `go test -race -count=1 ./...` from `backend/agent/`: 2 consecutive green runs, all 3 packages. `grep -c '^require' go.mod` = 0. `gofmt -l ./src/ai/openaicompat/` empty. `make lint` → `0 issues`. `openaicompat` top-level test count: 176 (slice-3 baseline) → 183 (+7 new top-level; verified via `grep -c '^func Test' usage_test.go` = 7). Focused command `go test -race -count=1 -run TestUsage ./...` from `backend/agent/`: green, 7/7 top-level (9 incl. subtests). REFACTOR: none needed — no duplication or magic numbers introduced.

## Slice 5 — AI-28.4 Unknown and delta-less tolerance — COMPLETE

- [x] 5.1 RED: tests for R-ATS-017…019 (S-ATS-063…073): unrecognised SSE event type / non-chunk `object` skip, undeclared delta fields incl. `obfuscation` (C7) tolerated, zero-delta/no-content normalization with no min-count enforcement (S-ATS-070 inspection), keep-alives inert anywhere.
  Evidence: created `tolerance_test.go` (10 top-level tests: R-ATS-017 S-ATS-063…067, R-ATS-018 S-ATS-068/069, R-ATS-019 S-ATS-071…073). RED confirmed via `go test -race -count=1 -v -run 'TestTolerance|TestKeepAlive' ./src/ai/openaicompat/...` against the unmodified slice-4 code (before any chunk.go/stream.go edit): **2 tests genuinely FAILED** — `TestTolerance_UnrecognisedSSEEventType_SkippedBetweenTwoContentChunks` (`deltas = ["alpha" "INTRUDER" "omega"], want ["alpha" "omega"]`, S-ATS-063) and `TestTolerance_NonChunkObjectDiscriminator_SkippedBetweenTwoContentChunks` (`deltas = ["uno" "INTRUDER" "dos"], want ["uno" "dos"]`, S-ATS-066) — both fixtures deliberately carry an adversarial `"INTRUDER"` content string on the frame that must be skipped, so the RED is non-vacuous (a naive fixture with an empty-choices no-op frame would have passed by accident, since the frame would have contributed zero events either way; verified this risk BEFORE writing the fixtures, per the "vacuous-pass catalogue" discipline). The other 8 tests passed immediately (S-ATS-064/065/067's undeclared-field tolerance, S-ATS-068/069's delta-less/empty-content normalization, S-ATS-071/072/073's keep-alive inertness), as expected — non-vacuity for those proven separately (see 5.2).
- [x] 5.2 GREEN: implement skip/tolerance paths in `chunk.go`/`stream_state.go`.
  `chunk.go`: added `Object string` field to `wireChunk`; added `chunkObjectDiscriminator = "chat.completion.chunk"` const (C1); added `wireChunk.isChunk() bool` returning true when `Object` is absent OR matches the discriminator, false only when PRESENT and MISMATCHED. `stream.go`'s `run()`: added `if frame.Event != defaultEventType { continue }` immediately after the frame loop's own `for`, before the sentinel check; added `if !chunk.isChunk() { continue }` immediately after `decodeChunk` succeeds, before `state.applyChunk`. Both tests green on first run after this change. Safety net: full suite (183 top-level pre-slice-5) re-run green — zero regressions.
  **Disclosed deviation from design.md D3's literal text**: D3 reads "object ≠ chat.completion.chunk (**or absent**) → skip". Implementing the "or absent" half literally would skip every one of this package's 176 pre-slice-5 chunk fixtures, none of which set `object` at all (`grep -c '"object"' *_test.go` confirmed zero hits across every streaming test file before this slice) — a safety-net-breaking regression, not a tradeoff. `isChunk()` therefore only skips a PRESENT, MISMATCHED value. This fully satisfies every WRITTEN scenario in R-ATS-017…019 (none tests the absent-object case — S-ATS-066's own text only names `"object":"chat.completion"`, a present value), and treats "absent" as R-ATS-021's own "recognized chunk missing a required field" charter (a later, unimplemented slice) rather than this loose skip rule's. Full reasoning recorded in `chunk.go`'s own `isChunk` doc comment.
  **Non-vacuity proof for the 8 already-passing tests** — two independent mechanisms, not one uniform mutation:
  - **Staged, reverted mutation** (undeclared-field tolerance, S-ATS-064/065/067, plus incidentally S-ATS-063/068/069 which also carry unmapped `role`/other fields on their fixtures): temporarily wrapped `decodeChunk`'s `json.Unmarshal` with a strict `json.NewDecoder(...).DisallowUnknownFields()`. `go test -race -count=1 -v -run 'TestTolerance' ./src/ai/openaicompat/...` → **7 of 9** tolerance tests FAIL: `unexpected error event (S-ATS-064)`, `unexpected error event (S-ATS-065)`, `deltas = [], want ["after"] (S-ATS-067)`, `drained kinds = [error], want [responsestart completion] (S-ATS-068)`, `drained kinds = [error], want [...] (S-ATS-069)`, plus S-ATS-063/066 (already known-RED, now RED for the compound reason that their own legitimate content chunks also carry `role`). Reverted; `git diff` on `decodeChunk` confirmed byte-identical to its pre-mutation form.
  - **Comparative fixture design, no mutation needed** (keep-alive inertness, S-ATS-071…073): keep-alive inertness is a `decoder.go` (AI-27, frozen) property this slice cannot and must not mutate. S-ATS-071's own plain-vs-keepalive twin-transcript comparison is inherently non-vacuous without an artificial mutation: any regression that made keep-alives NOT inert would make the two parallel drains diverge (different kind/sequence/delta at some index), failing the comparison for real.
- [x] 5.3 Evidence: `-race -count=1` twice; `go.mod` + lint.
  `go test -race -count=1 ./...` from `backend/agent/`: 2 consecutive green runs, all 3 packages. `grep -c '^require' go.mod` = 0. `gofmt -l ./src/ai/openaicompat/` empty. `make lint` → `0 issues`. `openaicompat` top-level test count: 183 (slice-4 baseline) → 193 (+10 new top-level; verified via `grep -c '^func Test' tolerance_test.go` = 10). Focused command `go test -race -count=1 -run 'TestTolerance|TestKeepAlive' ./...` from `backend/agent/`: green, 10/10. S-ATS-070 [inspection] discharged: `chunk.go`/`stream_state.go` read confirms no code path enforces a minimum delta count, minimum block count or non-empty-text requirement anywhere. REFACTOR: none needed.

## Slice 6 — AI-28.5 Protocol-order violations — COMPLETE

> Depends only on 28.1.1 and is file-disjoint from slices 3–5; MAY be developed on a branch parallel to slice 2 if preferred. This plan keeps the linear chain (PR6→PR5) per the proposal's delivery table for review predictability.

> **Two coordinator rulings executed before 6.1** (full rationale: Engram obs #2497). **Ruling A** — `S-ATS-046` spec rev 3: corrected from an unterminated-sentinel-frame reading (which collided with AI-27's frozen EOF discipline) to a well-formed sentinel frame at exact EOF, the reading slice 3's landed tests already implemented. Commit `10a171d` (spec) + `fe6033d` (doc-comment alignment in `terminal_test.go`, zero test-logic change). **Ruling B** — the `object` discriminator three-way rule + fixture normalization precondition: every chunk fixture across the package (86 literal occurrences in 8 files, plus `bridge_test.go`'s two render helpers) normalized to carry `"object":"chat.completion.chunk"` first, with the full suite green (commit `7bc1ec8`), discharging the safety-net conflict that drove slice 5's pragmatic "absent OR matching" deviation — then the final rule implemented (commit `19e9ab5`): absent → skip (R-ATS-017 family), present-and-mismatched → skip (S-ATS-066, unchanged), present-and-correct-but-missing-a-required-field → malformed terminal (S-ATS-081, R-ATS-021).

- [x] 6.1 RED: table-driven tests for R-ATS-020…022 (S-ATS-074…085): 5-row violation table incl. row-1 contrast vs S-ATS-032 (S-ATS-074), malformed-known-type vs unknown-type distinguished in one test (S-ATS-082), `recover()` no-panic probe (S-ATS-083), bound/existence-check inspection (S-ATS-085).
  Evidence: created `violation_test.go` (11 top-level tests). Genuine RED confirmed via `go test -race -count=1 -v -run 'TestProtocolViolation'` against the unmodified slice-5 code: `TestProtocolViolation_AbsentObjectDiscriminator_SkippedBetweenTwoContentChunks` failed for real (`deltas = ["alpha" "INTRUDER" "omega"], want ["alpha" "omega"]`); `TestProtocolViolation_MissingRequiredModelField_MalformedTerminal`'s first fixture draft passed VACUOUSLY via an unrelated mechanism (D9's zero-value-`FinishReason` rejection when no terminal chunk is ever observed) — caught before trusting the green, fixture reshaped so the offending chunk carries the terminal `finish_reason` itself, isolating the missing-model concern, which then failed for real (clean completion instead of a terminal failure). The five-row table (`TestProtocolViolation_StructuralViolationTable`, one `t.Run` per row per S-ATS-079's own "one table-driven test" wording) failed for real on rows 1–4 (`INTRUDER`-shaped and count/id-mismatch assertions); row 5 passed on first execution — proven already covered by the existing `ai.NewResponseStart` identity-rejection path (S-ATS-015's own mechanism extended to a chunk that is simultaneously first and terminal), documented rather than forced. `TestProtocolViolation_NegativeIndexWithContent_MalformedTerminal` (R-ATS-020 row 4's other named shape) failed for real. `TestProtocolViolation_InvalidJSON_MalformedTerminal` (S-ATS-080) and `TestProtocolViolation_UnknownTypeVsBrokenKnownType_DifferentOutcomes` (S-ATS-082) both passed on first execution via the pre-existing decode-error fallback path — documented, not forced; the fallback's cause identity was still clarified (see 6.2). S-ATS-085 [inspection] discharged by review: no new map/slice access introduced by any of this slice's code, only nil-guarded pointer derefs and plain comparisons.
- [x] 6.2 GREEN: implement violation table + D7 failure builder in `stream_state.go`.
  `chunk.go`: `wireChoice.Index *int` (C2's own required per-choice field) + `hasValidIndex()`. `stream_state.go`: `mapperState.responseID` (remembers the established identity for row 3); four new unexported causes (`errDeltaAfterClose` row 1, `errDuplicateClose` row 2, `errSecondResponseStart` row 3, `errDeltaWithNoOpenBlock` row 4 — doc 0002's own structural name), all wrapping `ai.ErrMalformedResponse`, none exported (S-ART-054 untouched, R-ATS-025). `applyChunk` extended: row 3's identity-mismatch check ahead of identity establishment; a `terminalSeen`-guarded block catching rows 1/2 before finish_reason/content are otherwise processed; row 4's index check ahead of block-minting. Row 5 needed no code — the existing `errMalformedIdentity` path already covers it. `stream.go`: `decodeChunk`'s error branch now uses a dedicated `errMalformedChunkJSON` cause instead of reusing `errIncompleteStream` (a stream-ended-early sentinel) — a disclosed, zero-behavior-change clarity fix (`Category()` recognizes neither identity, so the fallback category applied identically before and after). All RED tests green after this change; S-ATS-083's recover-probe (`TestProtocolViolation_NoPanicAcrossEveryViolatingRow`, driven through the direct `applyChunk` seam so a real `recover()` can catch a real panic — a probe wrapping the full `Stream()` pipeline would only observe a separate goroutine, which an uncaught panic crashes before any caller-side `recover()` could run) proven non-vacuous via a staged, reverted mutation (dropped `hasValidIndex`'s nil check → genuine nil-pointer panic → correctly reported as a test failure, not a binary crash; reverted, `git diff` byte-identical after).
- [x] 6.3 RED+GREEN: cross-slice charter/citation scenarios R-ATS-025…028 (S-ATS-094…107): sentinel reuse via `errors.Is`, delivery-path handover, closed 9-member `FailureCategory` vocabulary, `go.mod` byte-identical/stdlib-only imports, no reasoning/tool-call paths, C1…C8 citation resolution (S-ATS-102…104), C6 no-error-frame path (S-ATS-105…107).
  Created `charter_test.go` (4 top-level tests). All passed on first execution — each proves an existing accessor (`Category()`, `Delivery()`, `errors.Is`) or an already-implemented skip rule (this slice's own object-discriminator rule for S-ATS-105; slice 1's event-type skip rule for S-ATS-106) from the charter/citation lens, none needing new production code; documented, not forced. S-ATS-094 derives its expected category from `Category(ErrTruncated)` itself (not a hardcoded literal) against a GENUINELY truncated transcript (mid-data-line cut, a real `ErrTruncated` from `Decoder.Finish()` — not S-ATS-048's own "clean framing, sentinel never observed" shape, which takes the unrelated `errIncompleteStream` cause). S-ATS-096 is not a new test: `truncation_test.go`'s own pre-existing `TestTruncation_ResponseStartOnlyNoSentinel_PartialOutputFalse` (S-ATS-050) already asserts exactly its two facts on exactly its shape. Inspection scenarios discharged by review: S-ATS-097 (`ai.FailureCategories()`'s own doc comment: nine members; this slice never references a tenth), S-ATS-098 (`git diff --stat` on `go.mod` against the slice-5 tip is empty), S-ATS-099 (every import across this slice's files is stdlib or in-repo `src/ai`), S-ATS-100 (no reasoning/tool-call event code paths; `tool_calls`/`function_call` appear only as C2 finish-reason enum string members, pre-existing since slice 2), S-ATS-101 (usage handling untouched since slice 4), S-ATS-102/103 (every C-claim citation in this slice's new/modified comments resolves within C1…C8; added an explicit C2 citation to `wireChoice.Index`/`hasValidIndex`, which initially cited only the requirement, not the underlying claim), S-ATS-104 (`doc.go`'s own pinned commit, wrapped across two comment lines, confirmed byte-matching `citations.md`'s), S-ATS-107 (no symbol/branch/comment in this package claims to map a Chat Completions in-stream error payload).
- [x] 6.4 Evidence: `-race -count=1` twice; `go.mod` + lint.
  `go test -race -count=1 ./...` from `backend/agent/`: 2 consecutive green runs, all 3 packages (`agenttest`, `ai`, `ai/openaicompat`). Focused command `go test -race -count=1 -run 'TestProtocolViolation|TestCharter' ./...`: green, 11/11 top-level (plus subtests). `grep -c '^require' go.mod` = 0. `gofmt -l` clean on every slice-6 file (same pre-existing, unrelated `src/ai/completion_test.go` finding disclosed-not-fixed, carried forward unchanged since slice 1). `make lint` → `0 issues`. `openaicompat` top-level test count: 193 (slice-5 baseline) → 204 (+11 new top-level; verified via `grep -c '^func Test' violation_test.go charter_test.go` = 7 + 4). REFACTOR: none needed beyond the disclosed `errMalformedChunkJSON` clarity fix (6.2), already covered by the same green runs.

## Slice 7 — AI-28.6 Pre-decode checks — COMPLETE

> GATE: verify AI-32.1 (failure-status taxonomy) landed before 7.1. If unlanded when slices 1–6 are green: record a carryover naming AI-32.1 as blocker; do not author a substitute taxonomy.
  Evidence (task 7.0 gate check, performed first, mechanically): `mapResponse` and `classifyStatus` confirmed present in `backend/agent/src/ai/openaicompat/failure_map.go` (AI-32 stage 1 merge, commit `ec1a4da`, already on this branch). `testdata/errormap/` confirmed carrying all 11 status fixtures (`openai_400/401/403/404/408/422/429/500/502/503/504.http`). Gate DISCHARGED — proceeded per R-ATS-024; AI-28.6 lands normally, not as a carryover.

- [x] 7.1 RED: tests for R-ATS-023…024 (S-ATS-086…093): non-stream content type refused pre-decode with a bounded, named-constant excerpt (case/parameter-tolerant match, credential-scan guard S-ATS-089), missing content-type refused, failure statuses routed pre-decode (zero content events first).
  Evidence: created `predecode_test.go` (7 top-level tests: S-ATS-086/087/088/090 for R-ATS-023, S-ATS-091/092/093 for R-ATS-024; S-ATS-089 is `[inspection]`, discharged by review in 7.3, not a test). Two-stage RED, per strict-tdd's sanctioned forms: (1) `go vet ./src/ai/openaicompat/...` — compile failure, `undefined: nonStreamContentType` at 3 call sites (S-ATS-086/088/090) — reference to not-yet-existing production code. (2) After adding only the unwired helpers (`nonStreamContentType`, `isStreamContentType`, `refuseNonStreamContentType`, no call from `Stream`) so the package would compile, `go test -race -count=1 -v -run 'TestPreDecode' ./src/ai/openaicompat/...` against that unwired state: 6 of 7 top-level tests **genuinely FAILED** for the right reason — `Stream() returned a non-nil channel..., want nil` on S-ATS-086/090/091/092 and all 11 rows of S-ATS-093's table, and `errors.As(err, &typed) = false` on S-ATS-088 — because `Stream` did not yet call either check, so it always created the carrier regardless of status or content type. Non-vacuity discipline applied to the two "would-otherwise-decode" fixtures (S-ATS-090, S-ATS-092): each transcript is a fully **completable** stream (content chunk + terminal chunk + sentinel, one delta reading `"should never decode"`) rather than a truncated or trivial one, so an unwired implementation would have decoded it all the way to a real `Completion` carrying that delta — a result unambiguously different from the required pre-stream refusal, not merely "some failure occurred" (vacuous-pass shapes 1/4/5 avoided: the `ch == nil` assertion is the structural, positive discriminator, not an incidental category match). S-ATS-087 (the accept path) **passed already** at this unwired stage — expected and disclosed, not forced: with no rejection logic yet, every content type was implicitly "accepted" by omission; the assertion stays meaningful going forward because a broken `isStreamContentType` that rejected everything would newly fail it post-wiring (documented, matching slice 6's own "passed on first execution, documented not forced" precedent).
- [x] 7.2 GREEN: implement pre-decode checks in `stream.go`.
  `stream.go`: added `streamContentType` (`"text/event-stream"`, R-ATS-023's own named constant), `isStreamContentType` (`mime.ParseMediaType`, which already lower-cases the media type and separates parameters — verified interactively against `go doc mime.ParseMediaType` and a throwaway probe program before relying on it: `""` → error, `"TEXT/EVENT-STREAM; charset=utf-8"` → `"text/event-stream"`), the `nonStreamContentType` typed cause (`Error()` embeds the excerpt — deliberately the opposite posture from `capture.go`'s own `capturedBody`, whose `Error()` deliberately never reproduces its bytes for a different obligation, R-AEM-016; `ai.Failure.Error()` itself is frozen to `"provider failure: " + category` and proven to never reproduce a wrapped cause's text, R-AIP-009/S-AIP-028…031 — so the excerpt is reachable one hop via `errors.Unwrap`/`errors.As`, never through the outer failure's own `Error()`), and `refuseNonStreamContentType` (captures via `captureBody`, AI-32.1's own bounded-capture machinery reused verbatim rather than a second bound — `captureLimit`, already named and documented, is the excerpt bound S-ATS-089 requires named, discharging design.md's "named excerpt-bound constant" phrasing without inventing a duplicate). `Stream` extended, strictly between `Do` and channel creation (design.md D1's own numbered step 6, before step 7) — first `mapResponse(resp, time.Now())` (R-ATS-024, returns nil untouched on 2xx), then `isStreamContentType` (R-ATS-023) — both refusals `return nil, failure` directly: `ai.PreStreamFailure`, never something observed on the carrier, per R-ATS-025's own handover rule ("before the carrier is returned") and `mapResponse`'s own pre-existing, AI-32.1-frozen contract (`ai.PreStreamFailure` only — `TestFailureMap_StatusCategoryTable` already pins `Delivery() == ai.DeliveryPreStream` on every row). All 7 top-level tests green after this change (`go test -race -count=1 -v -run 'TestPreDecode' ./src/ai/openaicompat/...`: 18/18 incl. subtests). Safety net: full suite (230 top-level pre-slice-7... see 7.3) re-run green — zero regressions. REFACTOR: added a "Pre-decode checks now gate the carrier (AI-28.6, R-ATS-023/024)" section to `stream.go`'s own top-of-file doc comment, matching this file's established per-requirement narrative convention (mirroring "Text mapping now lives in stream_state.go", "outputPreceded is now tracked" from earlier slices) — no behavior change, tests re-run green after.
- [x] 7.3 Evidence: `-race -count=1` twice; `go.mod` + lint.
  `go test -race -count=1 ./...` from `backend/agent/`: 2 consecutive green runs, all 3 packages (`agenttest`, `ai`, `ai/openaicompat`). Focused command `go test -race -count=1 -run TestPreDecode ./...`: green, 7/7 top-level (18/18 incl. subtests). `grep -c '^require' go.mod` = 0 (exits 1, the expected `grep` posture for a genuine zero-match count — same benign shape every earlier slice's own evidence already carries). `git diff --stat -- go.mod` empty — byte-identical. `gofmt -l .` from `backend/agent/`: only the same pre-existing, unrelated `src/ai/completion_test.go` finding (disclosed-not-fixed since slice 1); `stream.go` and `predecode_test.go` both clean. `make lint` (`golangci-lint run --config=.golangci.yml ./...`, which also runs `go vet`) → `0 issues`. `openaicompat` top-level test count: 223 (post-AI-32-merge baseline, captured before any slice-7 edit) → 230 (+7 new top-level; +18 incl. subtests, verified via `grep -c '^func Test' predecode_test.go` = 7 and the table test's 11 `statusTableCases` rows). S-ATS-089 `[inspection]` discharged by review: the excerpt bound is `captureLimit` (capture.go, already a named, documented constant — R-AEM-015 — reused rather than reinvented); the credential-scan guard (`credential_scan_test.go`'s `scanCredentialSurface`) requires no edit to cover this slice, by its own established, already-proven design (S-ART-014: every `_test.go` file carrying the external `package openaicompat_test` clause is swept automatically, with no per-file registration) — `predecode_test.go` uses the internal `package openaicompat`, this milestone's own established convention for every prior slice's test file (stream_test.go, chunk_test.go, violation_test.go, charter_test.go, …), so it is out of that scan's scope by the same precedent `credential_scan_test.go`'s own file comment already documents for AI-25's internal-package fixtures; `nonStreamContentType`/`refuseNonStreamContentType` are also structurally incapable of reproducing the request's own credential — they read only `resp.Header.Get("Content-Type")` and `resp.Body`, never `c.credential`/`Credential`, so R-ATS-023's "MUST NOT reproduce credential material" holds by construction, not by content-scanning (which is out of a mechanical implementation's reach for arbitrary vendor/proxy body text). REFACTOR: none needed beyond 7.2's own disclosed doc-comment addition.

## Milestone close

- [x] 8.1 Full suite `go test -race -count=1 ./...` from `backend/agent/`, run twice.
  Both runs green, all 3 packages, at the slice-7 tip (same evidence as 7.3's own two runs — not a third/fourth repetition, the identical command against the identical unchanged tree).
- [x] 8.2 Confirm 28/28 requirements and 107/107 scenarios (89 [test] / 18 [inspection]) discharged; `go.mod` unchanged; AI-00 import guards pass; AI-28.6 landed or recorded as a named carryover.
  **Requirements**: `grep -c '^### R-ATS-' specs/ai-provider-text-stream/spec.md` = 28. Every one homed per design.md's own "Requirement homes" table (28/28, zero homeless) and landed across slices 1–7, all now `COMPLETE` in this file.
  **Scenarios, mechanically re-counted**: extracted every `**S-ATS-0NN** *[test]*`/`*[inspection]*` bullet from `spec.md` via `grep -oE`: **107** total, **89 [test]** / **18 [inspection]** — matches the spec's own self-reported tally exactly (independent re-derivation, not a trust of the stated number).
  **[test] coverage, mechanically cross-referenced**: extracted every `S-ATS-[0-9]{3}` token literally present in any `*_test.go` file in `src/ai/openaicompat/` (`grep -ohE 'S-ATS-[0-9]{3}' *_test.go | sort -u`) and diffed against the full 107-ID list. **88/89 `[test]` scenarios have a direct, literal test-file citation reachable this way** (the 89th, `[inspection]`-adjacent citation shapes aside). The **one exception is `S-ATS-038`** (R-ATS-010, slice 2, "no completion event is emitted before the terminal chunk arrives"): zero literal references anywhere in the package, and direct inspection confirms no *unlabeled* test proves its specific claim either — `TestMapperState_ThreeContentChunks_EmitsThreeOrderedDeltas` (`stream_state_test.go`) drives three content-only chunks through `mapperState.applyChunk` directly but never calls `state.buildCompletion()` at all (the *only* call site that can ever construct a `Completion`, gated to the sentinel by design.md D4/D9), so the absence of a `Completion` event in that test's own result is structurally guaranteed regardless of correctness — it cannot distinguish an implementation that gets R-ATS-010 right from one that does not (vacuous-pass shape 1, traced, not guessed, per this milestone's own "trace why it passed before trusting it" discipline). **This is a genuine, pre-existing gap dated to slice 2's own close, predating slice 7 and outside this session's assigned tasks (7.x/8.x only)** — reported here rather than silently patched, per the "enumerate any scenario without a mapped test" instruction; not fixed in this session because doing so would mean editing an already-merged, already-closed prior slice's own test file outside the assigned work-unit boundary. Flagged as a risk below for the maintainer/verify phase to decide a follow-up. **RESOLVED (coordinator, 2026-08-04, same session, pre-verify)**: `TestStream_FiveNullFinishChunksNoTerminal_EmitsNoCompletion` added to `stream_text_test.go` — a positive drain-and-count over a five-null-finish-chunk transcript ending before any terminal chunk (all five deltas asserted present, zero Completion events anywhere, truncation terminal last). Bite proven by a staged, reverted silent-success mutation on stream.go's transport-EOF branch, which emitted a premature `stop` Completion and failed the test naming exactly that defect (`kinds: [... 5×text_delta completion]`). [test] coverage is now **89/89**.
  **[inspection] coverage**: all 18 discharged. Nine were already literally test-file-cited by an earlier slice's own evidence (`S-ATS-019/022/042/047/070/097/098/102/107`). The remaining nine were independently, mechanically re-verified in this session: `S-ATS-023` (doc.go's AI-26→AI-28 correction confirmed at task 1.2; `TestPolicy_NoNewSentinelsExported`'s live `go/ast` allowlist scan — not a manually maintained list — still passes with zero new exported sentinels, confirmed via this session's own full-suite runs); `S-ATS-043` (task 2.0's own gate-check evidence); `S-ATS-085` (task 6.1's own review); `S-ATS-089` (this session, 7.3 above); `S-ATS-099` (re-swept every non-test `.go` file's own import block in `src/ai/openaicompat/`: every import is stdlib or `github.com/cachicamas/backend/agent/src/ai`, zero third-party); `S-ATS-100` (grepped for `EventKindReasoning`/`EventKindToolCall` across `stream.go`/`chunk.go`/`stream_state.go`: zero hits; `tool_calls`/`function_call` appear only as `finishReasonEnum` map keys, the finish-reason gate's own allowlist — `wireDelta` maps only `Content`, so those fields are silently dropped by decode itself when present in a delta, the totalizing form of R-ATS-017's skip rule); `S-ATS-101` (`stream_state.go:202`, `s.usage = usageFromWire(*chunk.Usage)` is a plain overwrite, never a merge; `wireUsage` maps exactly `prompt_tokens`/`completion_tokens`, `total_tokens` and both detail objects deliberately undecoded); `S-ATS-104` (the hash is wrapped across `doc.go` lines 80–81 — `d4fb706e6e05d4cc9f1b33ca5` + `9b6e4f3e8edd439` — concatenates to byte-match `citations.md`'s own pinned commit exactly, re-verified this session after the naive single-line grep first (correctly) found nothing). `S-ATS-103` (every `C[0-9]` citation across every non-test `.go` file resolves within C1…C8, zero out-of-range hits, 4 `dialect-conventional` labels present) is reported at **high confidence from this mechanical range check, not from an exhaustive manual re-read of every comment describing field names/framing/terminal behavior** across all 7 slices' own files — disclosed distinctly rather than folded into a blanket "confirmed", since that exhaustive audit is outside this session's own assigned scope.
  **go.mod**: `grep -c '^require' go.mod` = 0; `git diff --stat -- go.mod` against the pre-AI-28 baseline is empty.
  **AI-00 import guards**: `S-ATS-099`'s own sweep above is the import-guard check for this package; the full-suite run (8.1) includes `agenttest`'s own AI-00-scoped guards, green.
  **AI-28.6**: landed normally this session (7.0's gate discharged, AI-32.1 present) — not a carryover.
