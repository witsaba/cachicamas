# clean-frontend-keep-only-landing-page — apply-progress

> This file is a one-line pointer to the source-of-truth apply-progress
> observation in Engram. The Engram observation is the canonical record;
> this file exists only so a future agent browsing the worktree root
> sees a hint and looks in Engram rather than treating this stale file
> as truth.
>
> Source of truth: `mem_search(query="sdd/clean-frontend-keep-only-landing-page/apply-progress", project="cachicamas")`
> Branch: `feat/clean-frontend-keep-only-landing-page`
> 12 work-unit commits + 1 refactor-commit ahead of `main`.
